# Redesign R2 — Client Surface Map

Generated by the r2-client-surface-map workflow (10 parallel readers). Drives the wave-based conversion. Datepicker decision: KEEP react-day-picker (themed) — compat merely wraps it.



---

# Surface: dashboard+projects-board

Perfect! Now I have all the information I need. Let me compile the comprehensive analysis:

---

## DASHBOARD + PROJECTS BOARD CONVERSION ANALYSIS

### (1) ROUTE FILE & DATA FETCHING

**Route:** `/app/(portal)/dashboard/page.tsx` — Auth & branching
- **Auth guard:** `requireProfile()` — gates all client/staff access
- **Branch logic:** 
  - If `profile.role === "client"` with `client_id` → delegates to `<ClientDashboard clientId={clientId} userName={profile.full_name} />`
  - Else (staff/admin) → renders staff home grid
  
**Data shapes fetched:**

*Program clients* (via `components/client/client-dashboard.tsx` line 41–97):
- `client_clients` → `client_type, program, start_date, comms_channel` (maybeSingle)
- `client_partner` → `full_name, email, avatar_url` (maybeSingle)
- `checklist_items` → `id, phase_label, title, link_url, assignee, is_done, sort_order` (kind='onboarding', ordered)
- `milestones` → `id, title, phase_label, status, sort_order` (ordered by sort_order)
- `metric_entries` → `period, value_numeric, value_text, definition:metric_definitions!inner(label, unit, sort_order)` (60 rows desc, inner join)
- `deliverables` → `id, title, status, type` (4 rows, desc created_at)
- `reports` → `id, title, summary` (1 row desc period_end, maybeSingle)
- `updates` → `id, title, body, pinned, created_at` (6 rows desc created_at)
- `deliverables.status='needs_review'` count → review badge

*Project clients* (via `components/client/projects-board.tsx` line 32–43):
- `client_clients` → `name` (maybeSingle)
- `projects` → `id, title, description, status, priority, start_date, due_date, target_date, created_at` (ordered desc)
- `deliverables` → `id, title, type, status, project_id` (ordered desc)

---

### (2) COMPONENT TREE TO RE-SKIN

**Program client dashboard** (`client-dashboard.tsx`):
```
<div> (flex col gap-8)
  ├─ Greeting + CTA row
  │  ├─ <Greeting name={firstName} monthLabel={monthLabel} />  (R0: already present)
  │  └─ [Live pill badge] (inline-flex amber border/bg)
  ├─ Review banner (amber tone, icon + text + button CTA)
  ├─ KPI band (2–4 MetricValue + MetricDelta cards, grid 2col → 4col)
  │  └─ Card (hover shadow on link)
  │     ├─ MetricValue (size="hero")
  │     └─ MetricDelta (variant="badge")
  ├─ Main grid (2fr left / 1fr right)
  │  ├─ LEFT: Start here, 90-day program, Latest deliverables
  │  │  └─ Card
  │  │     ├─ ClientChecklist (accordion, toggle_checklist_item RPC on client items)
  │  │     ├─ Milestones (grid with colored top-border by status, StatusChip)
  │  │     └─ Deliverables list (StatusChip per row)
  │  └─ RIGHT: Report card (dark solid with ember glow), Updates, Partner
  │     ├─ [Dark report card] (custom inline styles, blue-black overlay)
  │     ├─ Card (Updates list)
  │     └─ Card (Partner avatar + details + Book a call button)
```

**Project client projects board** (`projects-board.tsx`):
```
<div> (flex col gap-8)
  ├─ Greeting + "Add project" button row
  │  ├─ <Greeting name={firstName} monthLabel="" />  (no monthLabel)
  │  └─ <ProjectDialog trigger={<Button>Add project</Button>} /> (create path)
  ├─ [Empty state OR]
  └─ Projects grid (lg:grid-cols-2)
     ├─ Card (per project)
     │  ├─ Title + target_date/due_date
     │  ├─ [StatusChip kind="project"] (READ-ONLY: proposed|active|in_review|complete)
     │  ├─ [PriorityBadge] (visual: low|medium|high|urgent dot scale)
     │  ├─ Description (if present)
     │  ├─ Deliverables list (nested, StatusChip per deliverable)
     │  └─ "Edit" button → <ProjectDialog project={p} /> (update path)
```

**Existing R0 components already covering pieces:**
- `StatusChip` (kind-driven recipes: project, deliverable, milestone — READ-ONLY on client surfaces)
- `MetricValue` / `MetricDelta` (KPI numerals + deltas)
- `Card` / `CardContent` (container)
- `PersonAvatar` (partner, logo)
- `Button` (CTAs)
- `Badge` (secondary/amber variants)
- `EmptyState` (no projects)
- `Stagger` / `StaggerItem` (entrance anim)

---

### (3) CLIENT WRITE PATHS — PRESERVED VERBATIM

**On projects board surface** (components/client/project-dialog.tsx):

1. **Create project** (line 97–102):
   - Server action: `createProjectAction(input: ProjectCreateValues)` (`lib/actions/projects.ts:39`)
   - Calls RPC: `supabase.rpc("create_project", { p_title, p_description, p_priority, p_target_date })`
   - Validation: `projectCreateSchema` (title required, optional description, priority enum, target_date optional date)
   - Result toast + router.refresh()

2. **Update project** (line 89–96):
   - Server action: `updateProjectAction(input: ProjectUpdateValues)` (`lib/actions/projects.ts:77`)
   - Calls RPC: `supabase.rpc("update_project", { p_id, p_title, p_description, p_priority, p_target_date })`
   - Validation: `projectUpdateSchema` (extends create schema + id UUID)
   - **Status & due_date NOT passed** — RPC has no params for them (status lock)
   - Result toast + router.refresh()

3. **Checklist toggle** (components/client/client-checklist.tsx:72):
   - Client-side: `createClient().rpc("toggle_checklist_item", { item_id })`
   - Optimistic flip + revert on error
   - No audit; keeps local state in sync

---

### (4) INVARIANT TOUCHPOINTS

| Invariant | Location | Client-visible behavior |
|-----------|----------|------------------------|
| **Project status lock** | `project-dialog.tsx` (line 44–45 comment); `supabase/migrations/20260617183715_lock_project_status_to_staff.sql` | StatusChip `kind="project"` is READ-ONLY on card (lines 113, 131); client form never includes status field; RPC has no status param |
| **Due date (staff-only)** | `project-dialog.tsx` (line 46); RPC signature (supabase/20260618143121_project_advanced_options.sql line 76–77) | Client never sees/edits due_date; only target_date (client's desired date) in form |
| **Deliverable status chips** | `projects-board.tsx` (line 131) & `client-dashboard.tsx` (line 308) | StatusChip `kind="deliverable"` is always READ-ONLY (no client form ever modifies deliverable status directly) |
| **Checklist team items** | `client-checklist.tsx` (line 49–51) | assignee="team" items auto-count as done; show dashed-border icon + "We'll take care of this"; client cannot toggle them |
| **Internal threads (hidden)** | Not directly on dashboard; separate /calls-notes surface | No task/thread control on this surface; no time-tracking UI |

---

### (5) GLASS PLAN: Surface texture per rule

**KPI Band (program client only):**
- `MetricValue` + `MetricDelta` cards → **GLASS with scrim** (strong=true)
- Dark mode: ember-obsidian recipe (dark=true, strong=true)
- Light mode: warm glass (strong=true)
- Hover: lift animation (y:-4)

**Dense/tabular surfaces (READ-ONLY):**
- Checklist rows → **SOLID** (card bg, borders for dividers)
- Milestones grid → **SOLID** (card bg, colored top-border accent)
- Deliverables list → **SOLID** (card content, row dividers)
- Projects board cards → **SOLID** (card bg, nested deliverables list)

**Summary/text carriers (program dashboard right rail):**
- Latest report card → **DARK SOLID with ember glow underlay** (intentional immersive motif; stays opaque)
- Updates panel → **SOLID** (white/light card)
- Partner card → **SOLID** (white/light card)

**Review banner:**
- Amber warning tone, **SOLID** (amber-100 bg light, amber-50/amber-200 border)

---

### (6) CONVERSION CHECKLIST: Both surfaces

#### Program Dashboard (ClientDashboard component)
- [ ] Replace `<Greeting>` + hero text row with redesign `Measure` + `Eyebrow` + `rd-display` heading
- [ ] Replace CTA button row (Book a call + Live pill) with Fluent Button + redesign chrome
- [ ] Replace review banner inline styles with redesign `Banner` (amber tone, solid bg)
- [ ] **KPI band:** Wrap `Card` in `GlassSurface dark={onDark} strong ember` → `rd-kpi-grid` on parent
  - Replace `MetricValue` + `MetricDelta` with `rd-display` + `StatusPill`/`DeltaChip` from redesign/data-ui
- [ ] Replace left-rail Cards with `rd-solid` Panel (`rd-dash-grid` parent wrapper)
  - Start here: `ClientChecklist` stays interactive (redesign styling: `rd-eyebrow` + `rd-tnum` for %);  checkbox icons + status pills
  - 90-day program: milestones grid → `rd-ms-grid`; top-border colors; status pills instead of StatusChip
  - Latest deliverables: row list → status pills
- [ ] Replace right-rail report card: convert inline dark-bg styles → `rd-solid--dark` container + `<GlassSurface ember>` as absolute underlay (line 227)
- [ ] Updates card & Partner card → `rd-solid` panels with `rd-eyebrow` labels

#### Projects Board (ProjectsBoard component)
- [ ] Replace `<Greeting>` with redesign variant (no monthLabel)
- [ ] Replace `<Button>` "Add project" with Fluent Button (redesign skin)
- [ ] Replace `<EmptyState>` icon/text with redesign empty variant
- [ ] **Projects grid:** Each Card → `rd-solid` panel
  - Title + date info → plain text + `rd-eyebrow` (if needed)
  - Flex row: title (left) + status chip (right) + priority badge
    - Status: `StatusPill` from redesign/data-ui (no write; kind="project" read-only)
    - Priority: `PriorityBadge` (no redesign variant needed; use existing dot scale)
  - Description: plain text
  - Deliverables list → nested rows (status pills) — no changes
  - "Edit" button → Fluent Button `variant="ghost"`

#### ProjectDialog component (shared create/edit form)
- [ ] Form structure stays the same (title, description, priority select, target_date picker)
- [ ] **Preserve RPC calls exactly:** `createProjectAction` / `updateProjectAction`
- [ ] Button state + loading spinner (use Fluent `Button loading=true`)
- [ ] Input/Textarea/Select/DatePicker → use existing UI primitives (no redesign swap needed; form inputs are stable)

#### ClientChecklist component
- [ ] Phase accordion logic stays the same
- [ ] Checkbox icons: checked → solid amber circle + white check; unchecked → border circle
- [ ] Text styling: phase labels → `rd-eyebrow` or muted uppercase; item text → inherit (gray if team, normal if client)
- [ ] Status pills (You / 4Pie) → use `StatusPill` from redesign/data-ui
- [ ] "Open" link button → Fluent Button size="small"

---

### EXACT FILE PATHS & REF LINES

| File | Component(s) | Key lines |
|------|-------------|-----------|
| `app/(portal)/dashboard/page.tsx` | Auth branch, staff home | 29–35 (client check), 42–97 (queries) |
| `components/client/client-dashboard.tsx` | Program dashboard render | 45–50 (type check), 150–406 (render) |
| `components/client/projects-board.tsx` | Project client board | 25–154 (data + render) |
| `components/client/project-dialog.tsx` | Create/edit form + RPCs | 53–177 (form, write paths) |
| `components/client/client-checklist.tsx` | Onboarding checklist, toggle RPC | 68–78 (toggle), 107–227 (render) |
| `components/client/greeting.tsx` | Time-of-day greeting | 10–37 (client component) |
| `lib/actions/projects.ts` | createProjectAction, updateProjectAction | 39–75, 77–124 |
| `components/redesign/keystones/dashboard.tsx` | **Reference impl:** redesign KPI band, glass, panels | 104–136 (KPI glass), 304–309 (Panel wrapper) |
| `components/redesign/data-ui.tsx` | StatusPill, DeltaChip, Progress | 39–69, 75–111, 114–134 |
| `components/redesign/ui/surfaces.tsx` | GlassSurface, Eyebrow, Measure | 17–129 |
| `components/ui/status-chip.tsx` | STATUS_MAP recipes (read-only) | 18–69 |
| `components/projects/priority-badge.tsx` | Priority dot scale | 8–35 |
| `supabase/migrations/20260618143121_project_advanced_options.sql` | create_project / update_project RPC signatures | 28–107 |

---

### SUMMARY: What stays, what changes

**STAYS VERBATIM:**
- Server actions: `createProjectAction`, `updateProjectAction`
- Client RPCs: `create_project(p_title, p_description, p_priority, p_target_date)`, `update_project(p_id, p_title, p_description, p_priority, p_target_date)`, `toggle_checklist_item(item_id)`
- Form validation (Zod schemas)
- Data shapes (all queries stay the same)
- Auth guards (`requireProfile`, `requireClientAccess`)
- StatusChip read-only behavior (project status never client-editable)
- Checklist toggle logic (optimistic, RPC-backed)

**CHANGES TO STYLING ONLY:**
- Swap inline Tailwind + Card components for redesign glass/solid/panel surfaces
- Use `Eyebrow`, `StatusPill`, `DeltaChip` from redesign/data-ui
- Replace MetricValue/MetricDelta with `rd-display` + `rd-tnum` + chip variants
- Update button to Fluent Button (redesign skin)
- Apply motion/spring animations where KPI band is present
- Grid classes: `rd-kpi-grid`, `rd-dash-grid`, `rd-ms-grid`

This is a **pure presentation conversion** — zero changes to data flow, validation, RLS, or invariants.


---

# Surface: program

Perfect! Now let me verify the redesign status chip and understand what needs to be done. Let me look at the current implementation again:

Now I have all the information I need. Let me create a comprehensive summary document:

## Program Surface Analysis

### (1) Route File & Auth Guard

**File:** `app/(portal)/program/page.tsx:1-38`

- **Auth Guard:** `requireRole(["client"])` (line 26)
- **Client Type Redirect:** Line 38 — Project clients redirect to `/dashboard`
- **Data Fetched (RLS-scoped):**
  - `client_clients` view (line 30): `id, name, slug, industry, program, status, website_url, logo_url, start_date, end_date, service_type, investment, onboarding_form_url, welcome_doc_url, comms_channel, primary_contact_user_id, whats_included, whats_not_included, best_way_to_reach, response_time, call_scheduling_note, revision_policy, created_at, updated_at`
  - `milestones` table (lines 31-34): `id, title, description, phase_label, status, due_date` (ordered by `sort_order`)
- **Branching:** Program client only; project clients are excluded at runtime (line 38).

### (2) Render Component Tree & R0 Coverage

The program page is **fully self-contained** (no client-side components). Uses existing R0 primitives:

- **`PageContainer` + `PageHeader`** (lines 63-64): Layout + title ("Your program", "Everything your engagement covers.")
- **Four `Card` sections** (lines 66-171):
  1. **Details** (lines 66-80): Flexbox `<dl>` with label-value pairs (2-col grid `sm:grid-cols-2`), bottom borders. Fields: Program, Service type, Investment, Started, Ends.
  2. **Your journey** (lines 82-126): Milestone list as ordered list (`<ol>`). Each milestone: custom left-border (color from `MS_BORDER` map), title + phase_label + description, + **StatusChip** (`kind="milestone"`, `value={m.status}`).
  3. **What's included** + **What's not included** (lines 128-153): 2-col grid. Content rendered via **`<Markdown>` component** (prose + links + lists).
  4. **Working together** (lines 155-171): Optional `<dl>` with label-value pairs.

**Existing R0/R1 Components Already Covering Pieces:**
- ✅ `StatusChip` (line 119): Already maps `milestone` kind; used verbatim.
- ✅ `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`: Old Tailwind; must be replaced with Fluent `Card` from barrel (see below).
- ✅ `PageContainer`, `PageHeader`: Layout primitives; likely compatible but check for glass rule application.
- ✅ `Markdown`: Prose renderer; can stay as-is (not UI-heavy).

### (3) Client Write Paths

**NONE.** The program page is **read-only**. No form inputs, no server actions, no mutation RPCs. All data is server-fetched and rendered statically.

### (4) Invariant Touchpoints

**StatusChip (Milestone status)** — Line 119:
- Renders `kind="milestone"` with values: `upcoming`, `in_progress`, `done` (from `milestone_status` enum).
- **INVARIANT:** Status is **READ-ONLY** for clients. It is a display chip, not a control. Must remain a chip; do NOT add edit/click handlers.
- The color-coding (line 19-23: `MS_BORDER` map) **echoes the dashboard roadmap** per the in-code comment. Colors: done=#B45309 (amber), in_progress=#FBBF24 (gold), upcoming=#E7E5E0 (warm-light). Consider whether this overlaps with StatusChip's own color scheme; likely a duplicate of StatusChip's "milestone" kind definition (see `components/ui/status-chip.tsx:33-36`).

**No other invariants** (no time-tracking, no internal-thread content, no gating on this surface).

### (5) Glass Plan

Per the Warm Obsidian skin + glass rule (KPI/summary with scrim → glass; dense tables/forms/prose → solid):

- **Cards with Summary/KPI Feel** (solid background, but brief structured data):
  - **Details** (program, service type, investment, timeline): SOLID. This is a reference card, not a hero KPI. Keep `Card` solid.
  - **Your journey** (milestone list): SOLID. Dense list; no scrim needed.
  - **What's included / What's not included** (markdown prose): SOLID. Running text; must be readable without glassmorphism flare.
  - **Working together** (contact info, response time): SOLID. Reference data.

**No glass on this surface.** All sections are information dense or prose; the entire page is grounded, no overlays or chrome-style surfaces.

### (6) Conversion Checklist

#### Program Client Surface — page.tsx Conversion Checklist

- [ ] **Import changes**: Replace old card imports with Fluent `Card`, `CardHeader`, `CardFooter` from `@/components/redesign/ui`; keep `PageContainer`, `PageHeader` (verify they work in new layout context).
- [ ] **Card sections**: Swap `@/components/ui/card` → `@/components/redesign/ui`; Fluent Card is already solid (no glass class needed).
- [ ] **StatusChip for milestones** (line 119): Verify `kind="milestone"` is handled by the redesigned `StatusChip` (see `components/ui/status-chip.tsx` or replace with `StatusPill` from `@/components/redesign/data-ui` if R1 uses that). **Decision needed:** Does redesign use `StatusChip` (old) or `StatusPill` (new)? Check the design specs.
  - Current: `StatusChip` from `@/components/ui/status-chip.tsx`.
  - Redesign barrel exports: `StatusPill` from `@/components/redesign/data-ui` (requires `mode: "light" | "dark"`).
  - **Action:** If switching to `StatusPill`, add `mode="light"` prop (program clients are in light mode). But `StatusPill` auto-determines tone from value, so may be sufficient.
- [ ] **MS_BORDER color map** (lines 19-23): Verify these colors match the redesigned `StatusChip`/`StatusPill` colors for `milestone` kind. If `StatusPill` is used, remove the custom `MS_BORDER` styling (line 99-102) and let the pill handle color.
- [ ] **Milestone border styling** (lines 98-102): The left-border color from `MS_BORDER` is currently inline. Once StatusChip is updated, confirm whether the milestone list item still needs custom border-left, or if it's now handled by the chip alone.
- [ ] **Typography**: Verify `font-medium`, `text-sm`, `text-ink-3`, `text-ink` tokens are Tailwind or Fluent. If Fluent, use Fluent `Text`, `Body1`, `Caption1` wrappers from barrel.
- [ ] **Grid layout** (lines 71, 128, 162): `sm:grid-cols-2` — verify responsive classes work in Fluent scope (likely yes; Tailwind still applies).
- [ ] **Page layout**: `PageContainer width="standard" stack` — confirm `width` and `stack` props work with new layout system (they should; these are layout-only, not style-dependent).
- [ ] **Markdown component**: Can stay as-is. It renders prose; no UI primitive swap needed.
- [ ] **Test milestone rendering**: Ensure milestone left-border color is distinct and readable on new background. May need to adjust `MS_BORDER` if the new surface contrast is different.
- [ ] **Client type check** (line 38): Confirm redirect logic is unchanged (it is).
- [ ] **No server action changes required.** Page is read-only.

---

## Summary

**Program surface is read-only, single-client surface** with four information cards (Details, Your Journey/Milestones, What's Included/Not, Working Together). It uses old Tailwind Cards and a StatusChip for milestone status. The conversion is **straightforward**: swap Cards to Fluent, verify StatusChip/StatusPill is compatible with milestone colors, confirm typography tokens, and test layout responsiveness. No logic changes, no new writes, no invariant violations.


---

# Surface: performance

Perfect. Now I have all the information needed. Let me compile the comprehensive markdown report:

---

## Performance Surface Conversion Plan: Fluent UI + Warm Obsidian

### 1. Route & Data Fetch

**File:** `app/(portal)/performance/page.tsx` (lines 15–24)

**Auth Guard:**
- `requireRole(["client"])` — clients-only (line 16)

**Client Type Routing:**
- **PROGRAM clients** (the default): Page renders normally with full performance dashboard
- **PROJECT clients** (line 24): `redirect("/dashboard")` — entirely hidden from project-type clients; they have no access to this surface

**Exact Data Fetched (Read-only):**
```
- metric_definitions → id, label, unit, sort_order (ordered by sort_order)
- metric_entries → definition_id, period, value_numeric, value_text (ordered by period)
- competitors → id, name_or_handle, niche, follower_count, avg_views, top_content_format, hook_style, whats_working, gap_notes, adapted_idea, priority (ordered by priority DESC)
- reports → id, title, period_start, period_end, summary, pdf_path (ordered by period_end DESC)
```
All reads are direct `.select()` with no RPC calls or mutations.

---

### 2. Render Component Tree (Shadcn → Fluent Conversion Map)

**Page-level layout (shadcn → surfaces/primitives):**
- `PageContainer` (line 48) → wraps stacked sections; becomes `Measure` in R1 UI barrel
- `PageHeader` (lines 49–51) → "Your numbers, live" + description; becomes `Eyebrow` + heading in R1 pattern

**Three main content sections:**

#### A. Metrics Charts & Table (SOLID; lines 54–58, 129–212 in metrics-charts.tsx)
- **Chart wrapper:** `Card > CardContent` (shadcn) → becomes bare `div` with `rd-solid` class (R0 surface) + rounded-20 styling (lines 111–140 in perf-chart.tsx reference)
  - **Selector buttons:** native `Select/SelectTrigger/SelectContent/SelectItem` (shadcn) → **replace with Fluent `Segmented`** or bare button pills (R1 barrel offers `Segmented`)
  - **Line chart:** `MetricsLineChart` (recharts) → already exists as reference in `perf-chart.tsx`; reuse its dark/light mode coloring
  - **Metric delta inline:** `MetricDelta` (shadcn component, line 167) → import from `@/components/ui` but wrap with redesign `DeltaChip` for consistency (R0 data-ui)

- **Dense month-by-month table (lines 135–176):** `Table/TableHeader/TableBody/TableCell` (shadcn) → **replace with Fluent `Table`** component from barrel; keep layout/spacing identical
  - **Sticky column:** preserve the left-sticky month cell (line 139)
  - **Latest row highlight:** line 153 uses amber-50; migrate to redesign color tokens (dark: `rgba(245,158,11,0.08)`, light: `#fffaf0`)
  - **Metric delta inline:** same as above; use `DeltaChip` from R0

- **Text highlights table (lines 183–210):** same as above — Fluent `Table` replacement, solid panel, no glass

#### B. Competitors Cards Section (SOLID; lines 60–136 in page.tsx)
- **Grid wrapper:** `div.grid` (shadcn Tailwind) → becomes `div` with `rd-comp-grid` class (reference: perf-keystone lines 277–279)
- **Card per competitor:** `Card > CardContent` (shadcn) → becomes `div` with `rd-solid` class + padding/borderRadius (reference: perf-keystone lines 230)
- **Priority badge:** `StatusChip kind="priority"` (shadcn, line 74) → **replace with Fluent `Badge`** or `StatusPill` from R0 data-ui (import from barrel)
- **Stats layout:** plain `div` flexbox (lines 77–92) → keep as-is, style with inline tokens from `@fluentui/react-components` (line 46 in perf-keystone)
- **"Our play" highlight box (lines 124–129):** amber-50 bg → redesign amber alert style (reference: perf-keystone lines 245–249)

#### C. Reports Section (SOLID; lines 138–141 in page.tsx, all of client-reports.tsx)
- **List + viewer layout (lines 43–94):** `div.grid` with two columns → becomes `div` with grid layout
- **Report list items (lines 45–68):** buttons with custom styling (border-left amber, bg-amber-50 on active) → **replace outer list + buttons with Fluent `TabList` and `Tab`** (from barrel) or keep styled buttons but use Fluent primitives for borders/bg
  - **Status badge "Published":** `StatusChip kind="report" value="published"` (line 62) → **replace with Fluent `Badge`** or R0 `StatusPill`
- **Report viewer panel (lines 71–93):** `div.rounded-2xl.border.bg-surface.shadow-e2` → becomes `rd-solid` div with same structure
  - **Download button (line 81):** `DownloadButton` → keep as-is (it's client-side only, uses `getSignedUrlAction`)
  - **Markdown summary (lines 85–87):** `Markdown` component → keep as-is (no redesign needed for running text)

---

### 3. Client Write Paths (Preserved Verbatim)

**NONE on this surface.** The performance page is READ-ONLY.

- No status mutations on clients
- No metric entry edits
- No competitor updates
- No report creation/deletion
- The only client action is `getSignedUrlAction` in `DownloadButton` (line 28 of download-button.tsx) — **this is a read operation** (generates a signed URL for PDF download)

**Invariant:** Performance data is server-managed only; clients consume, never write.

---

### 4. Invariant Touchpoints

**Status displays (READ-ONLY chips, never writable on client surface):**
- **Competitor priority badge** (line 74 in page.tsx): `StatusChip kind="priority"` — only reads `competitors.priority`; no edit UI
- **Report status badge** (line 62 in client-reports.tsx): `StatusChip kind="report" value="published"` — hard-coded "published"; no edit UI
- **No task/project status controls on this surface** — those live in dashboard/project-board

**No internal-thread content:**
- No comments, no decision logs, no internal notes — all data shown is client-facing

**No time-tracking or timer UI:**
- Performance page is purely metric display; no timer widgets

**No invisible/internal task gating:**
- All data is visible to the logged-in client, with no conditional hiding based on internal flags

---

### 5. Glass Plan

Per the rule: **glass only on KPI/summary/chrome with scrim; solid on dense tables, forms, running text.**

| Section | Treatment | Reason |
|---------|-----------|--------|
| Page header "Your numbers, live" | **SOLID** (amber background or hero style) | It's a label/title, not a KPI; runs text |
| Metrics line chart (with selector) | **SOLID** (`rd-solid` div, no glass) | Dense data visualization; chart is the focal point |
| Month-by-month table | **SOLID** (`rd-solid` div) | Densest tabular data; AA worst-case surface; absolutely no glass |
| Highlights (text) table | **SOLID** (`rd-solid` div) | Running text per cell; legibility paramount |
| Competitor cards grid | **SOLID** (`rd-solid` per card) | Dense copy (niche, working, gap, play); text-heavy |
| Reports list | **SOLID** for both list and viewer panel | Markdown summary is running text; no KPI scrim |
| **Reference:** | | Redesign perf keystone already implements all as solid (lines 111–273) |

---

### 6. Existing R0/R1 Components Already Covering Pieces

| R0/R1 Component | Used For | Import |
|-----------------|----------|--------|
| `StatusChip` | Competitor priority, report status (current shadcn) | `@/components/ui/status-chip` → **migrate to Fluent `Badge`** or R0 `StatusPill` |
| `MetricDelta` | Month-over-month delta display (current shadcn) | `@/components/ui/metric-delta` → keep but nest in R0 `DeltaChip` for dark-mode consistency |
| `Card / CardContent` | All panels (current shadcn) | `@/components/ui/card` → **replace with plain `div.rd-solid`** + Fluent `Card` if using Fluent's visual style |
| `Table / TableBody / TableCell` | Dense month tables (current shadcn) | `@/components/ui/table` → **replace with Fluent `Table`** (from barrel) |
| `Select / SelectItem` | Metric picker (current shadcn) | `@/components/ui/select` → **replace with Fluent `Segmented`** (from barrel) or button pills |
| `EmptyState` | No data fallback (current shadcn) | `@/components/ui/empty-state` → **keep or migrate to Fluent pattern** (minor impact) |
| **Reference implementations:** | | `components/redesign/keystones/performance.tsx` + `perf-chart.tsx` |
| `Eyebrow` | Section labels (R1) | `@/components/redesign/ui` → already exists; use for "Month by month", "Highlights by month", "Competitors", "Reports" |
| `DeltaChip` | Month delta visualization (R0 data-ui) | `@/components/redesign/data-ui` → import and use for delta display with mode awareness |
| `StatusPill` | Priority/status badges (R0 data-ui) | `@/components/redesign/data-ui` → import and use for competitor priority, report status |
| `Shell`, `AmbientField`, `Measure` | Page structure (R1) | `@/components/redesign/ui` → **not used on current surface but available if re-skinning at route level** |

---

### 7. Conversion Checklist

**For PROGRAM clients (the main performance page):**

- [ ] **Page structure**
  - [ ] Replace `PageContainer` usage with explicit `Measure` if full redesign route is applied; otherwise leave `PageContainer` as-is
  - [ ] Replace `PageHeader` with inline `Eyebrow` + h1 heading in redesign style OR keep PageHeader but ensure it matches redesign colors

- [ ] **Metrics chart section**
  - [ ] Replace `Card > CardContent` wrapper with `div.rd-solid` (borderRadius: 20, padding: 1.3rem)
  - [ ] Replace `Select/SelectTrigger/SelectContent/SelectItem` (metric picker) with Fluent `Segmented` component
  - [ ] Ensure `MetricsLineChart` uses mode-aware colors (reference perf-chart.tsx for the exact palette)
  - [ ] Keep recharts lazy-loading via `next/dynamic`

- [ ] **Month-by-month table**
  - [ ] Wrap `Table` element in `div.rd-solid` (borderRadius: 20, padding: 0, overflow: hidden)
  - [ ] Replace shadcn `Table` primitives with Fluent `Table` from barrel
  - [ ] Update row highlight color (latest row) to redesign amber (light: #fffaf0, dark: rgba(245,158,11,0.08))
  - [ ] Replace inline `MetricDelta` with `DeltaChip` from R0 data-ui (mode-aware)
  - [ ] Preserve sticky left column and numeric alignment

- [ ] **Highlights table (text)**
  - [ ] Same `rd-solid` wrapper + Fluent `Table` replacement
  - [ ] No delta display; just text cells

- [ ] **Competitors grid**
  - [ ] Wrap grid in flex-col gap
  - [ ] Replace each `Card > CardContent` with `div.rd-solid` (borderRadius: 18, padding: 1.2rem)
  - [ ] Replace `StatusChip kind="priority"` with Fluent `Badge` or R0 `StatusPill` (import from barrel)
  - [ ] Replace "Our play" box bg-color to redesign amber alert style (dark: rgba(245,158,11,0.1), light: #fffaf0)

- [ ] **Reports section**
  - [ ] Keep list-item buttons but style with Fluent border/bg OR replace with Fluent `TabList/Tab`
  - [ ] Replace `StatusChip kind="report"` with Fluent `Badge` or R0 `StatusPill`
  - [ ] Replace viewer panel `div.shadow-e2` with `div.rd-solid`
  - [ ] Keep `DownloadButton` and `Markdown` as-is

- [ ] **Colors & tokens**
  - [ ] Import `tokens` from `@fluentui/react-components` for foreground/bg colors
  - [ ] Use redesign color palette for amber, neutral, success tones (reference data-ui.tsx)

- [ ] **Empty states**
  - [ ] `EmptyState` component usage is minimal (two places in metrics-charts.tsx); keep as-is or migrate to Fluent `Skeleton` if full redesign desired

**For PROJECT clients:**
- N/A — entirely hidden via redirect to `/dashboard` (line 24 of page.tsx)

---

### Summary of File Paths & Changes

| File | Type | Changes |
|------|------|---------|
| `app/(portal)/performance/page.tsx` | Route | **Light refresh:** Keep structure, migrate Card/PageHeader styling to use Fluent/redesign classes |
| `components/metrics/metrics-charts.tsx` | Client component | **Heavy:** Replace Select → Segmented, Table → Fluent Table, MetricDelta → DeltaChip, Card → rd-solid div |
| `components/metrics/metrics-line-chart.tsx` | Chart (Recharts) | **Keep as-is** — already optimized; verify colors match perf-chart.tsx |
| `components/client/client-reports.tsx` | Client component | **Medium:** Replace Card/buttons with rd-solid divs, StatusChip → Badge/StatusPill, update highlight colors |
| `components/redesign/keystones/performance.tsx` | **REFERENCE ONLY** | Existing R1 redesign; copy patterns for solid panels, dense tables, competitor cards, report list |

---

**All SOLID; no glass. All reads. No writes. PROGRAM-only. Go.** ✓


---

# Surface: content

Excellent! Now I have all the context I need. Let me compile the comprehensive conversion plan for the content surface:

---

## Content Surface Conversion Plan

### (1) Route File & Auth Guard

**File**: `app/(portal)/content/page.tsx` (lines 1–37)

- **Auth guard**: `await requireRole(["client"])` — client role only.
- **Redirect logic**: Checks `client_type` from `client_clients` table. **PROJECT clients redirect to `/dashboard`** (line 20); this surface is **PROGRAM-ONLY**.
- **Data fetched**:
  - Table: `client_clients` → `client_type` (maybeSingle, RLS-protected)
  - Table: `content_items` → columns: `id, title, platform, content_type, status, publish_date, asset_url` (RLS returns only `visible_to_client=true` items)
  - Ordering: by `publish_date` ascending, nulls last
- **Branches**: Only program clients reach this page; project clients never see it.

---

### (2) Render Component Tree

**Primary component**: `ClientContent` (`components/client/client-content.tsx`)

**Tree**:
```
PageContainer width="standard" stack
  └─ PageHeader (title="Content calendar", description)
     └─ ClientContent (items: ClientContentItem[])
        ├─ SegmentedControl (old: components/ui/segmented.tsx — view: "list" | "month")
        ├─ List view: <ul> with <li> per item
        │  ├─ Title (text)
        │  ├─ Platform + content_type + publish_date (metadata)
        │  ├─ StatusChip kind="content" value={status}
        │  └─ Button conditional (if published + asset_url exists)
        └─ Month view: grid calendar
           ├─ Month header + nav buttons
           └─ Day cells with color-coded items (status-mapped inline styles)
```

**Old R0 components currently used**:
- `SegmentedControl` (line 10, 74) — old charcoal variant → **migrate to `Segmented`** from `components/redesign/ui`
- `StatusChip` (line 7, 95) — **already R0**, maps `kind="content"` → reuse; preserve `STATUS_MAP.content` enum
- `Button` (line 9, 97–101) — old gray outline → **migrate to Fluent `Button`** (appearance="subtle")
- `EmptyState` (line 11, 65–68) — old bordered dashed box → **migrate to Fluent `Card`** or redesign equivalent if available

**New R1 components to import** (from `components/redesign/ui`):
- `Segmented` (custom, Fluent-themed)
- `Button` (custom wrapper over Fluent `Button`)
- `Card` (from Fluent; for empty state container if needed)

---

### (3) Client Write Paths (Preserved Verbatim)

**NONE on this surface.** The `/content` page is **READ-ONLY for program clients**.

- No create, update, delete, or status-change actions.
- No visibility toggles.
- Status and visibility are controlled on the **staff side** (e.g., `app/(portal)/clients/[clientId]/content`), not exposed to program clients.
- The `StatusChip` is purely **display** — not interactive.
- The external link (`View` button, line 96–101) is a **read-only link** to the published asset, not a write action.

**Conclusion**: No server actions need guarding on this surface. Preserve the invariant: program clients cannot mutate content state.

---

### (4) Invariant Touchpoints

**Status is READ-ONLY**:
- `StatusChip` (line 95 & month view line 135–136) displays content status but is **non-interactive**.
- Clients see only the publish state, never edit it.
- Staff controls status in the internal content management surface.

**No time-tracking or internal-thread content**:
- This page does not surface time entries, timer, cost, or internal notes.
- Only public fields: `title, platform, content_type, status, publish_date, asset_url`.
- The internal columns (`cta, core_message, notes, views_after_posting`) from the staff view are **not fetched** here (line 25 selects only 7 columns).

**Visibility is transparent**:
- RLS on `content_items` ensures only `visible_to_client=true` items are returned (enforced by auth policy, not client-side logic).
- No UI control for visibility toggle on this page.

**Touchpoint summary**:
- Line 25: SELECT only safe columns (no internal metadata)
- Line 95, 135–136: StatusChip is display-only (non-interactive, no change handler)
- Line 96–101: External link only; no state mutation

---

### (5) Glass Plan

**GLASS (KPI/summary with scrim)**:
- Month calendar header (month name + navigation buttons) — small summary chrome → **`GlassSurface strong`** (semi-transparent scrim for readability)

**SOLID (dense tables, forms, running text)**:
- List view `<ul>` (line 84–105) — data table → **solid background** (no glass)
- Day cells in month grid (line 131–150) — dense content → **solid background**
- Status chips (line 95, 135–136) — already use inline `STATUS_MAP` colors → keep solid
- SegmentedControl (line 74) — interaction chrome → **solid** (Fluent default)
- Empty state (line 65–69) — informational → **solid `Card`**

**Rationale**:
- The content calendar is a dense **data view** (list or grid), not a KPI dashboard. Glass only lifts chrome (headers, nav) that sit **above** the data, not around it.
- Month header is brief, auxiliary → glass; day grid is the substance → solid.

---

### (6) Conversion Checklist

**For PROGRAM clients only** (PROJECT clients redirect and never see this page):

- [ ] Replace `PageContainer` and `PageHeader` — keep as-is (they are not part of the redesign audit; they're layout-level)
- [ ] Replace `SegmentedControl` import from `components/ui/segmented` → `Segmented` from `components/redesign/ui` (line 10, 74)
  - Adjust props: `options` array structure is identical; `onValueChange` → `onChange`
  - Fluent `Segmented` returns `(value: T) => void`; wire to `setView(value)`
- [ ] Replace `Button` import and usages (lines 9, 97–101, 113–118)
  - Old: `variant="outline"` → New: `appearance="subtle"` (Fluent equivalent for secondary actions)
  - Old: `size="sm"` → New: Fluent `size="small"`
  - Keep `onClick`, `aria-label` unchanged
  - ChevronLeft/ChevronRight icons pass as `icon` prop
  - For the "View" link button: keep `asChild` pattern or convert to `<a>` with Fluent `Button` parent
- [ ] Keep `StatusChip` (line 7, 95, 135) — already R0, no migration needed. Preserve `STATUS_MAP.content` enum.
- [ ] Replace `EmptyState` (lines 11, 65–69)
  - Old: bordered dashed box with icon + text
  - New: `Fluent Card` or simpler solid container with text
  - Icons passed as before; structure is simpler (no special styling)
- [ ] Keep `formatDate` utility (line 14) — unchanged
- [ ] Keep `CONTENT_PLATFORMS`, `labelOf` constant imports (lines 6, 13) — unchanged
- [ ] CSS classes in month grid (lines 84, 121–150): replace Tailwind tokens with Fluent/redesign tokens where applicable
  - `divide-y divide-row-divider rounded-2xl border border-border` → Fluent tokens (e.g., `tokens.colorNeutralStroke1`)
  - `bg-bg`, `bg-surface`, `bg-border`, `text-ink-3` → use Fluent tokens or keep existing Tailwind tokens if not conflicting
- [ ] No server action changes needed — this surface has no writes.
- [ ] Loading state (`app/(portal)/content/loading.tsx`): Replace `Skeleton` from old UI → Fluent `Skeleton` (line 1)
- [ ] Inline status color mapping (line 136: `style = { background: r.bg, color: r.text }`) — keep; `STATUS_MAP.content` is preserved

**Summary**:
- **Minimal scope**: Only UI primitives change (Button, Segmented, EmptyState, Skeleton).
- **Data contract unchanged**: Same RLS, same fetch, same route redirect.
- **Invariants hold**: No write paths, status is read-only, no internal content leaks.
- **Glass rule**: Month header → scrim; data grid → solid.

---

**File paths for conversion targets**:
1. `app/(portal)/content/page.tsx` — route (line 1–37)
2. `components/client/client-content.tsx` — primary render (lines 1–158)
3. `app/(portal)/content/loading.tsx` — loading skeleton (lines 1–17)


---

# Surface: deliverables

Perfect. Now I have all the context I need. Let me create the comprehensive analysis document:

---

## SURFACE ANALYSIS: DELIVERABLES (Client-Facing)

### (1) ROUTE & DATA FETCHING

**Route file:** `app/(portal)/deliverables/page.tsx` (lines 1-92)

**Auth guard:** `requireRole(["client"])` — only clients can view this page; staff view their own deliverables through the team workspace at `/clients/[clientId]/deliverables`.

**RLS-scoped query:**
```
deliverables
  SELECT: id, title, description, type, status, due_date, delivered_at, preview_url, file_path, client_approved_at
  WHERE: visible_to_client = true AND client_id = <auth.user.client_id> (RLS-enforced)
  ORDER BY: created_at DESC
```

**Client type branching:** NONE — deliverables surface displays to BOTH program and project clients identically. Project clients do NOT hide this tab (only Program/Performance/Content are hidden in `client-shell.tsx`).

---

### (2) COMPONENT TREE & REDESIGN R0 COVERAGE

Current components (all old shadcn/Tailwind):
- **app/(portal)/deliverables/page.tsx** (route, container)
  - `PageContainer` (layout helper; converts to rd-measure)
  - `PageHeader` (title/description; converts to heading + subtitle)
  - `EmptyState` (icon + text; stays as-is, icon from lucide)
  - `Stagger` / `StaggerItem` (motion primitives; motion/react, unchanged)
  - **Card list** (grid of deliverables; `.rounded-2xl border border-border bg-surface p-[var(--card-pad)] shadow-e1 hover:shadow-e2` — this is SOLID rule, no glass)
    - `Badge` (type pill: `{labelOf(DELIVERABLE_TYPES, d.type)}`) — **replace with Fluent `Badge`**
    - `StatusChip` (kind="deliverable", value=status) — **live R0 component, import unchanged**
    - `DeliverableApprove` (client-only approve button; nested component)
      - `Button` (state "Approved" or "Approve") — **replace with R1 `Button` from `components/redesign/ui`**
      - motion.span (success check icon + scale animation) — **keep, motion/react is library-level**
      - `Check` icon (lucide) — keep
    - `DownloadButton` (nested component)
      - `Button` (variant="outline") — **replace with R1 `Button`**
      - `Download` icon (lucide) — keep

**No redesign versions exist yet.**

**Which R0/R1 components already cover pieces:**
- `StatusChip` (kind="deliverable") — already defined in `components/ui/status-chip.tsx`, uses the R0 STATUS_MAP with correct colors/dots/icons; **can be imported and used directly with no wrapper**
- `Button` — use `components/redesign/ui/Button` (Fluent-wrapped with loading support); variants map `outline` → Fluent `outline` appearance
- `Badge` — use Fluent `Badge` from `components/redesign/ui` export (§1.31-32 of index.ts)

---

### (3) CLIENT WRITE PATHS (PRESERVED VERBATIM)

**Two client-initiated actions on this surface:**

1. **Approve/acknowledge deliverable**
   - **RPC:** `set_deliverable_approval(deliverable_id: uuid, approved: boolean)` (SECURITY DEFINER)
   - **Server action:** `setDeliverableApprovalAction(deliverableId, approved)` (`lib/actions/deliverables.ts:285-324`)
   - **Guard:** `requireProfile()` (client role implicit via RPC enforcement)
   - **RPC contract:** enforces own-client + visible + client-approved_at column only (client has NO direct UPDATE policy on deliverables table)
   - **Audit:** `deliverable.client_approved` logged with `{ approved: boolean }`
   - **Notification:** if approved=true, notifies staff via `staffUserIds(client_id)` ("Deliverable approved by client")
   - **Revalidation:** `/deliverables` + `/dashboard`
   - **Component:** `DeliverableApprove` invokes this on button click; toast feedback on success/error

2. **Download file (signed URL)**
   - **Server action:** `getSignedUrlAction(clientId, path)` (`lib/actions/storage.ts:49-65`)
   - **Guard:** `requireClientAccess(clientId)` (for the client's own workspace; uses profile.client_id)
   - **Contract:** validates path starts with `{clientId}/`, then mints a 60-second signed URL via admin Supabase client (clients have NO storage.objects RLS policy)
   - **Return:** `{ ok: true; url: string }` or error
   - **Component:** `DownloadButton` invokes on click, opens signed URL in new tab via `window.open(res.url, "_blank")`

**No other writes permitted on this surface for clients.**

---

### (4) INVARIANT TOUCHPOINTS

**Read-only status display (MUST stay immutable):**
- `StatusChip` (kind="deliverable", value=status) — displays pending / in_progress / needs_review / delivered
- **Rule:** clients see this chip but cannot change the status; only staff can via `setDeliverableStatusAction` (team workspace route `/clients/[clientId]/deliverables`)
- **Lives at:** `DeliverableApprove` render path — shown on every deliverable card

**Approval interaction (bounded write-only control):**
- `DeliverableApprove` button — shown ONLY when `status === "needs_review" || client_approved_at` (the client can approve OR unapprove, but only when the staff has explicitly marked it needs_review)
- **No time-tracking timer:** deliverables do not include or expose any time-tracking UI on client surfaces (that's staff-only; see `tasks` and `content` for time metrics)

**No internal-thread content reachable:** deliverables do not link to or expose any internal notes/thread data.

**No invisible/internal task gating:** the deliverables list is simple visibility-filtered (visible_to_client = true); no conditional visibility based on task status or project internal state.

---

### (5) GLASS PLAN

The deliverable list is **SOLID cards ONLY** per the rule (dense data, not KPI/summary/chrome):

- **Card container:** `.rounded-2xl border border-border bg-surface p-[var(--card-pad)]` — SOLID
  - Currently: hardcoded Tailwind; becomes `rd-solid` class wrapper
  - Motion: shadow transition (hover:shadow-e1 to shadow-e2) persists unchanged
  - Grid layout: `gap-4 lg:grid-cols-2` persists unchanged

- **Title + type badge + metadata (description, dates):** SOLID text/inline
- **Chip + button row (status, approve, download, preview):** SOLID controls

**Zero glass surfaces on deliverables** (no floating overlays, no chrome). The shell itself (chrome) is glass; the page body is solid.

---

### (6) CONVERSION CHECKLIST

**For BOTH program and project clients:**

- [ ] Replace `app/(portal)/deliverables/page.tsx` imports:
  - [ ] `PageContainer` → keep (layout helper; does not change)
  - [ ] `PageHeader` → keep (heading + subtitle; does not change)
  - [ ] `Badge` → `Badge` from `components/redesign/ui` (Fluent; syntax unchanged at call site)
  - [ ] `StatusChip` → keep (import from R0; already correct)
  - [ ] `Button` from `components/ui/button` → `Button` from `components/redesign/ui` (at call sites: `DownloadButton` and `DeliverableApprove`)
  - [ ] `EmptyState` → keep (icon helper; unchanged)
  - [ ] `Stagger` / `StaggerItem` → keep (motion primitives; library-level)

- [ ] Update **DownloadButton** (`components/files/download-button.tsx`):
  - [ ] Line 7: import `Button` from `components/redesign/ui` instead of `components/ui/button`
  - [ ] No logic changes; variant/size props map unchanged to Fluent variants

- [ ] Update **DeliverableApprove** (`components/client/deliverable-approve.tsx`):
  - [ ] Line 10: import `Button` from `components/redesign/ui` instead of `components/ui/button`
  - [ ] Lines 36, 49: `Button` components use updated import; `variant="outline"` / `variant="amber"` map to Fluent appearances
  - [ ] motion.span + Check icon + spring animation: NO CHANGES (motion/react is library-level)
  - [ ] No server action changes; `setDeliverableApprovalAction` call persists verbatim

- [ ] Card styling on deliverables list:
  - [ ] Line 47 (`StaggerItem` className): keep shadow + hover transition as-is; `.bg-surface` / `.border-border` tokens remain unchanged in Fluent light/dark palette
  - [ ] If converting to use `rd-solid` class: apply it in addition to or replace current Tailwind (check if `@fluentui/react-components` Card is preferred; currently using a div, so keep the styled div)

- [ ] Verify RLS queries unchanged (line 21-26) — no client-side data shape changes

- [ ] Verify write path calls unchanged:
  - [ ] `setDeliverableApprovalAction` at line 26 (DeliverableApprove)
  - [ ] `getSignedUrlAction` at line 28 (DownloadButton)
  - [ ] Both remain server-only; no client-side API calls

- [ ] Test in light + dark modes (redesign toggle); verify Status chips match R0 palette

---

## FILE REFERENCES

- Route: `app/(portal)/deliverables/page.tsx` (lines 16–92)
- Server actions: `lib/actions/deliverables.ts` (lines 285–324: `setDeliverableApprovalAction`)
- Server action: `lib/actions/storage.ts` (lines 49–65: `getSignedUrlAction`)
- Components: 
  - `components/client/deliverable-approve.tsx` (lines 1–53)
  - `components/files/download-button.tsx` (lines 1–40)
- UI primitives:
  - `components/ui/status-chip.tsx` (R0, keep)
  - `components/redesign/ui/button.tsx` (R1 Button/EmberButton)
  - `components/redesign/ui/index.ts` (Fluent Badge export)
- Auth guard: `lib/auth/guards.ts` (lines 74–80: `requireRole`)
- Constants: `lib/constants.ts` (lines 56–74: DELIVERABLE_TYPES, DELIVERABLE_STATUSES)


---

# Surface: tasks

Perfect! Now I have a complete understanding. Let me write the comprehensive markdown report.

---

## Tasks Surface Conversion Plan

### (1) Route File & Data Fetching

**Client route:** `/app/(portal)/tasks/page.tsx`
- **Auth guard:** `requireRole(["client"])` — portal access only
- **Data fetched (all via RLS):**
  - `tasks`: `SELECT id, title, description, status, assignee_id, due_date, source_message_id, created_by, created_at` from own `client_id` + `visible_to_client=true`
  - `task_checklist_items`: `SELECT id, task_id, title, is_done, sort_order` — RLS mirrors parent visibility
  - `getAssignableMembers()`: client's own users + assigned staff + all admins
- **Branching on `client_type`:** None — tasks surface is CLIENT-ONLY (both program and project clients see this)
- **Returns:** `ClientTaskBoard` component with `ClientTaskRow[]` + `TaskMember[]`

**Staff route:** `/app/(portal)/clients/[clientId]/tasks/page.tsx`
- **Auth guard:** `requireClientAccess(clientId)` — admin or assigned team only
- **Data fetched:**
  - All tasks for the client (no `visible_to_client` filter — staff sees all)
  - Task checklist items for all tasks
  - **STAFF-ONLY:** `time_entries` per task (clients have no RLS policy)
  - Source message thread_type resolution (admin query) for "Created from a message" link
- **Returns:** `StaffTasksManager` with `StaffTask[]` (extends `ClientTaskRow` + `visible_to_client: boolean` + `timeEntries: TimeEntry[]`)

---

### (2) Render Component Tree

**Client surface** (`/tasks`):
```
ClientTaskBoard (components/tasks/client-task-board.tsx)
├─ PageContainer + PageHeader ("Tasks", add button)
├─ Stagger + StaggerItem grid (2-col responsive)
│  └─ Link → Card
│     ├─ title + StatusChip (read-only, task status)
│     ├─ User + due_date + source_message_id badge
│     ├─ TaskChecklistProgress (done/total mini-bar)
│     └─ description (line-clamped)
└─ TaskDetailDialog (modal, ?task={id})
   ├─ Title + StatusChip (read-only for client)
   ├─ Description (editable, client-only updateTaskAction)
   ├─ Assignee + Due (read-only display)
   ├─ Created-by + Created-at + source message link
   ├─ TaskChecklist (subtasks, editable via client RPCs)
   └─ (NO TaskTimer — staff-only)

ClientTaskDialog (modal, add button)
├─ Title, Description fields
├─ Assign-to (dropdown, circle only)
├─ Due-date picker
└─ Source message link (optional, for chat→task bridge)
```

**Existing R0/R1 components covering pieces:**
- `StatusChip` (kind="task") — read-only status display (already ember-glass safe)
- `Card` + `CardContent` — task card container
- `Button`, `Input`, `Label`, `Textarea`, `DatePicker` — form primitives
- `Dialog` — modal container for detail + add dialogs
- `Checkbox` — subtask toggles
- `EmptyState` — no-tasks fallback

**New surfaces to reskin:**
- Task card body (flex/grid layout in `ClientTaskBoard`)
- Task detail modal content sections (title/status, description, metadata, subtasks)
- Subtask list rows + progress bar (`TaskChecklist`)
- Add-task dialog form (`ClientTaskDialog`)

---

### (3) Client Write Paths (MUST be preserved)

**From `lib/actions/tasks-client.ts`:**

1. **`createTaskAction(input: TaskClientCreateValues)`** → RPC `create_task`
   - Args: `p_title, p_description, p_assignee, p_due_date, p_source_message_id`
   - Validates: caller is client, assignee in circle, source is client's OWN client_shared message
   - Sets task `visible_to_client=true` (client tasks always visible), `created_by` = caller
   - Returns: `{ ok: true/false, data?: { id } }`

2. **`updateTaskAction(input: TaskClientUpdateValues)`** → RPC `update_task`
   - Args: `p_task_id, p_title, p_description` **ONLY**
   - No status, assignee, due_date, visibility writes possible
   - Validates: caller is client, task is own + visible_to_client
   - Returns: `{ ok: true/false, error }`

**From `lib/actions/task-checklist.ts` (client branch):**

3. **`addChecklistItemAction(taskId, title)`** → RPC `add_task_checklist_item`
   - Args: `p_task_id, p_title`
   - Validates: own client, parent task visible_to_client
   
4. **`toggleChecklistItemAction(itemId)`** → RPC `toggle_task_checklist_item`
   - Args: `p_item_id`
   - Toggles `is_done` (idempotent toggle)

5. **`editChecklistItemAction(itemId, title)`** → RPC `edit_task_checklist_item`
   - Args: `p_item_id, p_title`

6. **`deleteChecklistItemAction(itemId)`** → RPC `delete_task_checklist_item`
   - Args: `p_item_id`

All client checklist RPCs validate: own client, parent task visible_to_client (inherited boundary).

---

### (4) Invariant Touchpoints

**READ-ONLY STATUS CHIP (Phase-1 lock):**
- **Location:** `TaskDetailDialog` line 168 (client branch) — `StatusChip kind="task" value={task.status}`
- **Rendering:** Both client and staff detail; staff has a `Select` to change it
- **Invariant:** Client NEVER has a status control; no client RPC for `update_task_status` exists
- **Preserved by:** `updateTaskAction` RPC signature (title + description only); no status param in `TaskClientUpdateValues`

**NO TIMER / TIME-TRACKING on client:**
- **Location:** `TaskDetailDialog` lines 261–268 — `{isStaff && clientId && currentUserId && <TaskTimer ...>}`
- **Invariant:** `TaskTimer` rendered ONLY when `role === "staff"`; client never sees it
- **Preserved by:** Conditional render gating on `isStaff` boolean; no client-side timer component

**NO CLIENT ACCESS to internal/invisible tasks:**
- **Location:** Client-side RLS on `tasks` table; `visible_to_client = true` filter on `/tasks` page query
- **Invariant:** Client's RLS-scoped list at page.tsx:13–17 ONLY returns visible tasks; `?task={id}` opens one already in that list (line 47 `openTask` lookup)
- **Preserved by:** RLS policy + the `?task` param resolved via `.find()` on already-gated list (no direct query by ID)
- **Internal boundary:** Staff task creation from internal messages auto-sets `visible_to_client = false` (tasks.ts:45), so internal content never surfaces

**Checklist visibility inherited:**
- **Location:** `TaskChecklist` component line 65; client checklist RPCs re-validate parent task visibility
- **Preserved by:** `add_task_checklist_item`, `toggle_task_checklist_item`, `edit_task_checklist_item`, `delete_task_checklist_item` all check parent is own + visible
- **Audit trail:** Logged in `tasks-client.ts` and `task-checklist.ts` (createTaskAction, updateTaskAction, etc.)

---

### (5) Glass Plan

**Glass (KPI/summary/card surfaces):**
- Task card in the grid (`Card` + `CardContent`) — summary view, hover-lift, scrim overlay on hover
- Task detail modal header area (title + status + metadata bar) — glass with subtle scrim
- Subtask progress bar section (`TaskChecklist` header + bar) — glass, light scrim
- Checklist item rows — solid background (interactive elements in dense list)

**Solid (dense tables, forms, running text):**
- Task description textarea in detail modal — solid background, full focus
- Subtask add-input (`Input` + `Button`) — solid
- Form fields in add-task dialog — solid (title, description, assignee, due date)
- Checklist item row content (edit input, delete button interaction zone) — solid when active

**Scrim approach:**
- Task cards: `hover:shadow-e3` on hover (existing); replace with glass-shadow + 5% overlay scrim
- Detail modal: `DialogContent` background glass, sections border-separated by `border-border` (existing)
- Subtasks: progress bar full-width glass, items in list solid with hover ghost (no scrim needed)

---

### (6) Conversion Checklist

**For both CLIENT and STAFF surfaces:**

- [ ] **Task card** (`ClientTaskBoard` line 87–112):
  - [ ] Card → import Fluent glass button / surface
  - [ ] Layout: flex/grid to align title + status + metadata + description
  - [ ] StatusChip stays read-only
  - [ ] TaskChecklistProgress bar styling → amber-600 progress (no change)
  - [ ] Apply glass + scrim hover effect

- [ ] **Task detail modal** (`TaskDetailDialog` lines 143–277):
  - **Title section (152–172):**
    - [ ] Input styling → Fluent
    - [ ] StatusChip display (client: chip; staff: Select) — no change to logic
  
  - **Description (174–185):**
    - [ ] Textarea → Fluent dark-themed
  
  - **Assignee + Due (188–218):**
    - [ ] Client: read-only text + icon → Fluent text styles
    - [ ] Staff: Selects → Fluent Dropdowns
  
  - **Visibility toggle (staff only, 221–231):**
    - [ ] Switch + label → Fluent Toggle + text
  
  - **Metadata bar (234–246):**
    - [ ] Created-by, created-at, source link → Fluent text + link styles
  
  - **Subtasks (254):**
    - [ ] `TaskChecklist` imported, no logic change; re-skin sub-component:
      - [ ] Progress bar header → glass with done/total text
      - [ ] Checklist rows: Checkbox + text + delete button
      - [ ] Add input + button → Fluent Input + Button
  
  - **Timer (staff only, 261–268):**
    - [ ] NO CHANGE — client never sees

- [ ] **Add task dialog** (`ClientTaskDialog` lines 76–139):
  - [ ] Title Input → Fluent
  - [ ] Description Textarea → Fluent
  - [ ] Assignee Dropdown → Fluent Select
  - [ ] Due-date Picker → Fluent DatePicker
  - [ ] Submit Button → Fluent

- [ ] **Subtask component** (`TaskChecklist` lines 197–262):
  - [ ] Header bar (199–206) → glass background
  - [ ] Progress bar (208–219) → amber-600 (keep existing)
  - [ ] Item row (221–241):
    - [ ] Checkbox styling → Fluent
    - [ ] Title text + edit input → Fluent Input
    - [ ] Delete button → Fluent icon button
  - [ ] Add row (243–259):
    - [ ] Input → Fluent
    - [ ] Add button → Fluent

- [ ] **Color system:**
  - [ ] Status chips remain amber/green/stone per `STATUS_MAP` (lines 64–68 in status-chip.tsx)
  - [ ] Text colors: `ink-2`, `ink-3` → Fluent gray tokens
  - [ ] Borders: `border-border` → Fluent divider token
  - [ ] Surfaces: `surface-2` (subtask hover) → Fluent subtle fill

- [ ] **Not a scope change:**
  - [ ] ✓ Client status always read-only (no UI change to logic)
  - [ ] ✓ No timer on client surfaces (conditional render unchanged)
  - [ ] ✓ No direct client writes beyond the 6 RPCs (preserve all)
  - [ ] ✓ RLS and internal-thread boundary unchanged

---

## Key File References

| File | Lines | Purpose |
|------|-------|---------|
| `app/(portal)/tasks/page.tsx` | 1–50 | Client route, data fetch, guards |
| `app/(portal)/clients/[clientId]/tasks/page.tsx` | 1–104 | Staff route, all-tasks + time entries + thread resolution |
| `components/tasks/client-task-board.tsx` | 1–133 | Client task grid + detail modal launcher |
| `components/tasks/task-detail-dialog.tsx` | 1–279 | Shared detail modal (client: title+desc; staff: full edit) |
| `components/tasks/task-checklist.tsx` | 1–262 | Subtasks list, add/toggle/edit/delete (both roles) |
| `components/tasks/client-task-dialog.tsx` | 1–139 | Client add-task form |
| `lib/actions/tasks-client.ts` | 1–111 | Client RPC wrappers: `createTaskAction`, `updateTaskAction` |
| `lib/actions/task-checklist.ts` | 1–106 | Client checklist RPCs; staff direct writes |
| `lib/tasks.ts` | 1–71 | Types, `getAssignableMembers()`, circle validation |
| `lib/constants.ts` | 43–47 | `TASK_STATUSES` (todo / in_progress / done) |
| `components/ui/status-chip.tsx` | 1–80 | `StatusChip` recipe for task status display |


---

# Surface: calls-notes

Perfect. Now I have all the information I need. Let me create the comprehensive analysis:

---

## Calls & Notes Surface Analysis

### 1. ROUTE FILE & AUTH GUARD

**File**: `app/(portal)/calls-notes/page.tsx:1-203`

**Auth Guard**: `requireRole(["client"])` (line 21) — CLIENTS ONLY (no staff variant). Currently role-gated, not client_type-aware, so both PROGRAM and PROJECT clients see this surface.

**Data Fetched** (via Supabase SELECT with RLS scoping):
- `call_types` (line 26–29): `id, name, duration_label, frequency_label, booking_url` ordered by `sort_order` — the call type catalog per client
- `call_bookings` (line 32–37): `id, title, start_at, meeting_url` filtered `status='booked'` + future only (`gte "start_at", new Date().toISOString()`)
- `call_recordings` (line 38–41): `id, call_date, call_type, recording_url, key_topic` ordered DESC by date
- `meeting_notes` (line 42–45): `id, title, meeting_date, body` ordered DESC by date

**RLS Enforces** (via policies in `20260610010004_rls_policies.sql`):
- Admins: all read + write
- Team: all read + write (if assigned to client)
- Clients: SELECT ONLY for rows where `visible_to_client=true`

**Client Type Branching**: NONE currently — calls-notes is NOT gated per `client_type`. Both program and project clients access the full UI. (No projects board on this surface; it's messaging/deliverables/tasks/documents that vary.)

---

### 2. RENDER COMPONENT TREE & EXISTING R0/R1 COVERAGE

**Page Body Structure**:
```
PageContainer (width="standard", stack=true)
  ├─ PageHeader (title="Calls & Notes", description)
  ├─ Grid of CallType Cards (grid-cols-2/3)
  │  └─ Card (old shadcn)
  │     ├─ CardHeader / CardTitle / CardDescription
  │     └─ CardContent
  │        └─ BookButton (or <p> fallback)
  │
  ├─ "Upcoming calls" Card
  │  └─ CardContent
  │     └─ [EmptyState or <ul>]
  │        └─ <li> + BookingTime + Button (old shadcn, "amber" variant)
  │
  ├─ "Recordings" Card
  │  └─ CardContent
  │     └─ [EmptyState or <ul>]
  │        └─ <li> + Button (old shadcn, "outline" variant)
  │
  └─ "Meeting notes" Card
     └─ CardContent
        └─ [EmptyState or <div> + Markdown]
```

**Components Being Used**:
- `PageContainer`, `PageHeader` (lines 17–18) — redesign/layout already; no Tailwind card/chip.
- `Card, CardContent, CardHeader, CardTitle, CardDescription` (lines 10–15) — OLD shadcn/ui (components/ui/card.tsx), MUST CONVERT to Fluent Card
- `Button` (line 5, imported from @/components/ui/button) — OLD shadcn, MUST CONVERT to Fluent Button
- `EmptyState` (line 6) — OLD shadcn (components/ui/empty-state.tsx), MUST CONVERT to Fluent equivalent
- `BookButton`, `BookingTime` (lines 7–8) — NEW redesign-aware wrappers; forward-compatible
- `Markdown` (line 4) — client-safe prose renderer, no convert needed

**Lucide Icons** (line 16): `CalendarClock, ExternalLink, FileText, Video` — reusable as-is with Fluent

**Redesign Primitives Already Available** (from `components/redesign/ui/index.ts`):
- `Button, EmberButton` (custom layer wraps Fluent Button with loading state + amber gradient)
- `Card, CardHeader, CardFooter` (Fluent native, solid-by-default, EXACT match)
- `Badge, Avatar, Divider, Table, Dialog, Popover` — available if needed for future expansions
- `StatusPill, DeltaChip` (data-ui.tsx) — not needed here (no status states on calls/recordings/notes)
- `GlassSurface` — for KPI overlay (not applicable to this surface; no summary metrics)

**Glass vs Solid Rule**:
- Call types GRID: SOLID (dense card display of options)
- Upcoming calls LIST: SOLID (tabular booking data)
- Recordings LIST: SOLID (data row display)
- Meeting notes SECTION: SOLID (running prose/markdown content)

None of these are KPI/summary/chrome — all SOLID.

---

### 3. CLIENT WRITE PATHS (RPC / SERVER ACTION)

**ZERO explicit client writes on this surface.** 

The BookButton flow is READ-ONLY from the portal's perspective:
- `BookButton` (components/booking/book-button.tsx:44–55) routes to either:
  - **Cal.com popup** (CalBookingButton via dynamic import) — metadata prefill only, NO direct DB write
  - **External link** (Calendly fallback) — external to portal, NO DB write

**Booking Persistence**: Cal.com webhook only (`app/api/cal/webhook`) — SERVICE ROLE writes to `call_bookings` table. Clients cannot write `call_bookings`, `call_recordings`, or `meeting_notes` directly. These are READ-ONLY views for clients.

**No server actions or RPCs invoked from this page.**

---

### 4. INVARIANT TOUCHPOINTS

**Project-Status & Task-Status Locks**: NOT APPLICABLE — no project/task controls on this surface.

**Time Tracking / Internal Timer**: NOT APPLICABLE — no timers or internal work logs.

**Internal-Thread Boundary**: NOT APPLICABLE — notes are `visible_to_client=true` by default; no internal-only fields exposed.

**Invisible / Internal Task Gating**: NOT APPLICABLE.

**READ-ONLY Invariant (Core)**:
- Call types, recordings, meeting notes are all SELECT-ONLY for clients (enforced by RLS policies: `call_recordings_client_select`, `meeting_notes_client_select`, `call_bookings_client_select`)
- **MUST PRESERVE**: Do not add edit/delete buttons for clients. Maintain `visible_to_client` filtering at DB level.

---

### 5. GLASS PLAN

**NO glass surfaces on this page.**

All sections are SOLID:
- **Call type cards** (line 56–81): Catalog of booking options → SOLID card grid
- **Upcoming calls** (line 84–125): Booked future sessions → SOLID list with metadata
- **Recordings** (line 127–167): Past recordings links → SOLID list with link access
- **Meeting notes** (line 169–200): Rich prose from staff → SOLID container with Markdown

This is a DATA/CONTENT-HEAVY page (no dashboard KPI, no hero, no chrome overlays). All `Card` components should render as opaque Fluent `Card` with default theming (no `GlassSurface`).

---

### 6. CONVERSION CHECKLIST

**Both client types (PROGRAM + PROJECT):**

- [ ] Replace `@/components/ui/card.tsx` imports with `@/components/redesign/ui` (Fluent `Card, CardHeader, CardFooter`)
  - Line 10–15 imports → remove old imports
  - Line 58, 84, 127, 169: `<Card>` → stays same (Fluent API compatible at component name)
  - Line 59, 85, 128, 170: `<CardHeader>` → compatible (Fluent export)
  - Line 60, 86, 129, 171: `<CardTitle>` → CUSTOM; Fluent has no CardTitle wrapper; use `<div className="text-lg font-semibold">` or `<Fluent.Text>` + styling

- [ ] Replace `@/components/ui/button.tsx` imports with `@/components/redesign/ui` (custom `Button`)
  - Line 5 import → swap to redesign
  - Line 114, 156: `<Button variant="amber">` → `<Button appearance="primary">` (Fluent appearance model; amber maps to primary in Warm Obsidian theme)
  - Line 156: `variant="outline"` → `appearance="outline"` (Fluent)

- [ ] Replace `EmptyState` (line 6) with Fluent equivalent or inline:
  - No Fluent EmptyState primitive; create wrapper or inline JSX:
    ```tsx
    <div className="flex flex-col items-center justify-center gap-2 rounded-xl border-2 border-dashed border-border px-6 py-12 text-center">
      {icon && <span className="text-ink-faint">{icon}</span>}
      <Text>{title}</Text>
      {description && <Caption1 className="text-ink-3">{description}</Caption1>}
    </div>
    ```
  - OR source a `design-token`-aware EmptyState stub in redesign/ui

- [ ] Import Fluent `Text`, `Caption1` for typography (line 20 `redesign/ui/index.ts` already exports these)

- [ ] Keep `BookButton`, `BookingTime`, `Markdown` as-is (already redesign-aware or format-agnostic)

- [ ] Keep `PageContainer`, `PageHeader` as-is (already redesign)

- [ ] Keep Lucide icons as-is (no change needed for icon set)

- [ ] Verify RLS policies remain untouched (no invariant changes) — clients still SELECT-only

**Verification Steps**:
1. Page loads with Fluent theme (no Tailwind Card artifacts)
2. Call type grid renders as opaque cards, not glass
3. Upcoming calls list shows with Fluent Button styling (primary="amber" for Join CTA, outline for fallback)
4. Recordings link buttons render as outline Fluent buttons
5. Meeting notes prose renders with Markdown (no Card styling change; Fluent-aware Markdown wrapper if needed)
6. Empty states render with correct icon + text layout (centered, dashed border or minimal border)
7. No client can write or edit any of these rows (RLS + UI read-only)

---

### 7. ROUTE & COMPONENT FILE PATHS

**Route**:
- `app/(portal)/calls-notes/page.tsx` (server component, async)

**Booking Components** (client-side, already forward-compatible):
- `components/booking/book-button.tsx` (wrapper; routes to Cal or external)
- `components/booking/cal-booking-button.tsx` (Cal.com popup; includes metadata config at line 46–51)
- `components/booking/booking-time.tsx` (client-side TZ local render; hydration-safe)

**Layout**:
- `components/layout/page-container.tsx` (already redesign)
- `components/layout/page-header.tsx` (already redesign; no change needed)

**Markdown**:
- `components/markdown.tsx` (format-agnostic; no UI framework dependency)

**Old UI to Replace** (from page imports):
- `components/ui/card.tsx` → `components/redesign/ui` (Fluent re-export)
- `components/ui/button.tsx` → `components/redesign/ui` (custom Button wrapper)
- `components/ui/empty-state.tsx` → inline or `components/redesign/ui/empty-state.tsx` stub

**Fluent Barrel**:
- `components/redesign/ui/index.ts` (imports from `@fluentui/react-components` + custom Button/EmberButton)

---

### FINAL NOTES

- **Calls & Notes is CLIENT-ONLY, READ-ONLY, ZERO write paths** — the simplest conversion candidate on the portal.
- Cal.com metadata passing (clientId, callTypeId) in CalBookingButton (line 51) stays untouched; it's a browser-only config, not DB-affecting.
- Both PROGRAM and PROJECT clients see this surface identically (no client_type branching).
- Glass rule does not apply; all SOLID.
- No invariant touching; no status locks, no timers, no internal content.


---

# Surface: documents

Now I have all the information I need. Let me compile the comprehensive markdown report:

## Summary

The **documents surface** (`app/(portal)/documents/page.tsx`) is a READ-ONLY client-facing view that displays files grouped by category with download functionality. It applies to **BOTH client types** (program and project clients) — there is **no branching** on `client_type` for this surface. The documents page is the simplest surface in the client portal: no writes, no status controls, no internal threads, just a categorized file list with signed-URL downloads.

---

## 1. Route File & Data Fetch

**File:** `app/(portal)/documents/page.tsx:1-77`

**Auth guard:** `requireRole(["client"])` at line 18
- Redirects non-clients (team/admin) — they have a different `/clients/{clientId}/files` staff editor.
- No client branching; the only `profile.client_id` use is passing it to `DownloadButton` (line 65).

**Data fetched (RLS-scoped):**
- **Table:** `files` (line 22-25)
- **Selection:** `id, name, category, storage_path, size_bytes`
- **Filters:** implicit RLS: `client_id = app.my_client_id() AND visible_to_client = true`
- **Order:** `created_at DESC` (newest first)
- **Result shape:** `files[]` with no joins

**RLS policy in effect:** `files_client_select` (`20260610010004_rls_policies.sql`):
```sql
for select to authenticated
using (client_id = app.my_client_id() and visible_to_client)
```
Clients see ONLY files marked `visible_to_client=true` for their own client.

**No branching on client_type:** The page fetches and renders identically for both program and project clients.

---

## 2. Render Component Tree & Existing R0/R1 Coverage

**File hierarchy:**

- `app/(portal)/documents/page.tsx` (RSC)
  - `PageContainer` (`components/layout/page-container.tsx`)
    - `PageHeader` (`components/layout/page-header.tsx`) — title + description
    - `EmptyState` (`components/ui/empty-state.tsx`) — when `list.length === 0`
    - **file list sections** (inline, no separate component)
      - `<h2>` category header (uppercase text, handwritten styles)
      - `<ul>` list rows (handwritten flex/gap styles)
        - file name + size
        - `DownloadButton` (`components/files/download-button.tsx`)

**Existing R0/R1 components already covering pieces:**
- **No existing R0/R1 coverage.** The documents page uses only:
  - R0 layout primitives: `PageContainer`, `PageHeader`, `EmptyState` (these have **no redesign equivalents yet** — they remain shadcn/Tailwind)
  - R0 button: `Button` from shadcn (`components/ui/button.tsx`) via `DownloadButton`
  - No status chips, avatars, metric values, or cards (the entire page is plain text + buttons)

**Components NOT needed for documents:** StatusChip, PersonAvatar, MetricValue/Delta, Card (no summary/KPI surfaces here).

---

## 3. Client Write Paths

**Documents is READ-ONLY for clients.** The page calls **zero server actions.**

The `DownloadButton` component (`components/files/download-button.tsx:26-28`) calls:
- **`getSignedUrlAction(clientId, path)`** — `lib/actions/storage.ts:49-65`
  - **NOT a mutation** — it's a read action (mints a signed URL for a file the client already has RLS access to)
  - Guard: `requireClientAccess(clientId)` at line 53
  - **FLAGGED ISSUE (from CLAUDE.md §94-105):** This action calls `requireClientAccess`, which **redirects clients** to `/dashboard`. Additionally, the user-scoped Supabase client has **NO `storage.objects` policy**, so `createSignedUrl` would fail. The correct fix is the **service-role-after-RLS pattern** (verify RLS access, then sign with service role) — exactly what Phase 5d message attachments use. **This is out of scope for the conversion; map it as-is.**

**Write paths on the STAFF /clients/{clientId}/files editor (NOT this surface):**
- `uploadDocumentAction` — `lib/actions/files.ts:18-66`
- `setFileVisibilityAction` — `lib/actions/files.ts:68-92`
- `deleteFileAction` — `lib/actions/files.ts:94-128`

These are **staff-only** and do not appear on the client documents surface.

---

## 4. Invariant Touchpoints

**Documents is entirely READ-ONLY for clients — zero invariants apply here.**

Invariant-related surfaces elsewhere:
- **status controls (READ-ONLY chips for clients):** deliverables, tasks, projects, milestones — NOT on documents
- **time-tracking/timer (must NOT appear on client surfaces):** NOT used anywhere in the portal currently
- **internal-thread content (must never be reachable):** `/messages` hides internal threads via RLS; NOT on documents
- **task gating (invisible/internal):** `/tasks` respects `visible_to_client` flag; NOT on documents

**On the documents surface:** no status, no timers, no internal threads, no tasks. The page has **zero invariant touchpoints.**

---

## 5. GLASS Plan

**Documents uses SOLID throughout — no glass surfaces.**

Rationale:
- **SOLID (dense tables, forms, running text):** The file list is a plain table-like list (handwritten `<ul>` with flex rows). This is dense tabular data — **solid only**.
- **No KPI/summary surfaces:** No metrics, deltas, badges, or key figures that would warrant glass-with-scrim.
- **No chrome/overlays:** No modals, dialogs, or navigation that would use glass.

**Conversion:** Reuse Fluent v9 `Card` primitives for the category sections if a card wrapper is desired, but the page structure (category heading + list) can remain a simple `<div>` with a border. The file-list rows should stay **solid**.

---

## 6. Conversion Checklist

**For BOTH client types (program and project clients):**

- [ ] **Layout primitives:** Keep `PageContainer`, `PageHeader` from R0 (no redesign variants yet).
  
- [ ] **Empty state:** Reuse existing `EmptyState` or wrap in a Fluent Card if upgrading to glass-bordered treatment.
  
- [ ] **Category headers:** Replace handwritten `<h2 className="text-[11px] font-bold tracking-wider text-ink-3 uppercase">` with the Redesign **`Eyebrow`** primitive (`@/components/redesign/ui`; tone="amber" for light, tone="onDark" for dark) to match the system's mono label aesthetic.
  
- [ ] **File list sections:** Wrap each category's `<ul>` in a Fluent **`Card`** (SOLID, `dark` variant in dark mode). Apply `rounded-2xl border border-border` via Fluent Card styling. Replace inline `divide-y divide-row-divider` with Fluent table or list structure if adopting Fluent fully (or keep as `<ul>` if Tailwind border suffices).
  
- [ ] **File rows:** 
  - File name: use Fluent **`Body1Strong`** or **`Text`** (SOLID, no glass).
  - File size: use Fluent **`Caption1`** (SOLID).
  - Download button: replace shadcn `Button` with Fluent **`Button`** (outline or ghost variant, size="small").
  
- [ ] **DownloadButton component:** Update `Button` import from `@/components/ui/button` to `@/components/redesign/ui` (Fluent Button). The component logic (call `getSignedUrlAction`, open URL) stays unchanged. **DO NOT fix the flagged `/documents` download RLS issue** — that's a separate decision.
  
- [ ] **Mode awareness:** Wrap in `useRedesignMode()` if Eyebrow tone needs to switch (already done in `DownloadButton` — keep it client-only). Category headings are pure presentation, so `tone` can be hardcoded to "amber" (light/dark detected in the `Eyebrow` component itself).
  
- [ ] **Density:** Clients use `data-density="spacious"` via the shell; the page inherits this. No explicit density override needed.
  
- [ ] **No write-path changes:** `getSignedUrlAction` logic unchanged. No audit, no mutations.

---

## File References

| File | Line | Purpose |
|------|------|---------|
| `app/(portal)/documents/page.tsx` | 1–77 | Main client documents page (RSC, no rewrites) |
| `components/files/download-button.tsx` | 1–41 | Client-facing download button (update Button import) |
| `lib/actions/storage.ts` | 49–65 | `getSignedUrlAction` (keep logic; flagged RLS issue out of scope) |
| `lib/constants.ts` | 76–86 | `FILE_CATEGORIES` enum (already used; no change) |
| `components/layout/page-container.tsx` | 1–49 | Layout primitive (keep R0) |
| `components/layout/page-header.tsx` | 1–55 | Title + description (keep R0) |
| `components/ui/empty-state.tsx` | 1–39 | Empty state (keep R0 or wrap in Card) |
| `components/redesign/ui/surfaces.tsx` | 65–92 | `Eyebrow` primitive (use for category headers) |
| `components/redesign/ui/button.tsx` | — | Fluent Button (import in DownloadButton) |
| `components/redesign/ui/index.ts` | 1–125 | Redesign barrel (Card, Button, Text, Caption1, etc.) |
| `app/shell/client-shell.tsx` | 78–100 | `ClientShell` (both client types, no branching on documents) |
| `CLAUDE.md` | 94–105 | Flagged `/documents` download RLS issue (document, don't fix) |

---

## Implementation Notes

1. **The documents surface is the simplest client page:** no forms, no status controls, no internal boundaries, no write paths. A straightforward Fluent upgrade.

2. **No client_type branching:** Both program and project clients see the identical page. The shell (`ClientShell`) branches nav (hiding Program/Performance/Content tabs for project clients), but the `/documents` route itself does not branch.

3. **Fluent Button in DownloadButton:** The only significant code change is replacing the shadcn Button import with Fluent's. The open-and-download logic remains verbatim.

4. **RLS is already correct:** The page's data fetch is RLS-scoped; no policy changes needed. The `getSignedUrlAction` issue (flagged in CLAUDE.md) is a separate issue flagged for decision outside this conversion.

5. **Glass rule:** SOLID only. The file list is dense tabular data. No KPI surfaces, no summary cards. Wrap category sections in Fluent Cards (solid) with Eyebrow labels.


---

# Surface: messages

Perfect! Now I have all the information I need. Let me create the comprehensive analysis report:

---

## Messages Surface — Redesign Scope & Implementation Guide

### 1. Route File & Auth Guard

**File:** `app/(portal)/messages/page.tsx` (lines 1–42)

**Auth & RLS Boundary:**
- Guard: `requireRole(["client"])` — client-only surface (staff access via `/clients/{id}/messages`)
- Data fetch (parallel):
  - `threads.select("id").eq("type", "client_shared").maybeSingle()` — RLS: `threads_client_select` policy filters ONLY the client's own `client_shared` thread; internal thread is invisible
  - `getAssignableMembers(me.client_id)` — for @mention circle in composer

**Branching:** Single-type client (no branch on client_type; design applies only to program & project clients in same portal). Clients are RLS-scoped to one thread, visible once (the page does NOT branch).

**Data shape fetched:**
```
threads: { id }
messages: (passed to Conversation as initialMessages via getThreadMessagesAction)
  → ThreadMessage[] = { id, body, authorId, authorName, authorRole, createdAt, attachmentName, editedAt }
thread_participants: (fetched in Conversation.useEffect)
  → ThreadParticipant[] = { id, name }
```

---

### 2. Render Component Tree (Paths to Reskin)

**Root component:** `components/messaging/conversation.tsx` (client, 672 lines)

**Subcomponents:**
- `components/messaging/message-attachment.tsx` (73 lines) — image inline preview or download chip
- `components/ui/dropdown-menu` (shadcn) — emoji picker
- `components/ui/textarea` (shadcn) — composer input
- `components/ui/button` (shadcn) — send/save/cancel/delete
- `components/ui/alert-dialog` (shadcn) — delete confirmation
- `components/markdown` — message body rendering
- `components/tasks/task-create-dialog` — inline task creation from message

**Existing R0/R1 redesign components that cover pieces:**
- `components/redesign/ui/button.tsx` → `Button` (Fluent + loading state) & `EmberButton` (amber gradient CTA) — REPLACE shadcn Button
- `components/redesign/ui/surfaces.tsx` → `GlassSurface` (dark/strong/ember variants) — USE for card/summary surfaces
- No existing Fluent textarea, dropdown, alert-dialog, markdown, or attachment preview in redesign layer — these remain shadcn or need new wrappers

---

### 3. Client Write Paths (MUST Preserve Verbatim)

All paths are **SECURITY DEFINER RPCs** — the client calls via server actions; RLS can never be bypassed.

**File:** `lib/actions/messages.ts` & `lib/actions/message-attachments.ts`

| Action | RPC Called | Route | Invariant |
|--------|-----------|-------|-----------|
| `postMessageAction(threadId, body, mentionedIds, attachmentPath, attachmentName)` | `post_message(p_thread_id, p_body, p_attachment_path, p_attachment_name)` | `messages.ts:42–118` | **DEFINER checks**: `app.can_access_thread()` (client OWN client_shared only) + body-or-attachment required |
| `editMessageAction(messageId, body)` | `edit_message(p_message_id, p_body)` | `messages.ts:264–270` | **DEFINER checks**: author-only, thread re-checked, non-deleted |
| `deleteMessageAction(messageId)` | `delete_message(p_message_id)` | `messages.ts:272–278` | **DEFINER checks**: soft-delete (idempotent), author-only, thread re-checked |
| `markThreadViewedAction(threadId, notifLink)` | `mark_thread_read(p_thread_id)` + notification updates | `messages.ts:244–255` | **DEFINER checks**: `app.can_access_thread()`; clears bell notifications for thread |
| `getThreadMessagesAction(threadId, after?)` | Direct SELECT (no RPC) | `messages.ts:168–181` | **RLS-scoped**: `messages_client_select` (shared-only, non-deleted) |
| `searchThreadMessagesAction(threadId, query)` | Direct SELECT (no RPC) | `messages.ts:222–236` | **RLS-scoped**: `messages_client_select` with ILIKE body search |
| `getThreadParticipantsAction(threadId)` | Direct SELECT (no RPC) | `messages.ts:130–159` | **RLS-scoped** thread read; staff list is filtered per thread type (internal → staff only) |
| `uploadMessageAttachmentAction(threadId, formData)` | Direct thread SELECT + service-role storage.upload | `message-attachments.ts:25–49` | **RLS checks**: thread read gates; service role does upload (clients lack storage policy) |
| `getMessageAttachmentUrlAction(messageId)` | Direct message SELECT + service-role signed URL | `message-attachments.ts:58–74` | **RLS checks**: message read gates; RLS denies internal messages, so clients never fetch internal attachments |

**Critical:** All six boundary gates in Conversation.tsx at line 118–137 (syncNew) and 142–145 (fullRefetch) use RLS-scoped actions, never raw realtime payloads.

---

### 4. Invariant Touchpoints

#### A. **Internal Thread Boundary** (Four-layer enforcement)
- **Layer 1 (DB RLS):** `threads_client_select` & `messages_client_select` = `type = 'client_shared'` only; internal invisible
- **Layer 2 (RPC check):** `app.can_access_thread()` validates thread type matches caller scope
- **Layer 3 (Realtime filter):** Conversation line 284 — raw payload never rendered; incremental fetch via RLS-scoped action
- **Layer 4 (Attachment access):** `getMessageAttachmentUrlAction` reads message via RLS; internal read fails, so URL never minted

**Location:** app/(portal)/messages/page.tsx line 15 comment + messages.ts 122–128 (getThreadParticipantsAction) + conversation.tsx 273–301 (realtime subscribe)

#### B. **Status Chip / Audience Indicator** (Read-Only, No Status Lock Like Tasks)
- **Line 361–363:** `banner` (amber "Internal…" | emerald "Visible to client") — VISUAL ONLY, no interaction
- **Clients see:** banner hidden (isClient check line 360); no option to flip audience
- **Staff see:** banner visible in both audiences; toggles via the `audience` prop (but clients never reach staff routes)
- **Invariant:** The audience is BAKED into the thread row (type column) — immutable after creation

**Location:** conversation.tsx 361–363 (display logic) + page.tsx line 36 (`audience="shared"` hardcoded for clients)

#### C. **Time Tracking & Invisible Internal Gating**
- **No time-tracking UI on this surface** — no timers, no billable-hours widgets, no internal-only fields
- **Internal thread is INVISIBLE to clients at the DB layer** — no "you lack permission" message; it simply doesn't exist (RLS returns empty)

**Location:** RLS policies in migrations/20260615224421_communication_4a.sql lines 40–42 (threads), 64–66 (messages)

---

### 5. Glass Plan — Surface Categorization

Per the redesign rule: **glass-with-scrim (KPI/summary/cards)** vs **SOLID (dense tables, forms, running text)**

#### Glass Surfaces (Ember-Obsidian + Scrim)
- **Message list container** (line 404): `.flex-1.space-y-3.overflow-y-auto` — BECOMES `GlassSurface dark strong` (reading area needs scrim for text)
- **Individual message bubble** (line 414–418): Currently amber/white nested card — BECOMES `GlassSurface dark` (each message is a standalone pane)
- **Search dropdown** (line 385): `.absolute.inset-x-2.top-full.z-20.max-h-72` — BECOMES `GlassSurface dark` (summary/context panel)
- **@mention dropdown** (line 596): `.absolute.bottom-full.left-0.mb-1.w-64` — BECOMES `GlassSurface dark` (transient picker)

#### Solid Surfaces (Dense Interaction)
- **Composer textarea** (line 626): Running text input — **REMAIN solid** (form control, no glass)
- **Action buttons** (emoji, bold, italic, attach, task, send) (lines 537–570): Dense toolbar — **REMAIN solid** (tight controls)
- **Search input** (line 369–382): `rounded-lg bg-surface-2 px-2.5` — **REMAIN solid** (dense form)
- **Attachment chip** (line 524–531): Flex badge — **REMAIN solid** (dense label)
- **Who-can-see banner** (line 517–521): Informational chip — **REMAIN solid** or `GlassSurface` (context) — TBD per design

#### No Change Needed
- Modal/dialog surfaces (delete confirm, task create) — import existing Fluent equivalents

---

### 6. Conversion Checklist

#### For Both Client Types (Program & Project)

- [ ] **Conversation.tsx imports & button replacement**
  - [ ] Import `Button`, `EmberButton` from `components/redesign/ui/button`
  - [ ] Replace all shadcn `Button` calls with redesign `Button` (maintain `loading` prop)
  - [ ] Replace "Send" CTA with `EmberButton` (amber gradient + white text)
  - [ ] Keep shadcn `Textarea` for now (no Fluent equivalent yet)

- [ ] **Message list & bubble styling**
  - [ ] Wrap message list (line 404) in `GlassSurface dark strong` with classes preserved
  - [ ] Wrap each message bubble (line 414–418) in `GlassSurface dark ember` (decorative bloom on own messages)
  - [ ] Keep amber/white asymmetry for mine/theirs (mine = right-align, amber-tinted; theirs = left, neutral)

- [ ] **Dropdowns & transient surfaces**
  - [ ] Emoji picker dropdown → `GlassSurface dark` wrapper
  - [ ] @mention picker dropdown → `GlassSurface dark` wrapper
  - [ ] Search results dropdown → `GlassSurface dark` wrapper

- [ ] **Attachment component**
  - [ ] Inline image preview (message-attachment.tsx): NO change (lazy-loaded via boundary-gated signed URL, already secure)
  - [ ] Download chip (message-attachment.tsx): Keep styled as `GlassSurface` or solid (keep existing design)

- [ ] **Composer toolbar**
  - [ ] Left-aligned action buttons (emoji, bold, italic, attach, task) — `actionBtn` classes → Fluent button equivalents
  - [ ] Keep amber hover/focus emphasis (line 61: `hover:border-amber-300 hover:bg-amber-50`)
  - [ ] File input hidden (line 583–592) — NO change

- [ ] **Context helpers**
  - [ ] Audit: NO time-tracking, NO internal-only fields, NO status controls
  - [ ] Verify: sendTransition, optimistic append (line 331–342), syncNew/fullRefetch use only RLS-scoped actions
  - [ ] Verify: Realtime subscribe never renders raw payload (line 273–301)

- [ ] **No-op: server actions**
  - [ ] `postMessageAction`, `editMessageAction`, `deleteMessageAction`, `markThreadViewedAction`, `getThreadMessagesAction`, `searchThreadMessagesAction`, `getThreadParticipantsAction`, `uploadMessageAttachmentAction`, `getMessageAttachmentUrlAction` — **VERBATIM, no changes**

- [ ] **Testing & mobile**
  - [ ] Verify glass fallback under reduced-transparency/reduced-motion (CSS in redesign.css lines 130–150 handle it automatically)
  - [ ] Mobile placeholder: `coarse ? "Write a message…" : "Write a message… (markdown · @ to mention · ⌘↵ to send)"` (line 659–660) — NO change
  - [ ] Test emoji picker, @mention keyboard nav, message edit/delete on mobile

---

### File References

| Role | Path | Lines | Notes |
|------|------|-------|-------|
| **Route** | `app/(portal)/messages/page.tsx` | 1–42 | Auth guard, data fetch, thread discovery |
| **Main component** | `components/messaging/conversation.tsx` | 1–672 | Full UI: search, message list, composer, realtime, emoji/mention/edit/delete |
| **Attachment** | `components/messaging/message-attachment.tsx` | 1–73 | Signed URL fetch + inline preview |
| **Server actions** | `lib/actions/messages.ts` | 1–279 | postMessage, editMessage, deleteMessage, markThreadViewed, getThreadMessages, searchThreadMessages, getThreadParticipants |
| **Attachment actions** | `lib/actions/message-attachments.ts` | 1–75 | uploadMessageAttachment, getMessageAttachmentUrl |
| **RLS policies** | `supabase/migrations/20260615224421_communication_4a.sql` | 1–178 | threads (lines 20–43), messages (lines 45–68), thread_type boundary, post_message/mark_thread_read RPCs |
| **Edit + soft-delete** | `supabase/migrations/20260616191150_messaging_edit_soft_delete.sql` | 1–88 | edit_message, delete_message RPCs, deleted_at RLS narrowing |
| **Attachments** | `supabase/migrations/20260616144138_messaging_attachments_5d.sql` | 1–62 | attachment_path, attachment_name columns, extended post_message RPC |
| **Glass tokens** | `app/(redesign)/redesign.css` | 1–200+ | `.rd-glass`, `.rd-glass--dark`, `.rd-glass--strong`, `.rd-glass--ember`, fallback rules |
| **Redesign buttons** | `components/redesign/ui/button.tsx` | 1–77 | Button (Fluent + loading), EmberButton (amber CTA) |
| **Redesign surfaces** | `components/redesign/ui/surfaces.tsx` | 1–130 | GlassSurface (dark, strong, ember), Eyebrow, Measure, Shell, AmbientField |


---

# Surface: settings

Now I have all the information I need. Let me compile the comprehensive report:

## Settings Surface Conversion Report

### 1. Route & Auth Guard + Data Fetch

**File:** `app/(portal)/settings/page.tsx:16-48`

- **Auth Guard:** `requireProfile()` (line 17) — RLS-enforced per-user access; any authenticated, active user can access.
- **Data Fetched:**
  - `profiles` row (server-cached via `requireProfile()`): `id`, `full_name`, `email`, `avatar_url`, `role`
  - `notification_preferences` row (line 20–24): optional; RLS scopes to caller's own user_id. Columns: all boolean email preference flags (`email_message`, `email_deliverable_delivered`, `email_deliverable_approved`, `email_report_published`, `email_project_status`, `email_call_booked`). Defaults to all-true if no row exists.
- **Client Type Branching:** NONE. Settings is role-aware (client/team/admin) but does not branch by `client_type` (program vs project). All client types get the same settings surface.

---

### 2. Component Tree & R0/R1 Coverage

| Path | Purpose | Already-Covered R0/R1 |
|------|---------|----------------------|
| `components/settings/avatar-upload.tsx` | Photo upload/remove UI | `PersonAvatar` (R0, `components/ui/person-avatar.tsx`), `Button` (shadcn; needs reskin to Fluent) |
| `components/settings/profile-form.tsx` | Name edit + password reset | `Input`, `Label`, `Button` (all shadcn; need reskin), card layout (existing shadcn) |
| `components/settings/email-preferences.tsx` | Toggle grid for notification prefs | `Switch` (shadcn; reskinned in R0 with amber/border styling; can stay or upgrade to Fluent) |
| `components/layout/page-container.tsx` | Page width/density/stack layout | REUSABLE AS-IS (width="focused" for settings) |
| `components/layout/page-header.tsx` | Title + description + eyebrow | REUSABLE AS-IS |
| `components/ui/card.tsx` | Card wrapper | shadcn; needs reskin to Fluent Card or kept as solid container |
| `components/ui/person-avatar.tsx` | Avatar display | R0 component; uses shadcn Avatar fallback; stays |

**R1 Redesign Barrel:** `components/redesign/ui/index.ts` re-exports Fluent v9 primitives:
  - `Button`, `EmberButton` (custom Fluent wrappers with loading state) — at line 13
  - `Input`, `Label`, `Checkbox`, `Switch`, `Textarea`, `Field`, `Radio` — Fluent forms, line 21–29
  - `Card`, `CardHeader`, `CardFooter`, `CardPreview` — Fluent data display, line 62–73
  - Glass helpers: `GlassSurface`, `Shell`, `AmbientField`, `Eyebrow` — line 116

**Conversion Path:**
- Replace `components/ui/{Button, Input, Label, Switch, Card}` imports → `components/redesign/ui` (Fluent + Warm Obsidian theme)
- Keep `PersonAvatar` (R0 layer)
- Keep `PageContainer` and `PageHeader` (layout layer, already density/theme-aware)

---

### 3. Client Write Paths (Server Actions)

**MUST be preserved verbatim:**

| Action | File | Signature | What it Does |
|--------|------|-----------|-------------|
| `updateOwnProfileAction(input)` | `lib/actions/profile.ts:41–67` | `{ full_name: string }` → update caller's profiles row; RLS guard `profiles_update_own` blocks role/client_id/is_active | Updates full_name only; triggers profile revalidate |
| `uploadAvatarAction(form)` | `lib/actions/profile.ts:75–117` | FormData (file) → service-role upload to `avatars` bucket, cache-bust URL, write avatar_url to profiles; RLS permit via `profiles_update_own` | Validates magic bytes; 2 MB cap; audit log |
| `removeAvatarAction()` | `lib/actions/profile.ts:119–140` | void → delete from storage, set avatar_url=null | Storage cleanup + audit log |
| `sendPasswordResetAction(email)` | `lib/actions/auth.ts` (not shown but called line 11 profile-form.tsx) | `{ email: string }` → Supabase sendPasswordResetEmail | Check guards in auth.ts; likely no change needed |
| `updateEmailPreferencesAction(values)` | `lib/actions/notification-preferences.ts:16–32` | `Record<string, boolean>` → upsert notification_preferences row; RLS scopes to user_id=auth.uid(), allowlist enforced in action | Revalidates /settings; only allowed columns written per role |

---

### 4. Invariant Touchpoints

**Settings surface is purely personal (no team/client data at risk):**

- **No status chips/controls:** Profile name and email are NOT status-gated; avatar is cosmetic.
- **No internal-thread access:** No task/project/message browsing; only own profile/prefs.
- **No time-tracking/timer:** Not present on this surface.
- **RLS enforcement:** All writes are self-only (`user_id = auth.uid()`); the server actions also allowlist columns (notification prefs).
- **Invariant touchpoints:** NONE on this surface. Settings is safe; it's the safest surface (personal data only, no cross-client risk).

---

### 5. Glass Plan

**Settings is a form/settings surface → SOLID throughout.**

Per the rule: glass only on KPI/summary/overlays with scrim; forms, tables, running text → solid.

- **Sections (Cards):** SOLID
  - Photo upload card: SOLID
  - Profile edit card: SOLID (form)
  - Password reset card: SOLID (form)
  - Email preferences card: SOLID (form/toggles)
- **PageHeader:** SOLID (typography + context)
- **No glass overlays** on this surface.

---

### 6. Conversion Checklist

**Both client types use identical settings surface** (role-aware filters only; no program/project branch).

- [ ] **Import reskin:**
  - [ ] `components/ui/button` → `components/redesign/ui/Button` (Fluent with loading state)
  - [ ] `components/ui/input` → `components/redesign/ui/Input` (Fluent)
  - [ ] `components/ui/label` → `components/redesign/ui/Label` (Fluent)
  - [ ] `components/ui/switch` → `components/redesign/ui/Switch` (Fluent) — verify amber/border styling matches
  - [ ] `components/ui/card` → `components/redesign/ui/Card` (Fluent) — use CardHeader/CardContent/CardFooter re-exports
  - [ ] Keep `PersonAvatar` import unchanged (R0 component)

- [ ] **File updates:**
  - [ ] `app/(portal)/settings/page.tsx` — update imports only; no logic change
  - [ ] `components/settings/profile-form.tsx` — update form imports; verify form validation/submit still works
  - [ ] `components/settings/email-preferences.tsx` — update Switch/Button/Card imports; verify toggle state logic
  - [ ] `components/settings/avatar-upload.tsx` — update Button/PersonAvatar imports

- [ ] **Server actions (NO CHANGES):**
  - [ ] Verify `updateOwnProfileAction`, `uploadAvatarAction`, `removeAvatarAction`, `updateEmailPreferencesAction` are called identically
  - [ ] Verify RLS policies still allow writes (they will; no action code changed)
  - [ ] Verify `revalidatePath("/settings")` fires after each action

- [ ] **Test:**
  - [ ] Profile name edit + save (check full_name updates, toast shown)
  - [ ] Avatar upload, change, remove (check file magic-bytes validation, cache-bust URL, storage cleanup)
  - [ ] Password reset email send (check email arrives)
  - [ ] Email preference toggles + save (check notification_preferences row upserted, role-based filtering)
  - [ ] All BOTH client types (role filtering applies to email prefs; both see same surface otherwise)

- [ ] **Fluent theme integration:**
  - [ ] Wrap page in `<FluentScope>` + `<ThemedFluent>` if not already inherited from portal layout
  - [ ] Verify Warm Obsidian amber interactive + warm neutrals apply to buttons, inputs, switches
  - [ ] Verify AA contrast on all text (Fluent tokens + CSS vars handle this)

---

**Files to edit:**
- `app/(portal)/settings/page.tsx`
- `components/settings/profile-form.tsx`
- `components/settings/email-preferences.tsx`
- `components/settings/avatar-upload.tsx`

**No database/RLS/action changes required.**
