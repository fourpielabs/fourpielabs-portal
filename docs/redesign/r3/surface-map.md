# Redesign R3 — Staff Workspace + Admin Surface Map

Generated by the r3-staff-admin-map workflow (8 parallel readers). Drives the wave-based conversion. Glass = chrome/overlays/summary only; dense staff surfaces (metrics grid, tables, audit, forms) SOLID. Internal-thread boundary + timer + admin guards = presentation-only conversion, re-proven post-conversion.



---

# Scope: staff-chrome

Perfect. Now I have a comprehensive picture. Let me create the markdown report with all the findings:

## Staff Workspace Chrome Conversion Scope: Layout, Header & Tabs

### (1) Routes & Auth Guards

**Primary staff workspace layout:**
- **File:** `app/(portal)/clients/[clientId]/layout.tsx` (lines 14-84)
- **Auth guard:** `requireClientAccess(clientId)` (line 23) — admins always, team members only when assigned via `client_assignments` table
- **Data fetched:**
  - `clients` table: `id, name, slug, status, program, client_type, start_date, logo_url` (lines 26-30)
  - Calculates `dayLabel` for program clients only (lines 37-43: "Day N of 90")

**Per-client header location:** Embedded in the layout (lines 46-82) — part of the chrome, not a separate component.

**Tab navigation:**
- **File:** `components/clients/client-tabs.tsx` (lines 1-114, client component)
- **Props:** `clientId`, `isAdmin` (bool), `clientType` ("program" | "project")
- **Logic:** Branches tab list on `clientType` (lines 30-50):
  - **Projects:** Overview, Messages, Projects, Deliverables, Tasks, Calls
  - **Programs:** Overview, Messages, Checklist, Program, Content, Metrics, Competitors, Deliverables, Tasks, Calls
- **More menu:** Notes, Reports, Updates, Files (+ Settings if admin)

**Related admin pages:**
- `app/(portal)/clients/new/page.tsx` (client creation) — `requireRole(["admin"])`
- `app/(portal)/clients/page.tsx` (staff workspace list) — `requireRole(["admin", "team"])`
- `app/(portal)/admin/users/page.tsx` — `requireRole(["admin"])`

---

### (2) Render Component Tree (R0/R1/R2 Coverage)

**Chrome region (to convert):**
```
WorkspaceContainer (app/(portal)/clients/[clientId]/layout.tsx line 46)
  └─ flex layout (flex-wrap, gap-3)
     ├─ PersonAvatar (square, logo_url) — components/ui/person-avatar.tsx
     ├─ Header text block (h1 + badges + optional gear icon)
     │  ├─ h1: "font-display text-2xl font-semibold" — client.name
     │  ├─ Badge (variant="secondary" for Project | variant="amber" for Program)
     │  └─ StatusChip (kind="client", value=status)
     └─ Settings link (admin only, MoreHorizontal icon)
  └─ ClientTabs (client component, flex overflow-x-auto, border-b)
     ├─ Primary tabs (Link + animated underline via motion/layout)
     └─ DropdownMenu (More) — Radix UI primitives
  └─ {children} — page body (outside chrome, old Tailwind)
```

**Current UI components in use (old shadcn/Tailwind):**
- `PersonAvatar` (components/ui/person-avatar.tsx) — wraps Avatar/AvatarFallback/AvatarImage
- `Badge` (components/ui/badge.tsx) — CVA variants (default, secondary, outline, amber, destructive)
- `StatusChip` (components/ui/status-chip.tsx) — hardcoded color recipes per kind+value
- `DropdownMenu` — Radix UI (components/ui/dropdown-menu.tsx)
- `Button` (components/ui/button.tsx) — old shadcn, inline-flex, border

**R2 redesign primitives available (components/redesign/ui/):**
- `Button` — Fluent Button + loading state (already wraps FluentUI)
- `EmberButton` — bespoke gradient CTA (#b45309 light, gradient+glow dark)
- `GlassSurface` — containerization (dark/strong/ember flags)
- `Shell`, `AmbientField`, `Measure` — immersive layout (not used in per-client header)
- `Eyebrow` — mono label, tone-aware (amber/muted/onDark)

**Already converted (R1/R2):**
- `FluentScope` (components/redesign/themed-fluent.tsx) — wraps chrome regions in FluentProvider
- `ThemeToggle` — icon-only, light/dark toggle (part of staff shell sidebar)
- `StaffShell` (components/redesign/shell/staff-shell.tsx) — the outer chrome (sidebar + header bar)

---

### (3) Staff/Admin Write Paths (Server Actions & RPCs)

**Client management (admin only):**
- `createClientAction(input: ClientCreateValues)` (lib/actions/clients.ts:31) — creates client, optionally provisions portal user via Supabase invite
- `updateClientAction(input: ClientUpdateValues)` (lib/actions/clients.ts:134) — updates editable fields (name, industry, program, status, website_url, start_date, service_type, investment, comms_channel, internal_notes)
  - Called from ClientEditForm (components/clients/client-edit-form.tsx)
  - Result: toast notification, router.refresh()

**Preserved verbatim — DO NOT modify:**
- All Supabase query logic (client_assignments, RLS-scoped reads)
- Audit logging: `logAudit(actorId, action, entity, entityId, clientId, metadata)`
- Team member assignment queries (no UI changes to assignment logic)

---

### (4) Invariants & Guard Touchpoints

**Auth invariants:**
- `requireClientAccess(clientId)` on layout — admin OR assigned team member (RLS enforces)
- `requireRole(["admin"])` on settings tab & client creation
- Settings tab/gear icon: **admin-only visibility** (line 66-73, layout.tsx)

**Client type branching:**
- `isProject = client.client_type === "project"` (line 34, layout.tsx)
- Controls badge (Project vs program name), day counter, and tab list
- **Invariant:** Projects show no "Day N of 90" label (line 37)

**Status visibility invariants:**
- ClientTabs always visible (no gating per status)
- Settings link gated by `isAdmin` (lines 66-73)
- Soft-delete only: status → Paused/Churned (no hard delete in v1) — recorded in ClientEditForm description

**No destructive guards in the header/chrome itself** — all destructive ops (client hard-delete, last-admin blocks, etc.) live in settings or admin pages (out of scope).

---

### (5) Glass & Container Width Plan

**Glass application (Warm Obsidian ember-glass):**
- **Header + tabs chrome:** SOLID (NOT glass) — per spec, glass only on chrome surfaces when they're overlays/summaries; the per-client header is the workspace chrome, not a floating summary
  - Border + shadow per `rd-solid` / `rd-solid--dark` (from redesign.css)
  - Background: #ffffff / #1c1813 (opaque)
  - No backdrop-filter
- **Alternative interpretation (if "chrome" = glass):** Apply `GlassSurface` with `dark={isDarkMode}` (no strong/ember needed; these are utilities-only)

**Container width:**
- Uses `WorkspaceContainer` (components/layout/workspace-container.tsx) which delegates to `PageContainer` with width="standard" (1200px) or "wide" (1440px for Metrics tab only)
- Header does NOT change width; it sits inside the container and inherits
- No glass on the page body; bodies remain solid Tailwind (old R0/R1 screens)

---

### (6) Conversion Checklist

**Phase 1: Staff Chrome Layout & Header**
- [ ] Import `FluentScope` & `useRedesignMode` in layout
- [ ] Wrap the entire layout chrome (PersonAvatar + h1 + Badge + StatusChip + gear + ClientTabs) in a `<FluentScope>`
- [ ] Replace `PersonAvatar` with Fluent Avatar or a redesign-aware wrapper (preserve square + logo_url behavior)
- [ ] Replace `Badge` (amber/secondary variants) with Fluent Badge or redesign Badge alternative
- [ ] Replace `StatusChip` with a redesign-aware StatusChip (use same color recipes, apply as inline styles or Fluent Badge)
- [ ] Replace the settings gear button with Fluent IconButton (MoreHorizontal icon)
- [ ] Apply solid background + border (rd-solid / rd-solid--dark) to the header container
- [ ] Check reduced-motion & reduced-transparency a11y fallbacks (CSS handles this; verify no Fluent style overrides)

**Phase 2: ClientTabs Component**
- [ ] Convert ClientTabs to use FluentScope if not already wrapped
- [ ] Replace Radix DropdownMenu with Fluent Menu (already used in staff-shell.tsx — see MenuTrigger, MenuPopover, MenuItem pattern)
- [ ] Replace the animated underline (motion/layout) with Fluent Underline or a redesign motion variant (preserve the `layoutId="workspace-tab-underline"` spring behavior)
- [ ] Apply solid styling to tab nav (border-b + background)
- [ ] Preserve tab branching logic (isProject → primary tabs, client_type prop)

**Phase 3: Integration Testing**
- [ ] Verify admin sees settings gear; team members don't
- [ ] Verify project vs program tab lists branch correctly
- [ ] Verify "Day N of 90" appears only for programs, not projects
- [ ] Dark mode toggle in sidebar switches chrome theme
- [ ] Reduced-motion: springs → instant jumps (verified in CSS)
- [ ] Reduced-transparency: glass degrades to opaque solid

**Phase 4: Downstream**
- [ ] Do NOT modify page bodies (children) — they convert in R2 Wave N
- [ ] Do NOT touch server actions or Supabase queries
- [ ] Do NOT modify WorkspaceContainer (container width logic is correct)

---

### File Path References

- **Layout:** `D:\Claude Code\fourpielabs-portal\app\(portal)\clients\[clientId]\layout.tsx:14-84`
- **Tabs:** `D:\Claude Code\fourpielabs-portal\components\clients\client-tabs.tsx:1-114`
- **Guards:** `D:\Claude Code\fourpielabs-portal\lib\auth\guards.ts:88-103` (requireClientAccess)
- **Redesign surfaces:** `D:\Claude Code\fourpielabs-portal\components\redesign\ui\surfaces.tsx:18-129`
- **Redesign CSS:** `D:\Claude Code\fourpielabs-portal\app\(redesign)\redesign.css:50-95` (glass & solid classes)
- **Fluent theming:** `D:\Claude Code\fourpielabs-portal\components\redesign\themed-fluent.tsx:66-85` (FluentScope)
- **Staff shell reference:** `D:\Claude Code\fourpielabs-portal\components\redesign\shell\staff-shell.tsx:72-105` (ClientSwitcher pattern, Menu usage)
- **WorkspaceContainer:** `D:\Claude Code\fourpielabs-portal\components\layout\workspace-container.tsx:13-20`


---

# Scope: staff-tasks-timer

Perfect. I now have a complete understanding. Let me generate the comprehensive scope report:

## STAFF-TASKS-TIMER Scope Analysis

### (1) Route / Page Files + Auth Guards + Data Fetched

**Server-side page:**
- **D:\Claude Code\fourpielabs-portal\app\(portal)\clients\[clientId]\tasks\page.tsx** (lines 1–104)
  - **Auth guard:** `requireClientAccess(clientId)` (line 13) — admin or assigned team only; clients cannot access
  - **Data fetched:**
    - Tasks: SELECT `id, title, description, status, assignee_id, due_date, visible_to_client, source_message_id, created_by, created_at` (lines 19–21)
    - Task checklist items: SELECT `id, task_id, title, is_done, sort_order` (lines 35–37)
    - **Time entries (staff-only):** SELECT `id, task_id, user_id, started_at, ended_at` (lines 50–52) — RLS blocks clients; data is staff-consumed only
    - Assignable members (circle): staff-assigned team + admins + client portal users (line 24)
    - Source message thread_type (via admin client): resolves internal vs. client_shared (lines 72–79)

**Client-side layout:**
- **D:\Claude Code\fourpielabs-portal\app\(portal)\clients\[clientId]\layout.tsx** (lines 1–84)
  - **Auth guard:** `requireClientAccess(clientId)` (line 23)
  - **Role check:** `profile.role === "admin"` gated settings link (line 66)
  - Wraps all task/detail surfaces in `WorkspaceContainer` (line 46)

---

### (2) Render Component Tree + R0/R1/R2 Coverage

**Staff task list (card grid, NOT redesigned yet):**
- **D:\Claude Code\fourpielabs-portal\components\tasks\staff-tasks-manager.tsx** (lines 48–239)
  - Renders `<ul className="space-y-3">` with `.rounded-2xl .border .bg-surface` cards (lines 125–220)
  - Uses old shadcn/Tailwind: `StatusChip`, `Button`, `Select`, `EmptyState`, `Link`, `AlertDialog`
  - **Optimistic state:** local `[tasks, setTasks]` + server prop re-sync (lines 63–70)
  - Opens detail dialog via `?task={id}` query param (lines 74–75, 232–235)
  - **Quick actions:** status dropdown (Select) + edit (TaskFormDialog) + delete (AlertDialog) (lines 169–214)

**Staff task detail (dialog surface):**
- **D:\Claude Code\fourpielabs-portal\components\tasks\task-detail-dialog.tsx** (lines 70–279)
  - **Mode-aware rendering:** `isStaff` branch renders full control (status Select, assignee, due, visibility Toggle); client branch renders read-only (lines 156–169, 191–217, 220–230)
  - Uses old shadcn/Tailwind: `Dialog`, `Input`, `Textarea`, `Switch`, `DatePicker`, `Select`, `StatusChip`, `Button`
  - **Subtask slot:** `<TaskChecklist>` (line 254) — shared for both roles, uses SECURITY DEFINER RPCs for clients
  - **Time tracking slot (staff-only):** `<TaskTimer>` (lines 261–267) — conditionally rendered only if `isStaff && clientId && currentUserId`

**Task timer component (staff-only):**
- **D:\Claude Code\fourpielabs-portal\components\tasks\task-timer.tsx** (lines 45–225)
  - Uses old shadcn/Tailwind: `Button`, `Input`, `formatDuration`, `formatDateTime`, Lucide icons
  - **Live clock:** `useEffect` interval to tick running entries (lines 75–80)
  - **One-running-entry rule:** detects `myRunning = entries.find(e => !e.ended_at && e.user_id === currentUserId)` (line 69)
  - **Control flow:**
    - If running: "Stop" button (plain stop, leaves in_progress) + "Stop & complete" button (→ done) (lines 122–127)
    - If not running: "Start timer" button (→ in_progress via RPC) (lines 131–133)
  - **Entry list:** sorted reverse-chronological, mine-only edit/delete (lines 141–214)
  - **Edit inline:** datetime-local inputs for start/end, saves via `editTimeEntryAction` (lines 147–179)

**Task checklist (both roles, detail slot):**
- **D:\Claude Code\fourpielabs-portal\components\tasks\task-checklist.tsx** (lines 65–262)
  - Shared rendering: Checkbox, Input, Button, animatePresence
  - **Staff path:** direct table writes via `staffAddChecklistItemAction`, `staffToggleChecklistItemAction`, etc. (lines 102–150)
  - **Client path:** SECURITY DEFINER RPCs (same function names minus "staff" prefix)
  - **Invariant:** inherited visibility — client only reaches items on tasks already in their RLS list

**Task form dialog (create/edit):**
- **D:\Claude Code\fourpielabs-portal\components\tasks\task-form-dialog.tsx** (lines 50–150+)
  - Uses old shadcn/Tailwind: `Dialog`, `Input`, `Textarea`, `Label`, `Select`, `Switch`, `DatePicker`, `Button`
  - RHF + Zod validation against `taskStaffSchema` (lines 72–83)
  - Calls `staffCreateTaskAction` or `staffUpdateTaskAction` (lines 88–89)

**R0/R1/R2 primitives already covering:**
- **D:\Claude Code\fourpielabs-portal\components\redesign\ui\index.ts** exports:
  - Custom: `Button`, `EmberButton`, `Segmented` (lines 13–14)
  - Fluent v9 forms: `Input`, `Textarea`, `Label`, `Field`, `Checkbox`, `Switch`, `Select`, `Combobox`, `Radio`, `RadioGroup`, `SpinButton` (lines 17–30)
  - Fluent overlays: `Dialog`, `DialogTrigger`, `DialogSurface`, etc.; `Menu`; `Popover`; `Tooltip` (lines 34–60)
  - Fluent data: `Card`, `Table`, `Tab`, `Avatar`, `Badge`, `ProgressBar`, `Spinner` (lines 62–86)
  - R0 surfaces: `Shell`, `AmbientField`, `Measure`, `Eyebrow`, `GlassSurface` (line 116)
  - Data-ui: `StatusPill`, `DeltaChip`, `Progress`, `humanize` (line 117)

---

### (3) Staff Task Write Paths (Server Actions / RPCs)

All in **D:\Claude Code\fourpielabs-portal\lib\actions\tasks.ts** (lines 1–177):

**Task lifecycle:**
- `staffCreateTaskAction(clientId, input: TaskStaffValues)` (lines 48–92)
  - Validates input against `taskStaffSchema` (line 53)
  - Checks assignee is in circle (lines 57–60)
  - **Resolves visibility:** if source is internal-threaded, forces `visible_to_client = false` (lines 61–62) — internal-thread boundary
  - Direct table insert under `tasks_admin_all` / `tasks_team_all` (lines 65–79)
  - Logs: `task.created` (line 84)

- `staffUpdateTaskAction(clientId, id, input: TaskStaffValues)` (lines 94–134)
  - Re-validates assignee to circle (lines 104–107)
  - Direct table update: title, description, status, assignee, due, visibility (lines 110–121)
  - Logs: `task.updated` (line 126)

- `staffSetTaskStatusAction(clientId, id, status: "todo" | "in_progress" | "done")` (lines 136–160)
  - Quick status-only update (lines 143–147)
  - Logs: `task.status_changed` (line 151)

- `staffDeleteTaskAction(clientId, id)` (lines 162–177)
  - Hard delete; no soft delete (line 165)
  - Logs: `task.deleted` (line 168)

**Time entry lifecycle** in **D:\Claude Code\fourpielabs-portal\lib\actions\time.ts** (lines 1–79):

All gate with `requireClientAccess(clientId)` (staff-only):

- `startTimerAction(clientId, taskId)` (lines 21–29)
  - Calls RPC `start_timer(p_task_id)` which:
    - Creates a time_entry with `user_id = auth.uid()`, `started_at = now()`, `ended_at = null`
    - Sets task status to `in_progress` (atomicity via RPC)
    - Enforces one-running-entry rule (RPC validates)
  - Logs: `task.time_started`

- `stopTimerAction(clientId, entryId, complete: boolean)` (lines 31–49)
  - Calls RPC `stop_timer(p_entry_id, p_complete)` which:
    - Sets `ended_at = now()`
    - If `complete = true`: also sets task status to `done`
    - If `complete = false`: leaves task status untouched (in_progress)
  - Logs: `task.time_completed` or `task.time_stopped` (line 41–42)

- `editTimeEntryAction(clientId, entryId, startedAt, endedAt)` (lines 51–68)
  - Calls RPC `edit_time_entry(p_entry_id, p_started_at, p_ended_at)` (validates own-entry + assigned staff)
  - Logs: `task.time_edited`

- `deleteTimeEntryAction(clientId, entryId)` (lines 70–78)
  - Calls RPC `delete_time_entry(p_entry_id)` (own-entry validation)
  - Logs: `task.time_deleted`

All time actions revalidate `/clients/{clientId}/tasks` (lines 27, 47, 65, 75).

---

### (4) Invariants + Guard Touchpoints

**Internal-thread boundary:**
- **D:\Claude Code\fourpielabs-portal\lib\actions\tasks.ts:45** — if source_message_id exists and thread_type = "internal", task is forced `visible_to_client = false`
- Prevents internal staff notes from leaking to client view

**Timer state model (strictest invariant):**
- **Start:** entry created with `ended_at = null`; task → `in_progress` (atomic via RPC)
- **Plain stop:** `ended_at = now()`; task stays `in_progress` (NOT completed)
- **Stop & complete:** `ended_at = now()`; task → `done` (atomic via RPC)
- **One-running-entry rule:** enforced in RPC (`start_timer` fails if any running entry exists for the same user/task or same task globally — check DB schema for exact rule)

**Staff-only access:**
- Time entry reads: RLS blocks clients entirely (no policy; they get `null`) (comment at D:\Claude Code\fourpielabs-portal\app\(portal)\clients\[clientId]\tasks\page.tsx:46–47)
- Timer UI: conditionally rendered only if `isStaff && clientId && currentUserId` (D:\Claude Code\fourpielabs-portal\components\tasks\task-detail-dialog.tsx:261)
- All time actions: gate with `requireClientAccess(clientId)` (D:\Claude Code\fourpielabs-portal\lib\actions\time.ts:22, 36, 52, 71)
- My-entry edit/delete: client-side UI only renders for `mine = e.user_id === currentUserId` (D:\Claude Code\fourpielabs-portal\components\tasks\task-timer.tsx:142, 192–214); RPC re-validates server-side

**Task visibility gating:**
- Client detail: can only open a task in their RLS list (own client_id + visible_to_client = true); the page query scope enforces it
- Source link: audience-correct deep-link (staff → `/clients/{clientId}/messages?tab=internal` if internal; client → `/messages` to their single shared thread) (D:\Claude Code\fourpielabs-portal\components\tasks\task-detail-dialog.tsx:115–119)

**Assignee validation:**
- **D:\Claude Code\fourpielabs-portal\lib\tasks.ts:67–71** — `isCircleMember(clientId, userId)` checks user is in client's circle (client users + assigned staff + admins)
- Called by create/update before any write (D:\Claude Code\fourpielabs-portal\lib\actions\tasks.ts:57–60, 104–107)

**Status locks:**
- Client cannot write status (no `update_task_status` RPC; was dropped per comment at D:\Claude Code\fourpielabs-portal\lib\schemas.ts:107–108)
- Client can only edit title + description (TaskClientUpdateSchema line 124–128)
- Staff has full control (TaskStaffSchema line 133–141)

---

### (5) Glass Plan

**Timeline context:** The portal uses the new Fluent v9 + Warm Obsidian theme with ember-glass effects. Glass rule (from context): glass ONLY on chrome/overlays/KPI-with-scrim; tables/grids/forms = SOLID.

**Staff tasks—timer conversion targets:**

| Component | Current | Target | Glass? | Notes |
|-----------|---------|--------|--------|-------|
| **StaffTasksManager (card grid)** | Tailwind `.bg-surface` cards | Fluent v9 Card or solid Measure | SOLID | Dense task list; should be opaque/solid. Use `rd-solid` class or bare Measure. |
| **Task detail dialog** | Old Dialog | Fluent DialogSurface in FluentScope | SOLID | Form controls stay solid (Fluent default). The dialog chrome itself: check if glass-scrim per modal depth. |
| **Timer section (inside detail)** | Old Tailwind amber banner | Fluent solid (or glass-with-scrim if summary KPI style) | Consider GLASS for running-entry highlight | Running timer banner (amber pulse) could be glass-strong-ember for that KPI feel; otherwise SOLID. |
| **Entry list** | Old Tailwind hover-bg-surface-2 | Fluent Table or `<ul>` with solid rows | SOLID | Dense data; solid. |
| **Timer controls (Play/Square/Check buttons)** | Old Button | Fluent Button (re-exported) | Already OK | Fluent Button inside the dialog is solid by default. |
| **Time entry edit inputs** | Old Input | Fluent Input | SOLID | Forms = solid. |
| **Checklist (detail slot)** | Old Tailwind | Fluent Checkbox + Input + Button | SOLID | Checklist is data-entry; stays solid. |

**Container width rules:**
- Staff tasks page: metrics/summary = WIDE; detail dialog = standard (inherits page layout)
- Detail dialog itself: standard measure (no width override needed; Dialog stays centered)

**Color tokens (mode-aware):**
- Use `useRedesignMode()` to detect light/dark (D:\Claude Code\fourpielabs-portal\components\redesign\client\task-board.tsx:29)
- Extract mode-aware tokens from `tokens` (Fluent exports) if needed for dynamic theming (line 31: `tokens.colorNeutralForeground1/2/3`)
- Amber interactive: `#b45309` (light) / `#fcd34d` (dark) — already in Eyebrow (D:\Claude Code\fourpielabs-portal\components\redesign\ui\surfaces.tsx:81)

**Conversion approach:**
- **Wrap page body** in `<FluentScope>` + `<AmbientField>` + `<Measure>` (following `ClientPageFrame` pattern from D:\Claude Code\fourpielabs-portal\components\redesign\client\page-frame.tsx)
- **Replace old Dialog** with Fluent `DialogSurface` inside the scope
- **Replace old Button/Input/Select/Label** with Fluent re-exports from `@/components/redesign/ui` (they're already wrapped and themed)
- **Replace card styling** with Fluent `Card` or solid `.rd-solid` class
- **Keep glass off:** tables, lists, forms, and detail content stay opaque/SOLID
- **Optional glass:** running-timer banner (`bg-amber-50` amber pulse) could upgrade to `<GlassSurface dark strong ember>` for polish, but not required

---

### (6) Conversion Checklist

**Phase 1: Component imports + scope setup**
- [ ] Verify `@/components/redesign/ui` exports all primitives (Button, Input, Textarea, Label, Select, Combobox, Switch, DatePicker, Dialog, Card, Table, etc.)
- [ ] Confirm `FluentScope`, `AmbientField`, `Measure`, `Eyebrow` are available and exported
- [ ] Check `useRedesignMode()` and `tokens` export working

**Phase 2: Page-level wrapper (optional, or defer to detail dialog)**
- [ ] If full page redesign: wrap StaffTasksManager in `<FluentScope><AmbientField /><Measure>...` (similar to ClientPageFrame)
- [ ] If partial: leave page body as-is, just convert the detail dialog

**Phase 3: StaffTasksManager (card grid) — Tailwind → Fluent**
- [ ] Replace `.rounded-2xl .border .bg-surface` cards with Fluent `<Card>` or `.rd-solid` divs
- [ ] Replace `<StatusChip>` with Fluent-style status or R0 `<StatusPill>`
- [ ] Replace old `<Button>` with Fluent Button from `@/components/redesign/ui`
- [ ] Replace old `<Select>` with Fluent Select (re-exported)
- [ ] Replace old `<AlertDialog>` with Fluent Dialog / DialogSurface
- [ ] Keep optimistic state logic unchanged

**Phase 4: TaskDetailDialog (form + slots)**
- [ ] Wrap content in Fluent `DialogSurface` (inside FluentScope from parent or self-wrap)
- [ ] Replace all old shadcn controls (Input, Textarea, Label, Select, Switch, DatePicker, Button) with Fluent re-exports
- [ ] Keep `isStaff` branch logic unchanged
- [ ] Keep `currentUserId`, `clientId`, `role` props unchanged
- [ ] Verify TaskChecklist and TaskTimer slots still render correctly (they import their own primitives)

**Phase 5: TaskTimer (staff-only, detail slot)**
- [ ] Replace old `<Button>` with Fluent Button
- [ ] Replace old `<Input>` with Fluent Input
- [ ] Keep clock logic, entry sorting, one-running-entry rule UNCHANGED
- [ ] Consider: amber running-entry banner could use `<GlassSurface dark strong ember>` for polish (optional)
- [ ] Replace Lucide icons stay the same (lucide-react is independent)
- [ ] Keep all RPC calls and `startTimerAction` / `stopTimerAction` / `editTimeEntryAction` / `deleteTimeEntryAction` UNCHANGED

**Phase 6: Test invariants**
- [ ] Timer start: entry created, task → in_progress (via RPC)
- [ ] Plain stop: `ended_at` set, task stays in_progress
- [ ] Stop & complete: `ended_at` set, task → done
- [ ] One-running-entry: second start fails gracefully
- [ ] My-entry only edit/delete: non-mine entries locked
- [ ] Internal task: forced visible_to_client = false
- [ ] Client cannot see timer UI or time entries (RLS + isStaff gate)
- [ ] Status locked: client cannot write status

**Phase 7: Polish (optional)**
- [ ] Add `Eyebrow` labels (uppercase mono) to section headers (Timer, Subtasks, etc.)
- [ ] Mode-aware color tokens where appropriate (running timer could be amber on both light/dark)
- [ ] Test reduced-motion (TaskTimer keeps existing motion; TaskChecklist has AnimatePresence—check if re-enabled)

---

## File Path Summary (Quick Reference)

**Pages & Layouts:**
- D:\Claude Code\fourpielabs-portal\app\(portal)\clients\[clientId]\tasks\page.tsx (server, data fetch, auth guard)
- D:\Claude Code\fourpielabs-portal\app\(portal)\clients\[clientId]\layout.tsx (workspace wrapper, auth guard)

**Components (to reskin):**
- D:\Claude Code\fourpielabs-portal\components\tasks\staff-tasks-manager.tsx (card grid, NOT redesigned yet)
- D:\Claude Code\fourpielabs-portal\components\tasks\task-detail-dialog.tsx (detail form + slots, Tailwind)
- D:\Claude Code\fourpielabs-portal\components\tasks\task-timer.tsx (staff-only timer, Tailwind)
- D:\Claude Code\fourpielabs-portal\components\tasks\task-checklist.tsx (subtask list, shared, Tailwind)
- D:\Claude Code\fourpielabs-portal\components\tasks\task-form-dialog.tsx (create/edit, Tailwind)

**Server Actions & RPCs (PRESERVE):**
- D:\Claude Code\fourpielabs-portal\lib\actions\tasks.ts (staffCreateTaskAction, staffUpdateTaskAction, staffSetTaskStatusAction, staffDeleteTaskAction)
- D:\Claude Code\fourpielabs-portal\lib\actions\time.ts (startTimerAction, stopTimerAction, editTimeEntryAction, deleteTimeEntryAction) + RPC calls

**Schemas & Types (PRESERVE):**
- D:\Claude Code\fourpielabs-portal\lib\schemas.ts (taskStaffSchema, TaskStaffValues)
- D:\Claude Code\fourpielabs-portal\lib\tasks.ts (TimeEntry, TaskChecklistItem, TaskMember, getAssignableMembers, isCircleMember)
- D:\Claude Code\fourpielabs-portal\lib\constants.ts (TASK_STATUSES)

**Design System (source for conversion):**
- D:\Claude Code\fourpielabs-portal\components\redesign\ui\index.ts (Fluent + custom re-exports)
- D:\Claude Code\fourpielabs-portal\components\redesign\ui\surfaces.tsx (Shell, AmbientField, Measure, Eyebrow, GlassSurface)
- D:\Claude Code\fourpielabs-portal\components\redesign\ui\button.tsx (Button, EmberButton)
- D:\Claude Code\fourpielabs-portal\components\redesign\themed-fluent.tsx (FluentScope, useRedesignMode, tokens)
- D:\Claude Code\fourpielabs-portal\components\redesign\data-ui.tsx (StatusPill, DeltaChip, humanize)
- D:\Claude Code\fourpielabs-portal\components\redesign\client\page-frame.tsx (wrapper pattern reference)

---

## Critical Notes

1. **TIMER STATE MODEL (strictest invariant):**
   - Start → in_progress (RPC atomic)
   - Plain Stop → `ended_at` set, status stays in_progress
   - Stop & complete → `ended_at` set, status → done (RPC atomic)
   - One-running-entry enforced by RPC

2. **INTERNAL-THREAD BOUNDARY:**
   - If source_message_id has thread_type = "internal", task forced visible_to_client = false (D:\Claude Code\fourpielabs-portal\lib\actions\tasks.ts:45)

3. **STAFF-ONLY TIME ENTRIES:**
   - No RLS policy for clients (D:\Claude Code\fourpielabs-portal\app\(portal)\clients\[clientId]\tasks\page.tsx:46–47)
   - Timer UI gated by `isStaff && clientId && currentUserId` (D:\Claude Code\fourpielabs-portal\components\tasks\task-detail-dialog.tsx:261)
   - All time actions gate with `requireClientAccess` (staff-only RPC enforcement + own-entry validation)

4. **STATUS LOCK:**
   - Clients cannot write status (RPC dropped per schema comment)
   - Client detail renders status as read-only StatusChip (D:\Claude Code\fourpielabs-portal\components\tasks\task-detail-dialog.tsx:168)
   - Staff detail renders status as Select control

5. **GLASS PLAN:**
   - Card grid, detail form, timer entry list, checklist = SOLID
   - Running-timer banner could be glass-strong-ember (optional polish)
   - Keep all chrome/modals/overlays glassmorphic per existing Fluent theme


---

# Scope: staff-dual-thread

Perfect. Now I have all the information needed. Let me compile the final report:

---

## Staff Dual-Thread Messages Page — Scope Analysis

### 1. ROUTE & AUTH GUARD

**File:** `app/(portal)/clients/[clientId]/messages/page.tsx:10–78`

**Auth guard (line 19):** `requireClientAccess(clientId)` 
- Admits admins (all clients) + team members assigned to the client (via `client_assignments` table)
- Redirects unassigned team / clients to `/clients`

**Data fetched (lines 23–28):**
```typescript
supabase.from("threads").select("id, type").eq("client_id", clientId)
  // Returns: shared thread (type='client_shared') + internal thread (type='internal')

getAssignableMembers(clientId)  // task mention circle, scope not critical
```

**Query param:** `?tab=internal` toggles between shared (default) and internal threads

---

### 2. RENDER TREE & COMPONENTS TO RE-SKIN

#### Page Component

**Root wrapper:** Currently unwrapped. **Conversion target:** Wrap in `ClientPageFrame` (from `@/components/redesign/client/page-frame.tsx`)
- Provides `FluentScope` (theme context) + `AmbientField` (full-bleed warm field) + `Measure` (readable width)
- Set `width="standard"` (default)

**Tab switcher (lines 47–59):**
- Two pill-button tabs: "Client thread" (Eye icon, emerald) + "Internal" (Lock icon, amber)
- Currently uses `Link` + local `tabCls()` Tailwind styling
- **Conversion:** Replace with `Segmented` control from `@/components/redesign/ui` (imported from Fluent v9)
  - Options: `[{ value: "shared", label: <><Eye /> Client thread</> }, { value: "internal", label: <><Lock /> Internal</> }]`
  - Active segment gets Fluent amber brand color (theme-aware)

**Audience indicator banner (lines 55–59):**
- Text: "Staff-only — the client cannot see this thread." (internal) or "Shared with the client." (shared)
- Currently plain text + icon
- **Conversion:** Stays as muted label next to tabs OR integrate into the Segmented `onChange` callback to show below tabs (less prominent)

#### Conversation Component

**File:** `components/messaging/conversation.tsx:63–670`

**Current rendering:**
- Unwrapped, uses Tailwind borders + surfaces + amber accents
- Who-can-see indicator banner (lines 361–363, shown when `!isClient`) uses Tailwind pills
- Message list (lines 404–509) shows messages in bubbles (amber for author, neutral for others)
- Composer (lines 514–667) has labeled action buttons (emoji, bold, italic, attach, task)

**Glass strategy:**
- Message list background: currently `internal && "bg-amber-50/30"` (subtle internal indicator)
  - Keep as is (SOLID background, not glass)
- Composer section: SOLID form surface (no glass on input areas)
- WHO-CAN-SEE banner: **GLASS with scrim** (chrome notification, high visual priority on both thread types)
  - Use `GlassSurface dark={false} strong={true}` to get the scrim level + ember glow if it's internal

**Component tree to convert:**
- Wrap entire `Conversation` output in a container that layers:
  1. `AmbientField` (if not already in parent page frame)
  2. `GlassSurface` for the who-can-see banner (SOLID, strong, no ember)
  3. SOLID surfaces for search box, message list, composer (Fluent defaults)

**Key internal pieces (no UI change, preserve verbatim):**
- Search: realtime input → `searchThreadMessagesAction()` (lines 219–230, RLS-gated)
- Message actions: edit/delete (lines 429–465) via `editMessageAction()` / `deleteMessageAction()`
- Mentions: `getThreadParticipantsAction()` populates the @picker (lines 214–216, RLS-scoped)
- Realtime sync: `postgres_changes` listener (lines 273–301) calls `syncNew()` / `fullRefetch()` (RLS-scoped)

---

### 3. STAFF WRITE PATHS (Server Actions / RPCs)

**All writes preserve DEFINER-enforced boundaries. DO NOT modify.**

**Post message:**
- Server action: `postMessageAction(threadId, body, mentionedIds, attachmentPath, attachmentName)`
  - **File:** `lib/actions/messages.ts:42–118`
  - **RPC:** `public.post_message(p_thread_id, p_body, p_attachment_path, p_attachment_name)` (20260615224421)
  - Guard: `app.can_access_thread()` re-checks thread access (admin/team/client-shared-only)
  - Recipients computed from `msg.thread_type` (returned by RPC), never from input
  - Mentions validated: internal → staff ONLY (client ids filtered out via `messageRecipientIds()`)

**Edit message:**
- Server action: `editMessageAction(messageId, body)`
  - **File:** `lib/actions/messages.ts:264–270`
  - **RPC:** `public.edit_message(p_message_id, p_body)` (20260616191150)
  - Guard: author-only + `app.can_access_thread()` + soft-delete check

**Delete message (soft):**
- Server action: `deleteMessageAction(messageId)`
  - **File:** `lib/actions/messages.ts:272–278`
  - **RPC:** `public.delete_message(p_message_id)` (20260616191150)
  - Guard: author-only + `app.can_access_thread()` + deleted_at null check
  - Idempotent: re-deleting returns the row unchanged

**Mark thread viewed:**
- Server action: `markThreadViewedAction(threadId, notifLink)`
  - **File:** `lib/actions/messages.ts:244–255`
  - **RPC:** `public.mark_thread_read(p_thread_id)`
  - Updates `thread_reads(thread_id, user_id, last_read_at)` + clears message notifications for that thread

**Attachment upload:**
- Server action: `uploadMessageAttachmentAction(threadId, formData)`
  - **File:** `lib/actions/message-attachments.ts` (not shown, but storage-scoped)
  - Writes to `message_attachments` bucket (RLS-gated)

---

### 4. INTERNAL-THREAD BOUNDARY (CRITICAL)

**The boundary is **six-layered**; the conversion must NOT widen any layer:**

1. **RLS at SELECT (messages policy):**
   - Client policy: `client_id = app.my_client_id() and thread_type = 'client_shared' and deleted_at is null`
   - Team/admin: `app.is_assigned(client_id) and deleted_at is null` (both types allowed)
   - Internal rows never visible to clients at DB

2. **RLS at SELECT (threads policy):**
   - Client policy: `client_id = app.my_client_id() and type = 'client_shared'`
   - Internal thread is invisible to clients; no client write policy

3. **Server action: getThreadMessagesAction() (line 168–180)**
   - Calls Supabase user client (RLS-enforced)
   - Client calling this for an internal thread id → empty result (RLS denies)
   - Staff calling → full internal thread (they are assigned)

4. **Server action: getThreadParticipantsAction() (line 130–159)**
   - Reads thread to determine type
   - If `type === 'internal'`: participant list = staff ONLY (line 143)
   - Client can never be offered into @mentions for internal thread

5. **Server action: postMessageAction() recipients (line 65–75)**
   - Threads back from RPC (`msg.thread_type`) — NEVER from input
   - `messageRecipientIds()` decision (lib/notification-recipients.ts:18–21):
     - `internal || (author === client)` → staff ONLY
   - Author excluded always

6. **Realtime: postgres_changes (20260616002055.sql)**
   - RLS honored per subscriber
   - Client subscribing to internal thread → no events received
   - Staff → all messages for assigned clients

**Page must preserve this:**
- Server-side: `requireClientAccess()` (staff/admin only) + RLS on all queries
- Client-side: never issue `getThreadMessagesAction()` without the real `threadId` (URL param, not user input)
- Realtime: no subscription filtering override — trust RLS
- Tab switching: both threads are fetched server-side in `StaffMessagesPage` (line 23–28); client cannot access internal even if params are spoofed (RLS denies)

---

### 5. GLASS PLAN & CONTAINER WIDTH

**Messages page uses standard width (metrics would use `wide`).**

**Glass surfaces:**
- **Who-can-see banner** (around line 361–363 in Conversation):
  - **GLASS with scrim** (`GlassSurface strong={true} dark={false}`)
  - High visual priority — staff must know which thread they're composing into
  - Emoji, amber (internal), emerald (shared)

**Solid surfaces:**
- Search bar (line 369)
- Message list (line 404)
- Composer form (lines 514–667)
- Action buttons (emoji picker, markdown, attach, task)
- Message bubbles

**Background:**
- Page: `ClientPageFrame` wraps, provides `AmbientField` (full-bleed warm field)
- Message list bg: current `internal && "bg-amber-50/30"` is fine (subtle, SOLID)

---

### 6. CONVERSION CHECKLIST

- [ ] Wrap page body in `ClientPageFrame` (width="standard")
- [ ] Replace tab pills with `Segmented` control (Fluent v9, amber active state)
- [ ] Convert who-can-see banner to `GlassSurface strong={true}` (SOLID fill, high contrast)
- [ ] Ensure Conversation component renders inside `FluentScope` (from ClientPageFrame)
- [ ] Update Conversation's composer action buttons to use Fluent `Button` (from redesign/ui)
- [ ] Verify message bubbles render on solid background (no glass)
- [ ] Test realtime: RLS should still gate internal thread (no UI change needed, DB boundary holds)
- [ ] Test @mentions: internal thread should show staff only (RLS on getThreadParticipantsAction enforces)
- [ ] Smoke test: client user role cannot reach `/clients/[id]/messages` (requireClientAccess redirects)
- [ ] Smoke test: spoofing `?tab=internal` as non-staff → RLS denies thread fetch (empty conversation)
- [ ] Verify edit/delete actions preserve author-only + can_access_thread guards (RPC, no change)

---

## File Reference Summary

| File | Role |
|------|------|
| `app/(portal)/clients/[clientId]/messages/page.tsx` | Page root; tab switcher; thread fetch & guard |
| `app/(portal)/clients/[clientId]/layout.tsx` | Workspace layout (no change for messages scope) |
| `components/clients/client-tabs.tsx` | Client workspace nav tabs (no change) |
| `components/messaging/conversation.tsx` | Message list, composer, realtime sync, @mentions |
| `lib/actions/messages.ts` | postMessageAction, getThreadMessagesAction, getThreadParticipantsAction, editMessageAction, deleteMessageAction, markThreadViewedAction, searchThreadMessagesAction |
| `lib/notification-recipients.ts` | Internal thread → staff only; author excluded (pure logic) |
| `lib/auth/guards.ts` | requireClientAccess guard |
| `supabase/migrations/20260615224421_communication_4a.sql` | threads, messages, thread_reads tables + RLS + post_message RPC + mark_thread_read RPC + can_access_thread helper |
| `supabase/migrations/20260616191150_messaging_edit_soft_delete.sql` | edit_message + delete_message RPCs + soft-delete policy narrowing |
| `components/redesign/client/page-frame.tsx` | ClientPageFrame wrapper (target for page body) |
| `components/redesign/ui/` | Segmented, Button, EmberButton, GlassSurface, AmbientField, Measure, surfaces, themed-fluent |

---

**CRITICAL INVARIANTS TO PRESERVE:**
1. Internal thread **NEVER visible to clients** (RLS + server-side UI gate)
2. Thread fetch always **staff-gated** (requireClientAccess)
3. Participant list for internal = **staff only** (getThreadParticipantsAction logic)
4. Message recipients **derived from RPC return**, never input (postMessageAction)
5. Realtime **honors RLS per subscriber** (no override)


---

# Scope: staff-metrics

Perfect! Now I have all the context I need. Let me compile my findings into the scope document.

---

## STAFF-METRICS SCOPE ANALYSIS

### (1) Routes & Auth Guards

**Page route:** `D:\Claude Code\fourpielabs-portal\app\(portal)\clients\[clientId]\metrics\page.tsx:10-16`
- **Auth guard:** `requireClientAccess(clientId)` (line 16)
- **Data fetched:**
  - `metric_definitions` (id, key, label, unit, is_active, sort_order) where client_id=clientId; ordered by sort_order
  - `metric_entries` (definition_id, value_numeric, value_text) for current month period
  - `metric_entries` (definition_id, period, value_numeric, value_text) for last 12 months (historical)

**Server-side computed state:**
- `activeDefs`: filtered definitions where is_active=true; mapped to `ActiveDef[]` shape
- `initialValues`: month's current entries as string map (numeric→String, text→raw)
- `numericDefs` & `textDefs`: split by unit; sent to charts component
- `entryStatus`: per-month fill count vs active total; status = complete|in_progress|empty

---

### (2) Render Component Tree (to re-skin)

All components live in `components/metrics/`:

| Path | Role | Current State |
|------|------|---------------|
| `metrics-workspace.tsx:50` | **Root layout; mode toggle** — "Enter data" / "Client preview" split-view switch | Tailwind (old shadcn Button, SegmentedControl, Dialog) |
| `definitions-manager.tsx:168` | **Left sidebar** — metrics CRUD + reorder | Tailwind (Input, Label, Badge, Switch, Select, Dialog from old shadcn) |
| `monthly-entry-grid.tsx:31` | **Main grid** — table-like form (monthly entry per active def) | Tailwind (sticky header, sticky footer save bar, custom input styling) |
| `metrics-charts.tsx:45` | **Client preview surface** — numeric trend + "Month by month" table + "Highlights" text table | Tailwind (Select, Table from old shadcn) |
| `metrics-line-chart.tsx:19` | **Recharts wrapper** — lazy-loaded area+line chart | Recharts (chart code, not UI layer) |

**CSV import:** lives in a Dialog overlay inside metrics-workspace, opened as a variant of metrics-workspace on "Enter data" tab.

---

### (3) WRITE PATHS (Server Actions — Preserve Verbatim)

All in `lib\actions\metrics.ts`:

| Action | Signature | RLS Guard |
|--------|-----------|-----------|
| `createMetricDefinitionAction` | (clientId, values: MetricDefinitionValues) | `requireClientAccess` |
| `updateMetricDefinitionAction` | (clientId, id, values) | `requireClientAccess` + eq(id) + eq(client_id) |
| `setMetricDefinitionActiveAction` | (clientId, id, active: boolean) | `requireClientAccess` + toggle active flag |
| `moveMetricDefinitionAction` | (clientId, id, direction: "up"\|"down") | `requireClientAccess` + swap sort_order with neighbor |
| `getMonthEntriesAction` | (clientId, period: string) → {entries: {numeric, text}} | `requireClientAccess` + select by period |
| `saveMonthEntriesAction` | (clientId, period, values[]) → upsert | `requireClientAccess` + upsert on (definition_id, period) |
| `commitCsvAction` | (clientId, rows[]) → {committed, errors[]} | `requireClientAccess` + validate each row; upsert valid; return per-row errors |

All log audit trail via `logAudit()` with action names like `metric_definition.created`, `metric_entries.saved`, etc.
All revalidate paths: `/clients/${clientId}/metrics` and `/clients/${clientId}`.

---

### (4) INVARIANTS & GUARD TOUCHPOINTS

**Data invariants:**
- **Metric definitions** (client-owned):
  - Unique (key, client_id) constraint (code checks error.code === "23505"; ref line 52-53, 85-86)
  - sort_order swaps preserve ordering across all defs per client
  - is_active toggles; inactive defs don't appear in activeDefs filter (line 49)
  - Text metrics excluded from charts (line 70-71 separate textDefs)

- **Metric entries**:
  - Upsert on (definition_id, period) — re-entry updates, no duplicates (line 250)
  - Numeric parsing: empty string → null; NaN → null; valid → Number (lines 234-235, 307)
  - Text fields: store raw; null if empty
  - period normalized to YYYY-MM-01 (line 181)
  - created_by tracks actor (lines 242, 309)

**CSV import validation:**
- Per-row errors (bad key, bad period, non-numeric value for numeric metric)
- Valid rows committed; invalid rows skipped with user-facing error details (line 328)

**No destructive locks** (unlike projects/tasks):
- Definitions can be deactivated but not hard-deleted
- Re-activation allowed
- Entries editable/overwritable via upsert

---

### (5) GLASS & CONTAINER WIDTH RULES

**Container widths:**
- Metrics edit surface (definitions + grid + status): **standard** (1200px, line 105 lg:grid-cols[...])
- Client preview (charts): **standard** (wrapped in max-w-standard, line 128)
- CSV import dialog: no explicit width (Dialog + max-w-2xl, line 94); treated as overlay, not dense table

**Glass vs. Solid:**
- **Entry status sidebar** (right, line 113): solid (no glass class; rounded-2xl border bg-surface p-5 shadow-e2)
- **Definitions sidebar** (left, line 189): solid (rounded-2xl border bg-surface p-5 shadow-e2)
- **Monthly entry grid** (center, line 103): solid (border border-border bg-surface shadow-e2)
- **Client preview wrapper** (line 129): solid (rounded-2xl border border-border bg-surface p-5 sm:p-6)
- **CSV preview table** (inside dialog, line 169): solid (rounded-lg border)
- **Charts in preview** (line 135): solid table (rounded-2xl border border-border shadow-e2)

**Lesson:** Tables/grids/forms are SOLID throughout. No glass here (entry metrics are data, not chrome/KPI).

---

### (6) CONVERSION CHECKLIST

**UI Layer (Fluent + Warm Obsidian):**

- [ ] **metrics-workspace.tsx** (root):
  - [ ] Swap old `SegmentedControl` from ui/ → Fluent `TabList`/`Tab` OR new Segmented from redesign/ui
  - [ ] Swap `Button` (Upload icon variant) → Fluent `Button` or redesign `Button`
  - [ ] Swap `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogTrigger` → Fluent `Dialog*` from redesign/ui
  - [ ] Swap old `SegmentedControl` → check if redesign/ui/segmented exists & use it
  - [ ] Layout grid (lg:grid-cols-[320px_1fr_300px]) stays; swap containers to Fluent `Card` or solid surfaces
  - [ ] Status sidebar: border + bg-surface + shadow → Fluent solid card (`Card` from redesign/ui, or native Fluent Card)
  - [ ] Wrap entire component in `FluentScope` (per ClientPageFrame pattern)

- [ ] **definitions-manager.tsx** (left panel):
  - [ ] Swap `Input`, `Label`, `Badge`, `Switch`, `Select`, `Dialog*`, `Button` → Fluent equivalents from redesign/ui
  - [ ] Container: solid card (Fluent `Card` + Fluent styling)
  - [ ] Reorder buttons (ChevronUp/Down icons): use Fluent `Button` size="icon"
  - [ ] DefDialog form: swap `Controller`/RHF with Fluent Field/Input/Select
  - [ ] Dialog styling: solid, not glass

- [ ] **monthly-entry-grid.tsx** (main form):
  - [ ] Input fields: swap custom input → Fluent `Input` (already solid)
  - [ ] Month picker: custom <input type="month"> + ChevronDown → Fluent `Select` or custom styled with Fluent tokens
  - [ ] Grid layout: swap "grid grid-cols-[1fr_72px_140px]" → Fluent Table or data-table
  - [ ] Sticky footer bar: swap custom CSS → Fluent glass pattern (if needed) or stay solid
  - [ ] Save/Discard buttons: Fluent `Button`
  - [ ] Error display: Fluent error styling (red tokens from redesign theme)
  - [ ] "unsaved" badge: Fluent `Badge` or custom with theme tokens

- [ ] **metrics-charts.tsx** (client preview):
  - [ ] `Select` (metric picker): Fluent `Select` or `Combobox`
  - [ ] Tables (numeric + text): Fluent `Table`/`TableHeader`/`TableRow`/`TableCell` (already solid)
  - [ ] Empty state icon + title/description: Fluent typography (`Title1`/`Subtitle2`) + icon (lucide)
  - [ ] Chart container: solid border/bg (stay as-is or wrap in Fluent Card)
  - [ ] MetricDelta inline badge: Fluent `Badge` or custom delta chip from redesign/data-ui

- [ ] **CSV import dialog:**
  - [ ] Dialog overlay: Fluent Dialog (already in redesign/ui)
  - [ ] Buttons (Download template, Commit): Fluent `Button`
  - [ ] FileDropzone: check if exists in redesign/ui; if not, keep old or wrap with Fluent tokens
  - [ ] CSV preview table: Fluent `Table` (already SOLID, correct for data)
  - [ ] Status badges (OK/error): Fluent `Badge` or inline spans with theme tokens
  - [ ] Instructions text: Fluent typography

**Structure & Wrapping:**

- [ ] Wrap **metrics-workspace** (and all its children) in `FluentScope` so Fluent + Warm Obsidian theme applies
- [ ] Set `width="standard"` on container (via PageContainer or similar)
- [ ] Keep `.section-stack` and grid gap logic (existing Tailwind utilities); verify they work with Fluent components
- [ ] Ensure `data-density="compact"` if staff-only mode is strict; else use default

**Preserve:**

- [ ] All server actions in lib/actions/metrics.ts (verbatim)
- [ ] All Supabase queries + RLS
- [ ] Audit logging
- [ ] Revalidate paths
- [ ] Current UX flow (enter month → modify → save; preview toggle; CSV import)

---

### Files Summary

**Routes:**
- `app/(portal)/clients/[clientId]/metrics/page.tsx` — page entry, guard, data fetch
- `app/(portal)/clients/[clientId]/metrics/loading.tsx` — skeleton (check if needs update)

**Components to re-skin (in order of dependency):**
- `components/metrics/metrics-workspace.tsx` — root orchestrator
- `components/metrics/definitions-manager.tsx` — sidebar left
- `components/metrics/monthly-entry-grid.tsx` — main form center
- `components/metrics/metrics-charts.tsx` — preview right (client surface)
- `components/metrics/metrics-line-chart.tsx` — Recharts wrapper (no UI layer changes)
- `components/metrics/csv-import.tsx` — dialog form overlay

**Server actions (preserve):**
- `lib/actions/metrics.ts` — all 7 actions + CSV commit

**Schemas & constants (already present):**
- `lib/schemas.ts` → `metricDefinitionSchema`, `MetricDefinitionValues`
- `lib/constants.ts` → `METRIC_UNITS`, `labelOf()`

**Utilities & helpers (keep):**
- `lib/format.ts` → `formatMonthYear`, `monthsBetween`, `formatMonthShort`
- `lib/auth/guards.ts` → `requireClientAccess`
- `lib/audit.ts` → `logAudit`


---

# Scope: staff-tabs-a

Perfect! Now I have all the information I need. Let me create the comprehensive markdown report:

---

# SCOPE: staff-tabs-a — Conversion Plan

Staff editor tabs for program clients: **Checklist, Program (Milestones), Content Calendar, Competitors Manager**. 
Converting R0 Tailwind editors → R2 Fluent UI (Warm Obsidian + ember-glass) while preserving all server actions, RLS, and business logic.

---

## (1) ROUTE / PAGE FILES + AUTH GUARD + DATA FETCHED

All staff pages live under `/clients/[clientId]/{tab}/page.tsx` and share the workspace layout.

| File | Guard | Data Fetched |
|------|-------|--------------|
| `app/(portal)/clients/[clientId]/checklist/page.tsx:8–32` | `requireClientAccess(clientId)` | `checklist_items`: id, kind, phase_label, title, link_url, assignee, sort_order, is_done, visible_to_client |
| `app/(portal)/clients/[clientId]/program/page.tsx:8–89` | `requireClientAccess(clientId)` | `clients`: id + 11 program fields (service_type, investment, dates, comms, etc.); `milestones`: id, title, description, phase_label, status, due_date, visible_to_client, sort_order |
| `app/(portal)/clients/[clientId]/content/page.tsx:6–28` | `requireClientAccess(clientId)` | `content_items`: id, title, platform, content_type, status, publish_date, cta, core_message, notes, asset_url, views_after_posting, visible_to_client |
| `app/(portal)/clients/[clientId]/competitors/page.tsx:12–32` | `requireClientAccess(clientId)` | `competitors`: id, name_or_handle, niche, follower_count, avg_views, top_content_format, hook_style, whats_working, gap_notes, adapted_idea, priority, visible_to_client |

**Layout**: `app/(portal)/clients/[clientId]/layout.tsx:14–80` — `requireClientAccess()` at layout level; renders `ClientTabs` component (`components/clients/client-tabs.tsx:23–80`) which drives tab nav for program clients only (projects get a different tab set).

---

## (2) RENDER COMPONENT TREE + R2 COVERAGE

### Checklist Tab
| Old (R0 Tailwind) | Path | New (R2) |
|---|---|---|
| `ChecklistEditor` | `components/checklist/checklist-editor.tsx:284–313` | Needs full re-skin (KindPanel layout, tree accordion, item rows) |
| `KindPanel` (internal) | `checklist-editor.tsx:68–282` | Stateful accordion; replace Badge/Button/Lucide → `Badge`, `Button` from redesign/ui + mode-aware tokens |
| `ChecklistItemDialog` | `components/checklist/checklist-item-dialog.tsx:49–160` | Dialog form: `Input`, `Label`, `Select`, `Switch` → all available in redesign/ui |

**Pattern**: Wrap editor at page level in `ClientPageFrame` (width="standard"). Replace plain `<div>` layout with FluentScope tokens for color/sizing. Glass only on dialogs/overlays; item list rows = solid background (rd-solid).

### Program Tab (ProgramForm + MilestonesEditor)
| Old (R0) | Path | New (R2) |
|---|---|---|
| `ProgramForm` | `components/program/program-form.tsx:17–114` | Form inside a Card body; re-skin with redesign/ui primitives |
| `MilestonesEditor` | `components/program/milestones-editor.tsx:192–340` | List of milestone cards; replace Badge/Select/Dialog → redesign/ui equivalents |
| `MilestoneDialog` | `milestones-editor.tsx:70–190` | Same dialog pattern as ChecklistItemDialog |

**Pattern**: Already have `ProgramBody` (R2 client read-only at `components/redesign/client/program-body.tsx:20–100`). Staff editor wraps both form + milestones in a `ClientPageFrame` with solid card surfaces for data entry.

### Content Tab (ContentCalendar)
| Old (R0) | Path | New (R2) |
|---|---|---|
| `ContentCalendar` | `components/content/content-calendar.tsx:52–380` | Table + Month view switcher; replace Table/Select/Badge → redesign/ui |
| `ContentDialog` | `components/content/content-dialog.tsx:52–218` | Form dialog: same primitives swap |

**Pattern**: Already have `ContentBody` (R2 client read-only at `components/redesign/client/content-body.tsx:19–115`) with identical list + month view logic. Staff editor = same visual structure + dialogs for CRUD.

### Competitors Tab (CompetitorsManager)
| Old (R0) | Path | New (R2) |
|---|---|---|
| `CompetitorsManager` | `components/competitors/competitors-manager.tsx:211–382` | Grid of competitor cards + manager UI; replace Badge/Button/Dialog → redesign/ui |
| `CompetitorDialog` | `competitors-manager.tsx:69–209` | Form dialog with 9 fields + priority/visibility switches |

**Pattern**: Card-based grid layout; solid cards (rd-solid) for the manager display, glass optional on dialogs if they carry data summaries (not required for forms).

---

## (3) STAFF WRITE PATHS (Server Actions) — PRESERVE VERBATIM

All in `lib/actions/{module}.ts`. **No changes to Supabase queries, RLS guards, or audit logging.**

### Checklist Actions (`lib/actions/checklist.ts`)
- **createChecklistItemAction** (line 21): Insert with sort_order, requires clientId + kind
- **updateChecklistItemAction** (line 71): Update phase_label/title/link_url/assignee/visible_to_client
- **deleteChecklistItemAction** (line 107)
- **toggleChecklistDoneAction** (line 131): Update is_done + done_by/done_at timestamp
- **setChecklistVisibleAction** (line 160): Visibility toggle
- **moveChecklistItemAction** (line 187): Swap sort_order with sibling (same kind)

### Program Actions (`lib/actions/program.ts`)
- **updateProgramAction** (line 13): Single action; updates client row with 11 fields

### Milestones Actions (`lib/actions/milestones.ts`)
- **createMilestoneAction** (line 19): Insert with sort_order
- **updateMilestoneAction** (line 66)
- **setMilestoneStatusAction** (line 103): Status enum update
- **deleteMilestoneAction** (line 129)
- **moveMilestoneAction** (line 153): Swap sort_order with adjacent milestone

### Content Actions (`lib/actions/content.ts`)
- **createContentItemAction** (line 45): Insert + created_by audit
- **updateContentItemAction** (line 72)
- **setContentStatusAction** (line 99): Status enum
- **setContentVisibilityAction** (line 124)
- **deleteContentItemAction** (line 149)

### Competitors Actions (`lib/actions/competitors.ts`)
- **createCompetitorAction** (line 38)
- **updateCompetitorAction** (line 65)
- **setCompetitorVisibilityAction** (line 92)
- **deleteCompetitorAction** (line 117)

**All actions**:
- Call `requireClientAccess(clientId)` → blocks non-admin/non-assigned staff
- Validate via Zod schema before mutation
- Audit-log via `logAudit()`
- Revalidate page path + `/clients/{id}` cache
- Return `{ ok: true } | { ok: false; error: string }`

---

## (4) INVARIANT / GUARD TOUCHPOINTS

### Internal-Thread Boundary
- **Page-level guard**: `requireClientAccess(clientId)` at page.tsx level (checked once per request).
- **Action-level guard**: Repeated in each server action for defense-in-depth.
- **RLS**: Implicit via Supabase `.eq("client_id", clientId)` filters — staff can only see/edit their assigned client(s).

### Timer / State Model
- **Checklist items**: `is_done` bool + `done_by` (user id) + `done_at` (ISO timestamp). Staff toggles trigger audit log with action `checklist_item.completed` or `checklist_item.reopened`.
- **Milestones**: `status` enum (upcoming|in_progress|done). No time-based auto-transitions.
- **Content items**: `status` enum (idea|drafting|in_review|approved|scheduled|published) + `publish_date` (optional date string). Staff-only control.

### Visibility Gating
- **`visible_to_client`**: Boolean on all four entities (checklist, milestones, content, competitors). Staff toggle controls what the client portal sees. Separate from RLS — staff always see all, visibility is *presentation control*.
- **Client reads**: Client portal only fetches rows where `visible_to_client = true` (enforced via RPC or query, not RLS).

### Project / Task Status Locks
- Not in scope (these tabs are program-client-only; projects get a different tab set per `ClientTabs` logic).

### Admin Destructive Guards
- **Hard delete**: No special guard; `requireClientAccess` is sufficient (admin or assigned staff only).
- **Visibility toggle**: No count/dependency check (staff can hide/show freely).
- **Sort reorder**: Swap only with immediate neighbor; no cyclic constraints.
- **No self-delete** / **last-admin** blocks: Not needed for staff-tabs-a (these are data entities, not user/role entities).

---

## (5) GLASS PLAN + CONTAINER WIDTH RULES

### Glass Rule (per R2 Warm Obsidian system)
- **Glass (with scrim)**: Chrome overlays (Dialog/MenuPopover), KPI summaries with text, sidebar chrome.
- **Solid**: Forms, tables, grids, list rows, data-entry cards.

### Per-Tab Breakdown

#### Checklist
- **Main container**: `ClientPageFrame` (width="standard") wrapping the entire page.
- **Accordion + rows**: Solid background (`rd-solid` or default card). Item rows = `bg-surface` equivalent.
- **Dialog** (add/edit item): Glass dialog surface with amber scrim (standard Fluent overlay).
- **No KPI summary needed** (no stats card).

#### Program
- **Main container**: `ClientPageFrame` (width="standard").
- **ProgramForm card**: Solid (rd-solid) — it's a data-entry form.
- **Milestones list + card previews**: Solid. Milestone status Select dropdown = standard Fluent (no glass override needed).
- **Dialog** (add/edit milestone): Standard glass dialog.
- **Optional**: Small "Program overview" KPI card (# of milestones, completion %) → glass-with-scrim if added, but not required by spec.

#### Content
- **Main container**: `ClientPageFrame` (width="standard").
- **ContentCalendar container**: Solid card (`rd-solid`) holding both the view switcher + table/month grid.
- **Table rows + month grid cells**: Solid (`rd-solid` background for month cells; table bg inherited from card).
- **Dialog** (add/edit content): Standard glass.
- **No KPI** (already in client-facing `ContentBody`; staff version is edit-only).

#### Competitors
- **Main container**: `ClientPageFrame` (width="standard").
- **Card grid**: `sm:grid-cols-2` layout. Each card = solid (`rd-solid`) with rounded corners (matching R2 style ~20px border-radius).
- **Card content**: Flex + text inside solid card; no inner glass.
- **Dialog** (add/edit competitor): Standard glass.

### Container Widths
- **Metrics tab**: Not in scope (staff-tabs-a is checklist/program/content/competitors only).
- **All four tabs**: Use `ClientPageFrame` default width="standard" (approx 1200px max, responsive to screen).
- No "wide" layout needed for these editors.

---

## (6) CONVERSION CHECKLIST

### Phase 1: Checklist Editor
- [ ] Create new file `components/redesign/staff/checklist-editor.tsx` (wrap old logic in Fluent + tokens).
- [ ] Re-skin KindPanel: replace Tailwind classes with FluentScope tokens + `tokens.*` color vars.
- [ ] Convert Button → `Button` from redesign/ui (includes EmberButton if needed for primary CTA).
- [ ] Convert Badge → `Badge` from redesign/ui.
- [ ] Convert SegmentedControl → `Segmented` from redesign/ui.
- [ ] Keep ChecklistItemDialog structure but swap Input/Label/Select/Switch to redesign/ui.
- [ ] Test: Accordion expand/collapse, reorder (up/down), visibility toggle, status toggle, add/edit/delete.

### Phase 2: Program (Form + Milestones)
- [ ] Create `components/redesign/staff/program-editor.tsx` (wraps ProgramForm + MilestonesEditor).
- [ ] Re-skin ProgramForm: Layout field groups with redesign/ui primitives. Keep 2-col grid for dates/investment.
- [ ] Re-skin MilestonesEditor: Replace old card styling with `rd-solid` + Fluent Card/Table if needed, or plain div with token colors.
- [ ] Convert MilestoneDialog Input/Label/Select/DatePicker/Switch → redesign/ui.
- [ ] Test: Form validation, save program details, add/edit/delete/reorder/status-change milestones.

### Phase 3: Content Calendar
- [ ] Create `components/redesign/staff/content-editor.tsx`.
- [ ] Re-skin ContentCalendar: Segmented view switcher, table view, month grid.
- [ ] Table: Use Fluent `Table` primitives or plain div grid with token colors (ensure solid background).
- [ ] Month grid: Copy structure from `ContentBody` (client-facing) but add action buttons (edit/delete/visibility).
- [ ] Convert ContentDialog Input/Label/Select/Textarea/DatePicker/Switch → redesign/ui.
- [ ] Test: Switch views (table ↔ month), filter platforms/statuses, add/edit/delete/visibility items.

### Phase 4: Competitors Manager
- [ ] Create `components/redesign/staff/competitors-editor.tsx`.
- [ ] Re-skin CompetitorsManager: Card grid (`sm:grid-cols-2`), each card = solid (`rd-solid`, 20px radius).
- [ ] Action buttons (edit/visibility/delete) in card top-right; data display in card body.
- [ ] Convert CompetitorDialog → redesign/ui (all form fields).
- [ ] Test: Add/edit/delete/visibility competitors, grid layout on mobile/desktop.

### Phase 5: Page-Level Integration
- [ ] Update `app/(portal)/clients/[clientId]/checklist/page.tsx` to import + render new redesign editor.
- [ ] Update `app/(portal)/clients/[clientId]/program/page.tsx` (same).
- [ ] Update `app/(portal)/clients/[clientId]/content/page.tsx` (same).
- [ ] Update `app/(portal)/clients/[clientId]/competitors/page.tsx` (same).
- [ ] Wrap each page body in `ClientPageFrame` with width="standard".
- [ ] Verify `requireClientAccess` still guards all pages.

### Phase 6: Styling + Tokens
- [ ] Define reusable token shortcuts (e.g., `const fg1 = tokens.colorNeutralForeground1; const divider = onDark ? "#231f19" : "#f1efe8"`) at top of each editor file.
- [ ] Apply `rd-rise` class to headline + card containers (lift them above ambient field).
- [ ] Use `Eyebrow` component for section labels (e.g., "Checklist" at the top).
- [ ] Test light/dark mode toggle: verify all colors pass AA contrast, backgrounds align with ambient field.

### Phase 7: Responsive + Accessibility
- [ ] Verify `ClientPageFrame` responsive widths (mobile → tablet → desktop).
- [ ] Check grid/table layouts collapse gracefully on small screens.
- [ ] Audit dialog overflow (max-h-[90vh] for tall forms).
- [ ] Test keyboard nav (Tab, Enter, Escape in dialogs).
- [ ] Verify Lucide icon colors adapt to mode (if using icons).

### Phase 8: Testing
- [ ] Create/read/update/delete for each entity type (items/milestones/content/competitors).
- [ ] Reorder (up/down) for checklist + milestones.
- [ ] Visibility toggle for all four entities.
- [ ] Status changes (done ↔ pending for checklist; upcoming/in_progress/done for milestones; idea→...→published for content; none for competitors).
- [ ] Page load: data correctly fetched + rendered.
- [ ] Toast notifications appear (via existing sonner + Button loading state).
- [ ] No console errors; audit logs fire on each action.

---

## FILE PATHS REFERENCE

### Pages (Update to use new editors)
- `app/(portal)/clients/[clientId]/checklist/page.tsx`
- `app/(portal)/clients/[clientId]/program/page.tsx`
- `app/(portal)/clients/[clientId]/content/page.tsx`
- `app/(portal)/clients/[clientId]/competitors/page.tsx`

### Old Components (Source of truth for logic; not modified)
- `components/checklist/checklist-editor.tsx` (KindPanel + ChecklistEditor logic)
- `components/checklist/checklist-item-dialog.tsx`
- `components/program/program-form.tsx`
- `components/program/milestones-editor.tsx`
- `components/content/content-calendar.tsx`
- `components/content/content-dialog.tsx`
- `components/competitors/competitors-manager.tsx`

### Server Actions (Preserve verbatim; no changes)
- `lib/actions/checklist.ts` (6 exports)
- `lib/actions/program.ts` (1 export: updateProgramAction)
- `lib/actions/milestones.ts` (5 exports)
- `lib/actions/content.ts` (5 exports)
- `lib/actions/competitors.ts` (4 exports)

### R2 Primitives to Import
- `@/components/redesign/ui` → Button, Segmented, Input, Label, Textarea, Switch, Select, Dialog, etc.
- `@/components/redesign/client/page-frame` → ClientPageFrame
- `@/components/redesign/themed-fluent` → FluentScope, useRedesignMode, tokens
- `@/components/redesign/ui/surfaces` → Eyebrow, GlassSurface (if overlays need glass; mostly use Dialog default)

### Auth + Guards
- `lib/auth/guards.ts:requireClientAccess()` — called at page level + in each action

---

## NOTES

1. **Consistency with R2**: Follow the patterns from `components/redesign/client/client-checklist.tsx` and `components/redesign/client/content-body.tsx` — both already re-skinned R2 read-only views. Staff editors use the same token palette + layout discipline.

2. **No Schema Changes**: Supabase schemas, RLS policies, RPC functions (if any) are unchanged. All data validation stays in `lib/schemas.ts` (Zod validators already in place for all entities).

3. **Audit Trail**: Every action already calls `logAudit()` with the user id, action name, entity type, and client id. No changes needed.

4. **Glass Rule**: Only overlays (Dialog, MenuPopover, Tooltip) and KPI chrome get glass. Forms, data rows, and cards = solid (`rd-solid`). This is already applied in R2 client surfaces; follow the same pattern.

5. **Tab Routing**: `ClientTabs` at `components/clients/client-tabs.tsx` already routes program clients to these four tabs. No routing changes needed.


---

# Scope: staff-tabs-b

Perfect! Now I have enough context. Let me compile the comprehensive mapping report:

---

## Staff-Tabs-B Scope Map

### (1) ROUTE/PAGE FILES + AUTH GUARD + DATA FETCHED

| Route | Page File | Guard | Exact Data Fetched |
|-------|-----------|-------|-------------------|
| `/clients/[clientId]/reports` | `app/(portal)/clients/[clientId]/reports/page.tsx:5-25` | `requireClientAccess(clientId)` | `reports.select("id, title, period_start, period_end, summary, pdf_path, published, published_at").eq("client_id", clientId).order("period_end", desc)` |
| `/clients/[clientId]/deliverables` | `app/(portal)/clients/[clientId]/deliverables/page.tsx:8-50` | `requireClientAccess(clientId)` | clients(client_type); deliverables(id, title, description, type, status, due_date, preview_url, visible_to_client, file_path, created_at, client_approved_at, project_id); [conditional] projects(id, title) when client_type='project' |
| `/clients/[clientId]/files` | `app/(portal)/clients/[clientId]/files/page.tsx:13-49` | `requireClientAccess(clientId)` | `files.select("id, name, category, storage_path, size_bytes, visible_to_client, created_at").eq("client_id", clientId).order("created_at", desc)` |
| `/clients/[clientId]/calls` | `app/(portal)/clients/[clientId]/calls/page.tsx:9-40` | `requireClientAccess(clientId)` | `call_types.select(...).eq("client_id", clientId).order("sort_order")`; `call_recordings.select(...).eq("client_id", clientId).order("call_date", desc)` |
| `/clients/[clientId]/notes` | `app/(portal)/clients/[clientId]/notes/page.tsx:5-25` | `requireClientAccess(clientId)` | `meeting_notes.select("id, title, meeting_date, body, visible_to_client").eq("client_id", clientId).order("meeting_date", desc)` |
| `/clients/[clientId]/updates` | `app/(portal)/clients/[clientId]/updates/page.tsx:1-49` | `requireClientAccess(clientId)` | `updates.select("id, title, body, pinned, visible_to_client, author_id, created_at").eq("client_id", clientId)`; author name enrichment via `profiles(id, full_name, email)` |

**Layout Guard** (all tabs): `app/(portal)/clients/[clientId]/layout.tsx:23` → `requireClientAccess(clientId)` returns Profile; fetches `clients(id, name, slug, status, program, client_type, start_date, logo_url)`

**Tab Navigation** (client-tabs.tsx): All six tabs are in "More" dropdown; project clients omit notes/reports/updates from primary tabs but include in "More".

---

### (2) RENDER COMPONENT TREE TO RE-SKIN + EXISTING R0/R1/R2 COVERAGE

**Reports** (`app/(portal)/clients/[clientId]/reports/page.tsx`)
- Page renders: `<ReportsManager clientId reports />`
  - Manager: `components/reports/reports-manager.tsx:173-299` (U/C)
    - ReportDialog: form + upload via `uploadClientFileAction`
    - List: cards with publish/unpublish/edit/delete buttons
  - **Needs**: Re-skin from Card/Button (old shadcn) → Fluent + ClientPageFrame + glass summary pattern

**Deliverables** (`app/(portal)/clients/[clientId]/deliverables/page.tsx`)
- Page renders: `<DeliverablesList clientId deliverables projects clientType />`
  - Manager: `components/deliverables/deliverables-list.tsx:43-85+` (U/C)
    - DeliverableDialog: form + optional file upload
    - List: filterable by status/visibility
  - **Needs**: Fluent + ClientPageFrame + dense table pattern (solid, no glass)

**Files** (`app/(portal)/clients/[clientId]/files/page.tsx`)
- Page renders: `<Card>` (old) + `<FileUpload/>` + `<FilesList/>`
  - FileUpload: `components/files/file-upload.tsx` (U/C form)
  - FilesList: `components/files/files-list.tsx` (U/C table)
  - **Needs**: Fluent forms + ClientPageFrame

**Calls** (`app/(portal)/clients/[clientId]/calls/page.tsx`)
- Page renders: `<CallsManager callTypes recordings />`
  - Manager: `components/calls/calls-manager.tsx:1-200+` (U/C)
  - Call types + recordings (two sub-lists)
  - **Needs**: Fluent + ClientPageFrame + glass for call-type cards (per R2 pattern in CallsNotesBody)

**Notes** (`app/(portal)/clients/[clientId]/notes/page.tsx`)
- Page renders: `<NotesManager clientId notes />`
  - Manager: `components/notes/notes-manager.tsx` (U/C)
  - New/edit dialogs + list
  - **Needs**: Fluent + ClientPageFrame

**Updates** (`app/(portal)/clients/[clientId]/updates/page.tsx`)
- Page renders: `<UpdatesList clientId updates currentUserId isAdmin />`
  - Manager: `components/updates/updates-list.tsx` (U/C)
  - Create/pin/visibility/delete per-update
  - **Needs**: Fluent + ClientPageFrame + dense list

**Existing R2 Pattern Reference**: `components/redesign/client/calls-notes-body.tsx:23-112`
- Uses `ClientPageFrame` → `FluentScope` + `AmbientField` + `Measure`
- Glass for call-type cards (`className={panel}` = `"rd-solid--dark"` or `"rd-solid"`)
- Forms/tables always solid
- Mode-aware colors via `tokens.colorNeutralForeground1/2/3`, `divider`, `surface2`

---

### (3) STAFF WRITE PATHS (SERVER ACTIONS / RPCs) — PRESERVE VERBATIM

**Reports**:
- `lib/actions/reports.ts:24-56` → `createReportAction(clientId, values, pdfPath?)`
- `lib/actions/reports.ts:58-86` → `updateReportAction(clientId, id, values, pdfPath?)`
- `lib/actions/reports.ts:89-127` → `setReportPublishedAction(clientId, id, published)` ← **PUBLISH/UNPUBLISH GATING**
- `lib/actions/reports.ts:129-159` → `deleteReportAction(clientId, id)` + orphaned PDF cleanup

**Deliverables**:
- `lib/actions/deliverables.ts:45-92` → `createDeliverableAction(clientId, values, filePath?)`
- `lib/actions/deliverables.ts:94-166` → `updateDeliverableAction(clientId, id, values, filePath?)`
- `lib/actions/deliverables.ts:168-216` → `setDeliverableStatusAction(clientId, id, status)` ← status lock pattern
- `lib/actions/deliverables.ts:218-242` → `setDeliverableVisibilityAction(clientId, id, visible)`
- `lib/actions/deliverables.ts:244-277` → `deleteDeliverableAction(clientId, id)` + file cleanup
- `lib/actions/deliverables.ts:285-324` → `setDeliverableApprovalAction(deliverableId, approved)` ← **4a CLIENT-ONLY RPC path** (calls `set_deliverable_approval` security-definer RPC)

**Files**:
- `lib/actions/files.ts:18-66` → `uploadDocumentAction(clientId, formData)`
- `lib/actions/files.ts:68-92` → `setFileVisibilityAction(clientId, id, visible)`
- `lib/actions/files.ts:94-128` → `deleteFileAction(clientId, id)` + storage cleanup

**Calls**:
- `lib/actions/calls.ts:21-63` → `createCallTypeAction(clientId, values)`
- `lib/actions/calls.ts:65-96` → `updateCallTypeAction(clientId, id, values)`
- `lib/actions/calls.ts:98-119` → `deleteCallTypeAction(clientId, id)`
- `lib/actions/calls.ts:133-157` → `createCallRecordingAction(clientId, values)`
- `lib/actions/calls.ts:159-184` → `updateCallRecordingAction(clientId, id, values)`
- `lib/actions/calls.ts:186-209` → `setCallRecordingVisibilityAction(clientId, id, visible)`
- `lib/actions/calls.ts:211-232` → `deleteCallRecordingAction(clientId, id)`

**Notes**:
- `lib/actions/notes.ts:23-48` → `createMeetingNoteAction(clientId, values)`
- `lib/actions/notes.ts:50-75` → `updateMeetingNoteAction(clientId, id, values)`
- `lib/actions/notes.ts:77-100` → `setMeetingNoteVisibilityAction(clientId, id, visible)`
- `lib/actions/notes.ts:102-123` → `deleteMeetingNoteAction(clientId, id)`

**Updates**:
- `lib/actions/updates.ts:40-75` → `createUpdateAction(clientId, values)`
- `lib/actions/updates.ts:77-112` → `updateUpdateAction(clientId, id, values)` + author/admin guard
- `lib/actions/updates.ts:114-138` → `deleteUpdateAction(clientId, id)` + author/admin guard
- `lib/actions/updates.ts:140-166` → `setUpdateFlagsAction(clientId, id, flags)` + author/admin guard (pinned / visible_to_client)

---

### (4) INVARIANT / GUARD TOUCHPOINTS IN SCOPE

**Reports: Draft vs. Published Gating**
- **Invariant**: Clients see only `published = true` (RLS enforced in `20260610010004_rls_policies.sql` → `reports_client_select`: `client_id = app.my_client_id() and published`)
- **Staff Action**: `setReportPublishedAction(clientId, id, published)` toggles the flag + notifies clients on publish (`notify(..., type: "report_published", link: "/performance#reports")`)
- **UI Guard**: Staff sees all reports (published/draft); "Publish" / "Unpublish" buttons visible
- **Data Flow**: `ReportsManager` receives all reports; UI shows status chip ("draft" / "published") + publish button

**Deliverables: Visibility + Client Approval + Status Lock**
- **Invariant**: Clients see only `visible_to_client = true` (no RLS on deliverables for client role, but staff sets visibility)
- **Staff Guard**: `setDeliverableVisibilityAction` + `setDeliverableStatusAction` (status: pending → in_progress → needs_review → delivered)
- **Client Guard** (4a): `setDeliverableApprovalAction` calls security-definer RPC (`set_deliverable_approval`); clients can only approve/unapprove their own client's visible deliverables (RPC enforces this)
- **Notification**: On deliverable transition to `delivered` (if visible), notify client users
- **Project Link**: When `client_type = "project"`, deliverables may link to `project_id` (validated in action via `resolveProjectId`)

**Files: Visibility + Signed URL**
- **Invariant**: Clients see only `visible_to_client = true`
- **Download**: Uses signed URL from `DownloadButton` (calls storage bucket with short TTL)
- **Upload**: `uploadDocumentAction` stores in `client-files` bucket; ties to `files` row with metadata

**Calls**: Recording Visibility
- **Invariant**: Clients see only `visible_to_client = true` on call_recordings
- **Call Types**: Staff-only (no client visibility flag); clients see them always
- **Recordings**: Staff toggles visibility via `setCallRecordingVisibilityAction`

**Notes / Updates**: Author/Admin Control**
- **Notes**: Staff create/edit/delete; visibility flag; no author/admin guard (any staff member can edit)
- **Updates**: Author-only edit/delete/pin, OR admin can do anything (see `assertOwnerOrAdmin` in updates.ts:20-38)

**Storage Cleanup Paths**:
- Deliverables: `deleteDeliverableAction` → check `file_path`, remove from `client-files` bucket
- Files: `deleteFileAction` → check `storage_path`, remove from `client-files` bucket
- Reports: `deleteReportAction` → check `pdf_path`, remove from `client-files` bucket

---

### (5) GLASS PLAN + CONTAINER WIDTH RULES

**Glass Architecture** (per `components/redesign/client/calls-notes-body.tsx` pattern):
- **Wrapper**: `ClientPageFrame` (width: `"standard"` for all six tabs)
  - Inside: `FluentScope` + `AmbientField` (fixed, full-bleed) + `Measure` (relative, readable measure)
  - Ambient field sits behind sticky chrome
- **Chrome/Overlays** (glass = `className={panel}` = `"rd-solid--dark"` on dark mode / `"rd-solid"` on light):
  - Calls: Call-type cards → glass (active, interactive surface)
  - Reports: Summary section above list → glass optional (consider summary "published" count as KPI-like, but may be solid)
- **Tables/Grids/Forms** (always solid, never glass):
  - Deliverables list → solid table
  - Files list → solid table
  - Calls: Recordings list → solid
  - Notes list → solid
  - Updates list → solid
  - All forms (dialog content) → solid (Fluent input/textarea/select)

**Width Rules**:
- All six tabs: `width="standard"` (readable measure for regular content)
- No "wide" override needed (metrics are separate, already handled in R1/R2)

**Mode-Aware Tokens** (from `useRedesignMode()` + `tokens` from Fluent):
```ts
const { mode } = useRedesignMode();
const onDark = mode === "dark";
const fg1 = tokens.colorNeutralForeground1, fg2 = tokens.colorNeutralForeground2, fg3 = tokens.colorNeutralForeground3;
const panel = onDark ? "rd-solid--dark" : "rd-solid";
const divider = onDark ? "#231f19" : "#f1efe8";
const surface2 = onDark ? "#231f19" : "#f4f4f0";
```

**Styling Guide**:
- Eyebrow: `<Eyebrow tone={onDark ? "onDark" : "amber"}>` + section label
- Headings: `className="rd-display"` + `style={{ fontSize, fontWeight, color: fg1 }}`
- Body text: `color: fg2` (secondary) / `fg3` (tertiary)
- Dividers: `borderTop: i === 0 ? "none" : \`1px solid ${divider}\``
- Card gap: `gap: "1.25rem"` outer / `gap: "0.75rem"` inner
- Padding: `paddingBlock: "clamp(0.5rem, 2vw, 1rem)"` for fluid spacing

---

### (6) CONVERSION CHECKLIST

- [ ] **Reports Tab**
  - [ ] Wrap in `ClientPageFrame(width="standard")`
  - [ ] Migrate `ReportsManager` from old Card/Button → Fluent dialog + solid form
  - [ ] Report list items: solid cards (not glass), mode-aware styling
  - [ ] Keep `setReportPublishedAction` action verbatim; test RLS client-sees-published-only
  - [ ] Publish/unpublish button UX; notification flow unchanged
  - [ ] PDF download via signed URL (existing DownloadButton)

- [ ] **Deliverables Tab**
  - [ ] Wrap in `ClientPageFrame(width="standard")`
  - [ ] Migrate `DeliverablesList` → Fluent form + dense solid table
  - [ ] DeliverableDialog: form migration to Fluent inputs
  - [ ] Status select: pending → in_progress → needs_review → delivered
  - [ ] Visibility toggle per deliverable
  - [ ] File upload: existing action preserved; render with Fluent file input
  - [ ] Project link: conditional on `clientType="project"` (keep logic as-is)
  - [ ] Verify `resolveProjectId` validation runs server-side (action-side); UI just shows dropdown
  - [ ] Test client-approval RPC path (4a separate, not staff-only)

- [ ] **Files Tab**
  - [ ] Wrap in `ClientPageFrame(width="standard")`
  - [ ] FileUpload form: Fluent inputs (category select, visible checkbox)
  - [ ] FilesList: solid table, mode-aware styling
  - [ ] Download button: signed URL (existing)
  - [ ] Visibility toggle, delete per file
  - [ ] Storage cleanup on delete (existing action preserved)

- [ ] **Calls Tab**
  - [ ] Wrap in `ClientPageFrame(width="standard")`
  - [ ] Call-type cards: solid (or glass if treating as interactive summary) + Fluent button to add recording
  - [ ] CallsManager: separate sections for call types + recordings
  - [ ] Recording form: Fluent inputs; add/edit/delete actions
  - [ ] Visibility toggle on recording
  - [ ] Keep booking URL logic as-is (cal.com integration separate)

- [ ] **Notes Tab**
  - [ ] Wrap in `ClientPageFrame(width="standard")`
  - [ ] NotesManager: Fluent dialog for create/edit
  - [ ] Notes list: solid, mode-aware styling
  - [ ] Visibility toggle per note
  - [ ] Keep author tracking; no author-guard (staff only)

- [ ] **Updates Tab**
  - [ ] Wrap in `ClientPageFrame(width="standard")`
  - [ ] UpdatesList: Fluent dialog for create/edit
  - [ ] Updates list: solid, mode-aware styling
  - [ ] Pin + visibility toggles per update
  - [ ] Author/admin guard (edit/delete/pin): keep `assertOwnerOrAdmin` action logic verbatim
  - [ ] Test author-only and admin overrides

- [ ] **Global**
  - [ ] No changes to server actions (preserve all `requireClientAccess`, audit logging, notifications, RLS)
  - [ ] No changes to Supabase queries or RLS policies
  - [ ] All manager components: move from `components/{*}/*.tsx` (old UI) → keep same paths; re-skin only
  - [ ] Test localStorage/dark mode toggle behavior in new Fluent theme context
  - [ ] Verify token imports: `import { ..., tokens } from "@/components/redesign/ui"`
  - [ ] Verify FluentScope context wrapping (ClientPageFrame already does this)

---

**File References**:
- Layout: `app/(portal)/clients/[clientId]/layout.tsx:14-84`
- Page routes: `app/(portal)/clients/[clientId]/{reports,deliverables,files,calls,notes,updates}/page.tsx`
- Tab nav: `components/clients/client-tabs.tsx:23-114`
- Manager components: `components/{reports,deliverables,files,calls,notes,updates}/*-manager.tsx`
- Server actions: `lib/actions/{reports,deliverables,files,calls,notes,updates}.ts`
- R2 pattern ref: `components/redesign/client/{page-frame,calls-notes-body}.tsx`
- UI exports: `components/redesign/ui/index.ts`
- Fluent theme: `components/redesign/themed-fluent.ts`


---

# Scope: admin-clients

Perfect. Now I have all the information needed. Let me create the comprehensive scope document:

## SCOPE: Admin Clients Conversion (R0 → R2)

### (1) ROUTE & PAGE FILES + AUTH GUARDS + DATA FETCHED

**Clients List (Admin Overview)**
- **Route:** `D:\Claude Code\fourpielabs-portal\app\(portal)\clients\page.tsx` (lines 1–177)
- **Auth Guard:** `requireRole(["admin", "team"])` (line 24)
  - Admin sees all clients; team sees assigned-only (RLS enforced)
- **Data Fetched:**
  - `clients`: `id, name, slug, program, client_type, status, logo_url` (line 29)
  - `checklist_items`: `client_id, is_done` filtered by `kind='onboarding'` (line 30)
  - `projects`: `client_id, status` (line 31)
- **Features:**
  - Desktop table (lines 93–141): client avatar + name/slug, program/type badge, status chip, progress (checklist done/total for program; project count for project clients), action buttons (open, settings [admin-only])
  - Mobile cards (lines 144–172): same layout, link-driven

**Create Client (Admin)**
- **Route:** `D:\Claude Code\fourpielabs-portal\app\(portal)\clients\new\page.tsx` (lines 1–18)
- **Auth Guard:** `requireRole(["admin"])` (line 7)
- **Data:** None (new record form)
- **Features:** Conditional form by `client_type` (program vs project)

**Client Settings (Admin)**
- **Route:** `D:\Claude Code\fourpielabs-portal\app\(portal)\clients\[clientId]\settings\page.tsx` (lines 1–99)
- **Auth Guard:** `requireRole(["admin"])` (line 23) — admin-only, no team access
- **Data Fetched:**
  - Client: `id, name, slug, industry, program, status, website_url, start_date, service_type, investment, comms_channel, internal_notes` (lines 27–33)
  - Team (for assignment): `id, full_name, email` where `role='team'` and `is_active=true` (lines 37–42)
  - Assignments: `user_id` for the client (lines 44–47)
- **Features:** 
  - Details card (ClientEditForm + form validation)
  - Team assignments card (AssignmentManager: add/remove team members)

---

### (2) RENDER COMPONENT TREE + R2 COVERAGE

**Clients Page Components:**

| Path | Current | R2 Target | Status |
|------|---------|-----------|--------|
| `D:\Claude Code\fourpielabs-portal\app\(portal)\clients\page.tsx` | PageContainer (R0) + PageHeader (R0) + Table (R0) / Cards (R0) | ClientPageFrame + FluentScope + Card/CardHeader/CardBody (Fluent) + Table/Avatar (Fluent) | Needs R2 wrap |
| Desktop table uses: Button, Badge, StatusChip, PersonAvatar (R0) | All R0 | Fluent equivalents | R2 migration |
| Mobile cards use: Badge, StatusChip (R0) | All R0 | Fluent equivalents | R2 migration |

**Create Client Components:**

| Path | Current | R2 Target | Status |
|------|---------|-----------|--------|
| `D:\Claude Code\fourpielabs-portal\app\(portal)\clients\new\page.tsx` | PageContainer (R0) + PageHeader (R0) + ClientCreateForm | ClientPageFrame (R2) + FluentScope + Card (solid) | Wrap page in ClientPageFrame |
| `D:\Claude Code\fourpielabs-portal\components\clients\client-create-form.tsx` | React Hook Form + Tailwind layout (lines 87–289) | Field + Input + Select (Fluent solid, lines 16–31 in redesign/ui/index.ts) + Fluent Label, DatePicker | Full R2 form conversion |

**Client Settings Components:**

| Path | Current | R2 Target | Status |
|------|---------|-----------|--------|
| Settings page layout | Card (R0) + CardHeader / CardContent | Card + CardHeader (Fluent, solid surface) + CardBody | Wrap in ClientPageFrame |
| `ClientEditForm` | React Hook Form + Tailwind inputs | Field + Input + Select (Fluent, solid) + Label (Fluent) | Full R2 form conversion |
| `AssignmentManager` | R0 Button + Select + AlertDialog | Fluent Button + Select + AlertDialog (already Fluent-compatible) | Minimal migration |

**Fluent Primitives Already in Scope (redesign/ui/index.ts):**
- Forms: `Input`, `Textarea`, `Label`, `Field`, `Select`, `Checkbox`, `Switch` (lines 17–30)
- Data: `Card`, `CardHeader`, `CardFooter`, `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableCell` (lines 64–77)
- Overlays: `Dialog`, `AlertDialog` (lines 39–60)
- Display: `Avatar`, `Badge`, `Text`, `Title*` (lines 77–102)

**Custom R2 Components:**
- `ClientPageFrame` — wraps page bodies with FluentScope + ambient field + measure (D:\Claude Code\fourpielabs-portal\components\redesign\client\page-frame.tsx)
- `Button` / `EmberButton` — Fluent Button with loading state + amber CTA (D:\Claude Code\fourpielabs-portal\components\redesign\ui\button.tsx)
- `FluentScope` — provider scoping theme to mode + transparent background (themed-fluent.tsx)
- `GlassSurface` — glass recipe (surfaces.tsx, used for chrome/summary only; NOT in forms)

**Glass Rule (Strict):**
- Glass (`rd-glass` / `GlassSurface`) only on: chrome headers, summary cards, KPI overlays
- **SOLID** (`rd-solid` / Fluent Card) on: tables, forms, dense data grids
- Use `mode === "dark"` to swap `rd-glass--dark` or `rd-solid--dark` classes

---

### (3) STAFF WRITE PATHS (SERVER ACTIONS / RPCs)

**All in `D:\Claude Code\fourpielabs-portal\lib\actions\clients.ts` (lines 1–206):**

| Action | Signature | Guard | Revalidate | Notes |
|--------|-----------|-------|-----------|-------|
| `createClientAction` | `(input: ClientCreateValues) → Promise<Result<{ id: string; inviteError?: string }>>` (lines 31–131) | `requireRole(["admin"])` | `/clients`, `/admin/users` | Creates client row. If `client_email` supplied, invites client portal user (welcome + set-password link). Program clients seeded by DB trigger (checklist, milestones, metrics). Project clients seeded nothing. |
| `updateClientAction` | `(input: ClientUpdateValues) → Promise<Result>` (lines 134–175) | `requireRole(["admin"])` | `/clients/${id}/settings`, `/clients` | Updates name, industry, program, status, website_url, start_date, service_type, investment, comms_channel, internal_notes. No client_type/slug changes. |
| `setClientStatusAction` | `(clientId: string, status: ClientStatus) → Promise<Result>` (lines 178–205) | `requireRole(["admin"])` | `/clients/${id}/settings`, `/clients` | Soft-delete via status = "paused" or "churned" (no hard delete in v1). |

**Schemas (lib/schemas.ts lines 18–60):**
- `clientCreateSchema`: name (min 1), slug (lowercase-hyphen), industry (enum), program (enum), client_type (enum), status (enum), website_url (optional URL), start_date (optional YYYY-MM-DD), client_email (optional), client_full_name (optional)
- `clientUpdateSchema`: id (uuid), name, industry, program, status, website_url, start_date, service_type, investment, comms_channel, internal_notes

**User Assignment Actions (lib/actions/users.ts — imported by AssignmentManager):**
- `assignUserAction(clientId, userId)` — add team member to client workspace
- `unassignUserAction(clientId, userId)` — remove team member

---

### (4) INVARIANT / GUARD TOUCHPOINTS

**Client Type Lock:**
- Program vs project is set at creation (line 51 in clients.ts) and never changes in updateClientAction
- Program clients seed: onboarding checklist, 90-day roadmap, metric definitions (DB trigger, after INSERT)
- Project clients seed nothing (use projects board instead of program tier)
- `client_type` hidden in edit form; visible in create form as first decision (clientCreateForm.tsx line 89–115)

**Status Workflow (Soft-Delete Only):**
- Enum: "onboarding" → "active" → "paused" or "churned" (no hard delete)
- Status change via `setClientStatusAction` or inline in `updateClientAction`
- Paused/churned clients remain in DB (RLS still restricts team member access unless via audit logs)

**Admin-Only Guards:**
- Settings page: `requireRole(["admin"])` (line 23 in settings/page.tsx)
- Create client: `requireRole(["admin"])` (line 7 in new/page.tsx)
- Settings button in clients list: render only if `isAdmin` (line 131–135 in clients/page.tsx)
- Settings action in client layout: render only if `profile.role === "admin"` (line 66–73 in layout.tsx)

**Team Assignment Invariants:**
- Assigned team members in client_assignments table; unassigned team can't see client workspace (RLS + requireClientAccess guard)
- Admin always has access (no assignment needed)
- Clients never reach team workspace (redirected to /dashboard)

**Last-Admin / Self-Delete Blocks:**
- Not yet implemented (P2 note in settings/page.tsx). Current system allows admins to unassign themselves.

---

### (5) GLASS PLAN + CONTAINER WIDTH

**Glass Rule (per redesign.css & surfaces.tsx):**

| Surface | Rule | Class | Implementation |
|---------|------|-------|-----------------|
| Clients list page header + page chrome | Glass, strong (scrim) | `rd-glass rd-glass--strong` (optional ember) | If converted to glass header: wrap header in GlassSurface |
| Desktop table container | SOLID | `rd-solid` (not glass) | Fluent Table wrapped in Card (solid, no glass) |
| Mobile cards | SOLID | `rd-solid` (not glass) | Fluent Card with solid surface |
| Create form background | SOLID | `rd-solid` | Fluent Form container in Card (solid) |
| Settings Details section | SOLID | `rd-solid` | Fluent Card with ClientEditForm |
| Settings Assignments section | SOLID | `rd-solid` | Fluent Card with AssignmentManager |
| Form inputs (all) | SOLID by design | Fluent Input/Select/Field inherit solid | No special styling needed |

**Container Width Rules (redesign.css --rd-measure-*):**
- Clients list: **wide** (1440px max, `--rd-measure-wide`) — dense table needs horizontal scroll room
- Create client: **text** (720px max, `--rd-measure-text`) — forms are single column
- Client settings: **text** (720px max) — form-heavy page

**Mode Awareness (light/dark):**
- All surfaces: Check `mode === "dark"` via `useRedesignMode()` and swap `--dark` class suffix
- Background field: `<AmbientField mode={mode} />` (fixed, behind all content)
- Text tokens: `tokens.colorNeutralForeground1/2/3` for dark/light mode (from redesign/ui)
- Accent: Amber (#b45309 light, #fcd34d dark) via Fluent theme

---

### (6) CONVERSION CHECKLIST

#### Clients List Page (`app/(portal)/clients/page.tsx`)
- [ ] Wrap entire return in `<ClientPageFrame width="wide">` (from redesign/client/page-frame.tsx)
- [ ] Migrate PageHeader to Fluent `Title1` + `Caption1` (or keep as styled div with eyebrow)
- [ ] Replace `<Button>` with `<Button appearance="primary">` (redesign/ui)
- [ ] Replace `<Badge>` with Fluent `Badge` (redesign/ui index.ts line 78)
- [ ] Replace `<StatusChip>` with Fluent `Badge` or custom status variant
- [ ] Replace `<PersonAvatar>` with Fluent `Avatar` (or keep if no redesign equiv; check icon names)
- [ ] Replace Table (R0) with Fluent Table from redesign/ui (line 68–76)
- [ ] Wrap table in Fluent Card with solid surface: `<Card className="rd-solid">`
- [ ] Wrap mobile cards in Fluent Card with solid surface
- [ ] Add mode-aware CSS: Check `useRedesignMode()` for dark mode, apply `rd-solid--dark` or `rd-glass--dark`
- [ ] Remove all Tailwind text color / shadow classes; use Fluent tokens or rd-* utility classes
- [ ] Test: desktop table, mobile cards, empty state, isAdmin conditional renders

#### Create Client Page (`app/(portal)/clients/new/page.tsx`)
- [ ] Wrap return in `<ClientPageFrame width="text">`
- [ ] Keep PageHeader but migrate typography if needed
- [ ] Component stays the same (ClientCreateForm imported)

#### Client Create Form (`components/clients/client-create-form.tsx`)
- [ ] Wrap form in Fluent `Card` with solid surface (line 88, replace div.max-w-xl)
- [ ] Replace all `<Label>` with Fluent `<Label>` (redesign/ui line 21)
- [ ] Replace all `<Input>` with Fluent `<Input>` wrapped in `<Field>` for error/validation (redesign/ui line 18, 21)
- [ ] Replace all `<Select>` with Fluent `<Select>` + `<SelectTrigger>` + `<SelectContent>` (may be shadcn; verify Fluent equiv in redesign/ui line 23)
- [ ] Replace `<DatePicker>` with Fluent date input or custom Fluent-compatible wrapper
- [ ] Replace `<Button>` with Fluent `<Button>` (redesign/ui line 13, or `<EmberButton>` for primary CTA)
- [ ] Add `useRedesignMode()` hook for dark mode styling
- [ ] Ensure all form sections use `rd-solid` background
- [ ] Remove all Tailwind background classes (bg-bg, bg-surface); use Fluent Card instead
- [ ] Test: conditional program field visibility, slug autofill, client account email optional flow, validation messages

#### Client Edit Form (`components/clients/client-edit-form.tsx`)
- [ ] Same as ClientCreateForm: wrap in Card, migrate to Fluent primitives
- [ ] Test: all fields update correctly, no unintended revalidates

#### Client Settings Page (`app/(portal)/clients/[clientId]/settings/page.tsx`)
- [ ] Wrap return in `<ClientPageFrame width="text">`
- [ ] Replace `<Card>` (R0) with Fluent `<Card>` (redesign/ui line 64)
- [ ] Replace CardHeader / CardContent with Fluent `<CardHeader>` + content div
- [ ] Apply `rd-solid` class to card containers
- [ ] AssignmentManager: verify Fluent Button/Select work in dark mode
- [ ] Test: edit form submits, assignments add/remove, dark mode toggle

#### Glass Integration (All Pages)
- [ ] Verify no glass on table, form, or data surfaces (only solid)
- [ ] If adding a header bar: use GlassSurface with `dark` + `strong` for scrim
- [ ] Test reduced-motion (glass degrades to solid, no blur) and reduced-transparency (opaque fallback)

#### Final Validation
- [ ] All shadcn/Tailwind UI imports removed in favor of redesign/ui or Fluent
- [ ] No hardcoded colors (use Fluent tokens: `tokens.colorNeutralForeground1`, etc.)
- [ ] Dark mode toggle works (check mode via `useRedesignMode()`)
- [ ] RLS + server actions untouched
- [ ] Audit logs still fire on create/update/status-change
- [ ] Revalidate paths preserved

---

## FILE REFERENCE SUMMARY

| Purpose | File | Key Lines |
|---------|------|-----------|
| Clients list | `app/(portal)/clients/page.tsx` | 1–177 |
| Create client page | `app/(portal)/clients/new/page.tsx` | 1–18 |
| Client create form | `components/clients/client-create-form.tsx` | 1–291 |
| Client edit form | `components/clients/client-edit-form.tsx` | 1–191 |
| Client settings page | `app/(portal)/clients/[clientId]/settings/page.tsx` | 1–99 |
| Assignment manager | `components/clients/assignment-manager.tsx` | 1–146 |
| Client tabs (layout) | `components/clients/client-tabs.tsx` | 1–114 |
| Client layout | `app/(portal)/clients/[clientId]/layout.tsx` | 1–80+ |
| Server actions | `lib/actions/clients.ts` | 1–206 |
| Schemas | `lib/schemas.ts` | 18–60 (client schemas) |
| Auth guards | `lib/auth/guards.ts` | 64–103 (requireRole, requireClientAccess) |
| R2 page frame | `components/redesign/client/page-frame.tsx` | 1–32 |
| Fluent scope | `components/redesign/themed-fluent.tsx` | 1–125+ |
| Surfaces (glass/solid) | `components/redesign/ui/surfaces.tsx` | 1–150+ |
| R2 button | `components/redesign/ui/button.tsx` | 1–76 |
| R2 primitives barrel | `components/redesign/ui/index.ts` | 1–125 |
| Glass CSS | `app/(redesign)/redesign.css` | 1–150+ |
| Constants | `lib/constants.ts` | PROGRAMS, CLIENT_TYPES, CLIENT_STATUSES, etc. |

---

**PRESENTATION ONLY — ZERO LOGIC CHANGES:**
- Server actions (`createClientAction`, `updateClientAction`, `setClientStatusAction`) remain verbatim
- Supabase queries, RLS, triggers, and audit logging untouched
- Client type + status invariants preserved
- Team assignment guards and admin-only surfaces intact


---

# Scope: admin-users-audit

Perfect! Now I have all the information I need to provide a comprehensive analysis. Let me compile the findings.

## Scope Analysis: admin-users-audit

### (1) Route/Page Files & Auth Guards

**Files:**
- **D:\Claude Code\fourpielabs-portal\app\(portal)\admin\users\page.tsx:30-266**
  - `requireRole(["admin"])` on line 31
  - Fetches: `profiles` (id, full_name, email, role, is_active, client_id, avatar_url), `clients` (id, name), `client_assignments` (user_id, client_id), auth users (via admin API listUsers)

- **D:\Claude Code\fourpielabs-portal\app\(portal)\admin\audit\page.tsx:38-191**
  - `requireRole(["admin"])` on line 43
  - Fetches: `audit_log` (id, actor_id, action, entity, entity_id, client_id, metadata, created_at) [latest 200, filterable by client_id + action], `clients` (id, name), `profiles` (id, full_name, email)
  - Filters: client=queryParam, action=queryParam

---

### (2) Render Component Tree (Paths to Re-Skin)

**Pages (currently old shadcn/Tailwind — need FluentScope conversion):**
- `app/(portal)/admin/users/page.tsx:84-266` — PageContainer/PageHeader + InviteForm card + Table (desktop) + Cards (mobile)
- `app/(portal)/admin/audit/page.tsx:73-190` — PageContainer/PageHeader + AuditFilters + Table (desktop) + Cards (mobile)

**Components (need re-skin):**
- `components/admin/invite-form.tsx` — Form with email/full_name/role fields + Button (already using @/components/ui/button; migrate to redesign/ui)
- `components/admin/user-active-toggle.tsx` — Deactivate/Reactivate AlertDialog + Delete ConfirmDeleteDialog (migrate Button + dialogs to redesign/ui)
- `components/admin/pending-invite-actions.tsx` — Resend AlertDialog + Revoke AlertDialog (migrate to redesign/ui)
- `components/admin/audit-filters.tsx` — Select dropdowns for client + action filtering (migrate to redesign/ui)
- `components/clients/assignment-manager.tsx:40-146` — Assigned team list + Add dropdown (used in /clients/[clientId]/settings; migrate to redesign/ui)

**R0/R1/R2 components already covering pieces:**
- `components/redesign/ui/button.tsx` — FluentUI Button + EmberButton (use for CTA)
- `components/redesign/ui/surfaces.tsx` — GlassSurface, Shell, Measure, Eyebrow (glass only on chrome/overlays)
- `components/ui/person-avatar.tsx` — Already imported in users page (keep as-is)
- `components/ui/status-chip.tsx` — Already imported (keep as-is or migrate if pattern exists in redesign)
- `components/layout/page-container.tsx` & `components/layout/page-header.tsx` — Already in use (check if they need FluentScope wrapper)

---

### (3) Write Paths (Server Actions / RPCs) — Preserved Verbatim

**D:\Claude Code\fourpielabs-portal\lib\actions\users.ts**

1. **sendInviteAction(input: InviteValues)** (lines 23-98)
   - Guard: `requireRole(["admin"])`
   - Action: Auth admin API inviteUserByEmail + profiles insert (via trigger) + invitations.insert
   - Audit: `user.invited` or `user.invite_failed`
   - Error handling: mapEmailSendError + logAudit on failure

2. **resendInviteAction(userId: string)** (lines 105-165)
   - Guard: `requireRole(["admin"])` + pending-only check (email_confirmed_at === null)
   - Action: Admin API deleteUser (cascade) → re-invite
   - Audit: `user.invite_resent` or `user.invite_failed`

3. **revokeInviteAction(userId: string)** (lines 168-192)
   - Guard: `requireRole(["admin"])` + self-delete blocked (line 170)
   - Action: Admin API deleteUser (pending invite only)
   - Audit: `user.invite_revoked`

4. **deleteUserAction(userId: string)** (lines 204-250)
   - Guard: `requireRole(["admin"])` + checkUserDeletable (line 226-233) **← HARD-DELETE INVARIANTS**
   - Action: Admin API deleteUser (cascades profile + personal state, SETs CREATED_BY/AUTHOR_ID NULL for authored content, actor_id NULL in audit)
   - Audit: `user.deleted` (log BEFORE delete to capture identity, line 237-243)
   - Revalidate: /admin/users

5. **setUserActiveAction(userId: string, isActive: boolean)** (lines 257-282)
   - Guard: `requireRole(["admin"])` + self-deactivate blocked (line 262-264)
   - Action: profiles.is_active update
   - Audit: `user.reactivated` or `user.deactivated`

6. **assignUserAction(clientId: string, userId: string)** (lines 285-321)
   - Guard: `requireRole(["admin"])` + target.role === "team" check (line 297-299)
   - Action: client_assignments.upsert (onConflict: client_id,user_id)
   - Audit: `assignment.created`
   - Revalidate: /clients/[clientId]/settings + /admin/users

7. **unassignUserAction(clientId: string, userId: string)** (lines 324-349)
   - Guard: `requireRole(["admin"])`
   - Action: client_assignments.delete
   - Audit: `assignment.removed`
   - Revalidate: /clients/[clientId]/settings + /admin/users

---

### (4) Invariant / Guard Touchpoints

**Internal-thread boundary:**
- None in scope; admin pages don't touch internal threads.

**Timer state model:**
- None in scope (task timers are staff-only but admin scope doesn't manage them).

**Project/task status locks:**
- None in scope; admin only manages user access + team assignments.

**Admin destructive guards (CRITICAL):**

1. **Self-delete blocked** (D:\Claude Code\fourpielabs-portal\lib\user-delete-guard.ts:18-19, enforced in deleteUserAction:205)
   - checkUserDeletable: `if (targetId === callerId) → error: "You can't delete your own account."`

2. **Last-active-admin blocked** (D:\Claude Code\fourpielabs-portal\lib\user-delete-guard.ts:21-23)
   - checkUserDeletable: `if (targetRole === "admin" && targetActive && activeAdminCount <= 1) → error: "You can't delete the last active admin."`
   - Enforced pre-action by querying COUNT of active admins (deleteUserAction:220-224)

3. **Type-the-exact-name HARD DELETE** (UI gate in components/admin/user-active-toggle.tsx:61-91)
   - ConfirmDeleteDialog with `confirmPhrase={label}` (line 73) — user must type the full name to confirm
   - Action deleteUserAction validates again server-side via checkUserDeletable

4. **Self-deactivate blocked** (setUserActiveAction:262-264)
   - `if (userId === admin.id && !isActive) → error: "You can't deactivate your own account."`

5. **Self-revoke blocked** (revokeInviteAction:170)
   - `if (userId === me.id) → error: "You can't revoke your own account."`

6. **Pending-only revoke** (revokeInviteAction:175-177)
   - `if (email_confirmed_at exists) → error: "This user already accepted — deactivate instead of revoke."`

7. **Pending-only resend** (resendInviteAction:111-113)
   - `if (email_confirmed_at exists) → error: "This user already accepted their invite."`

8. **Team-only assignment** (assignUserAction:297-299)
   - `if (target.role !== "team") → error: "Only team members can be assigned to clients."`

---

### (5) Glass Plan & Container Width Rules

**Glass rule:**
- **Chrome/overlays/summary = glass-with-scrim:** AlertDialogs (Deactivate, Revoke, ConfirmDelete), Popover overlays (if any)
- **Tables/grids/forms = SOLID:** 
  - InviteForm (Card with CardContent — solid)
  - User table (dense, solid)
  - Audit table (dense, solid)
  - AssignmentManager list (solid)
  - AuditFilters (Card with CardContent — solid)

**Container width:**
- Metrics page: N/A (admin scope has no metrics KPI surface)
- Users page: `width="wide"` (PageContainer line 85)
- Audit page: `width="wide"` (PageContainer line 74)
- Both pages are standard wide admin dashboards with data tables

---

### (6) Conversion Checklist

**Pages:**
- [ ] Wrap `app/(portal)/admin/users/page.tsx` in `FluentScope` + revalidate layout structure
- [ ] Wrap `app/(portal)/admin/audit/page.tsx` in `FluentScope`
- [ ] Convert `<PageContainer>` + `<PageHeader>` to use FluentUI typography/styling if needed
- [ ] Verify theme tokens (dark/light mode aware)

**Components:**
- [ ] **invite-form.tsx:** Replace Button from shadcn with `@/components/redesign/ui Button`; keep form structure (solid)
- [ ] **user-active-toggle.tsx:** Replace Button + AlertDialog + ConfirmDeleteDialog with `@/components/redesign/ui` equivalents
  - AlertDialog → Fluent Dialog (line 96-117, 102-115 deactivate; implies delete uses ConfirmDeleteDialog shadow which wraps Dialog)
  - ConfirmDeleteDialog → Fluent DialogSurface (custom wrapper or check if exists in redesign)
  - Buttons → redesign/ui Button
- [ ] **pending-invite-actions.tsx:** Replace Button + AlertDialog with redesign/ui equivalents
- [ ] **audit-filters.tsx:** Replace Button + Card + Select with redesign/ui equivalents (solid Card/data form)
- [ ] **assignment-manager.tsx:** Replace Button + AlertDialog + Select with redesign/ui equivalents

**Guard touchpoints:**
- [ ] Verify checkUserDeletable is called in deleteUserAction (already done, line 226)
- [ ] Verify UI ConfirmDeleteDialog enforces type-the-name phrase (already done, line 73)
- [ ] Audit log capture happens BEFORE delete (already done, line 237)
- [ ] Last-active-admin query count includes target if they're admin (already done, line 220-224 eq "admin" + eq "is_active" = true)
- [ ] Self-guards (self-delete, self-deactivate, self-revoke) pass through action layer (already done)

**Styling & modes:**
- [ ] Verify all components respect useRedesignMode() for dark/light (Table, Cards already do via Fluent)
- [ ] Ensure dense table density if needed (audit log table may need row-height tuning)
- [ ] Pending invite row background uses CSS variable (line 209, 131 `--pending-row`) — verify in theme

**No changes to:**
- sendInviteAction, resendInviteAction, revokeInviteAction, deleteUserAction, setUserActiveAction, assignUserAction, unassignUserAction
- logAudit calls
- requireRole guards
- checkUserDeletable logic
- RLS + audit_log schema

---

**File paths (absolute, for reference):**
- D:\Claude Code\fourpielabs-portal\app\(portal)\admin\users\page.tsx
- D:\Claude Code\fourpielabs-portal\app\(portal)\admin\audit\page.tsx
- D:\Claude Code\fourpielabs-portal\components\admin\invite-form.tsx
- D:\Claude Code\fourpielabs-portal\components\admin\user-active-toggle.tsx
- D:\Claude Code\fourpielabs-portal\components\admin\pending-invite-actions.tsx
- D:\Claude Code\fourpielabs-portal\components\admin\audit-filters.tsx
- D:\Claude Code\fourpielabs-portal\components\clients\assignment-manager.tsx
- D:\Claude Code\fourpielabs-portal\lib\actions\users.ts
- D:\Claude Code\fourpielabs-portal\lib\user-delete-guard.ts
- D:\Claude Code\fourpielabs-portal\lib\audit.ts
- D:\Claude Code\fourpielabs-portal\lib\audit-actions.ts
- D:\Claude Code\fourpielabs-portal\components\redesign\ui\button.tsx
- D:\Claude Code\fourpielabs-portal\components\redesign\ui\surfaces.tsx
- D:\Claude Code\fourpielabs-portal\components\redesign\ui\index.ts
